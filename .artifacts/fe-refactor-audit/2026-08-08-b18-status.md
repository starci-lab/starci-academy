# BATCH 18 — Contract canon before API migration

**Base:** `6c787529` · **branch:** `mtp` · **committed:** no

## Verdict

Primarily architecture design. **One** proposal passed the implementation gate using an **existing** canon frame (`StackV` / `content-row`). Every other hold stays a documented proposal requiring teacher/canon approval — **no APIs invented**.

## Implementation-ready (applied)

| Contract | Action | Gate |
|---|---|---|
| ModalShell.`bodyClassName` | Removed from SB twin; `WithLeadingTabs` stacks Tabs+ScrollShadow via `StackV principle="content-row"` | Existing StackV owns 12px seam (former Tailwind `gap-3`); src never had the prop; 0 remaining consumers |

**Not claimed:** inventing `bodyVariant`. The escape was deleted, not promoted.

## Contracts requiring teacher / canon approval (`implementationReady: false`)

| Proposal | Consumers | Why not ready |
|---|---|---|
| `dialogWidth` + `footerVariant` | MiniCart only (discrete) | New APIs — no existing DrawerShell size/footer token |
| `viewportFit="near-fullscreen"` | CvPreview `92vh×96vw` | New API; HeroUI `cover`/`full` ≠ that geometry |
| PDF height vocabulary | CvPreview `84vh` + story px set | Open arbitrary set; no canon enum |
| SurfaceCard `bodyVariant` | ContinueCard stack-clip ≠ PressableGroup TILE_CHROME | Multi-semantic; one enum leaves escapes |

## Vendor boundaries

**30/30 HeroUI** remain genuine vendor-boundary. Identical accidental wrappers: **0**. No fake StarCi wrappers.

## Skeleton redesign

Proposal only (23 paths). **No loading behavior changes.**

## Nivoexpert follow-up

SB `DrawerShell.contentClassName` — sole consumer LessonEditorPanel (`w-full sm:max-w-[560px]`). **Not edited.** Keep SB-only until a nivo-allowed batch migrates the consumer.

## Changed files

```
.storybook/components/composites/layout/ModalShell/ModalShell.tsx
.storybook/stories/composites/layout/ModalShell/ModalShell.stories.tsx
.claude/fe/decision-ledger.json
```

## Coordinator checks

| Check | Result |
|---|---|
| Overlapping edits | **0** |
| Storybook/src ModalShell bodyClassName | **aligned** (both lack prop) |
| Runtime namespace / plural principle | **none** |
| Teacher holds | **untouched** (27) |
| Invented APIs | **none** |

## Verification

| Gate | Result |
|---|---|
| `npx tsc --noEmit` | exit 0 |
| `npx eslint --max-warnings=0` changed | exit 0 |
| namespaces + authoring + contentpage | 8 pass |
| principle-style | 7 pass |
| `npm run audit:fe` | exit 0; 27 held; 0 failing |

## Workers

- `2026-08-08-b18-worker-modal-surface-contracts.json`
- `2026-08-08-b18-worker-commerce-dialog-contracts.json`
- `2026-08-08-b18-worker-cv-preview-pdf-contracts.json`
- `2026-08-08-b18-worker-nivo-boundary-proposal.json`
- `2026-08-08-b18-worker-heroui-boundary-ledger.json`
- `2026-08-08-b18-worker-skeleton-redesign-proposal.json`
