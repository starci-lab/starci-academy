# BATCH 19 — Implement approved semantic contracts

**Committed:** no

## Applied contracts

| API | Variants | Mapping |
|---|---|---|
| DrawerShell `dialogWidth` | `default` \| `cart` | cart → `sm:max-w-md` |
| DrawerShell `footerVariant` | `default` \| `stacked` | stacked → MiniCart footer classes |
| ModalShell `viewportFit` | `default` \| `near-fullscreen` | near-fullscreen → `h-[92vh] w-[96vw] max-w-[96vw]` |
| PDFView `height` | compact…viewport | see resolver; default `document` |
| SurfaceCard `bodyVariant` | `default` \| `stacked` \| `tile` | stacked/tile body layout; no `relative` |

Removed for these semantics: `dialogClassName`, `footerClassName`, `containerClassName`, `heightClassName`. ContinueCard off `contentClassName` → `bodyVariant="tile"`.

## Migrated consumers

| Consumer | Change |
|---|---|
| MiniCartDrawer | `dialogWidth="cart"` + `footerVariant="stacked"` |
| CvPreviewModal | `viewportFit="near-fullscreen"` + PDF `height="viewport"` |
| ContinueCard Item/Hero (src+SB) | `bodyVariant="tile"` |
| PDFView.stories | named `height` tokens only |

## Storybook / src parity

Resolvers + shell/viewer/card APIs mirrored. SB DrawerShell keeps nivoexpert `contentClassName`; src twin does not.

## Resolver tests

`.storybook/test-runner/semantic-contracts.test.mjs` — 7/7 pass (positive mappings + negative arbitrary CSS rejection + SB/src table identity).

## Remaining holds (not invented)

- PressableGroup `contentClassName` TILE_CHROME (ledger)
- LabeledCard / ShowcaseMockup `contentClassName` one-offs
- DrawerShell SB `contentClassName` (LessonEditorPanel)
- ResizableRail story-local `heightClassName` (not PDFView)
- HeroUI ×30 / skeletons / teacher ×27

## Verification

| Gate | Result |
|---|---|
| `tsc --noEmit` | 0 |
| eslint errors (all changed) | 0 |
| eslint `--max-warnings=0` on new/clean migrate files | 0 |
| eslint max-warnings on ModalShell/PDFView/SurfaceCard/MiniCart | pre-existing held warnings only |
| semantic-contracts + plugin + principle tests | pass |
| `audit:fe` | 0 failing / 27 held |

## Canon / ledger

- Updated `starci-academy-backend/.../overlay-and-feedback.md` §§4–5
- Ledger: `b19-semantic-contracts-2026-08-08` applied; PressableGroup chrome hold opened
