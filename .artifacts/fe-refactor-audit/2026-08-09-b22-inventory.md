# BATCH 22 — inventory (pre-edit)

Checkpoint: `90009dc5`

## Proven chrome axis

| Finding | Class | Action |
|---|---|---|
| PressableGroup `TILE_CHROME` via `contentClassName` | intrinsic-semantic | `chromeVariant="tile"` |
| ContinueCard Item/Hero | body layout only | Keep `bodyVariant="tile"`; **no** chrome change (preserves default surface chrome) |

## Boundary proposals (no code edits)

1. DrawerShell `contentClassName` → LessonEditorPanel (nivoexpert)
2. ShowcaseMockup `contentClassName` → LearnLoopScroll (locked)
3. PressableGroupItem `span` for `col-span-2`

## Axes stay independent

- `chromeVariant` → outer chrome only (`rounded-2xl shadow-field` for `tile`)
- `bodyVariant` → body layout only
