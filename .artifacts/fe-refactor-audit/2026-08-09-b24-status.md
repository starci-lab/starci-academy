# BATCH 24 — Ratchet-ready CSS-door burn

**Committed:** no  
**Checkpoint:** `bf54bc28`

## Inventory delta (`starci-fe/no-public-classname-prop`)

| | Hits | Files |
|---|---:|---:|
| Before | 2492 | 936 |
| After | 2216 | 818 |
| Delta | **−276** | **−118** |

Rule severity unchanged (not flipped to `error`). Vendor allowlist unchanged.

## Applied code

Deleted proven-dead public `classNames` / `className` doors (zero JSX consumers on open-tag proof) across:

- atoms (display/feedback/forms/media) + shared `_input` / `_select` field types + `FieldSkeleton` / `TriggerSkeleton`
- composites (buttons/cards/chips/feedback/form/layout/lists/navigation/stats/text/viewers)
- frames (HideAbove, PageEndPad, RailShell, ResponsiveRow, ScrollArea, Split, SplitWorkspace, …)

SB → src twins mirrored where present (~134 product files).

| Special case | Action |
|---|---|
| `PinnedTrack.classNames` | **Restored** — live consumer `InnerLayout` (`flex-1`) |
| FlashcardReviewHistory `InputSearch` `classNames={["w-full"]}` | Migrated to `Box className="w-full"` (vendor boundary) |

## Holds (not claimed fixed)

- Button / Chip / Avatar / Typography / Stack / Grid live `classNames` APIs
- SurfaceCard chrome/body + residual `contentClassName`
- DrawerShell `contentClassName` (nivoexpert LessonEditorPanel)
- ShowcaseMockup `contentClassName` (locked LearnLoopScroll)
- MiniCart, CvPreview, PDFView, ModalShell B19–B23 contracts
- Vendor-boundary Box + HeroUI wrappers
- Nivo / Nivoexpert / locked paths / 27 teacher holds
- Badge / Logo / Spinner / Alert singular `className` call sites

## Workers

`atoms` · `composites` · `frames` · `pages-primary` (consumer migrate) · others empty this wave  
Artifacts: `2026-08-09-b24-worker-*.json`

## Verification

| Gate | Result |
|---|---|
| `tsc --noEmit` | pass |
| eslint sample (SnippetIcon, HighlightCard, form types) | pass `--max-warnings=0` |
| plugin + principle + semantic tests | 24/24 |
| `npm run audit:fe` | pass (27 teacher holds) |
| rule → error | **not done** |
| commit | **no** |
