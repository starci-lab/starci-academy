# BATCH 31h1 — Close Footer vocabulary and mass host slices

**Status:** partial (Footer + vocabulary complete; mass host remaining)  
**Committed:** no

## Verdict

Landed **ShowFrom**, **Measure**, and **InlineLink** (SB→src), then closed **Footer** on both trees at zero warnings. Host inventory 1799 → **1774** (−25). Pages remain blocked by `page-folder-two-files-only`.

## Contracts

### ShowFrom
| `at` | Classes |
|---|---|
| sm | `hidden @app-sm:flex` |
| md | `hidden @app-md:flex` |
| lg | `hidden @app-lg:flex` |

API: `at`, `body`, `isSkeleton?` — visibility only.

### Measure
| `size` | Class |
|---|---|
| sm | `max-w-sm` |
| md | `max-w-md` |
| lg | `max-w-lg` |

API: `size`, `body`, `isSkeleton?` — Footer-proven Tailwind scale (not Container `max-w-app-*`).

### InlineLink
- **Label:** `label`, `size?: sm|xs`, `onPress?`, `href?`, `isSkeleton?`
- **Icon:** `icon`, `ariaLabel`, `onPress?`, `href?`, `isSkeleton?`
- Derived from Footer consumers (onPress-first); no `className` door.

## Footer before → after

| | Before | After |
|---|---|---|
| Root | raw `<footer>` + Container | **FooterFrame** `identity={block, Footer}` |
| Public CSS / `cn` | src yes | **removed** |
| Wordmark visibility | `div.hidden @app-md:flex` | **ShowFrom at=md** |
| Brand width | `div.max-w-sm` | **Measure size=sm** |
| Links | HeroUI Link | **InlineLink** |
| Wordmark text | `div` chrome | **`span`** (Typography lacks 8px/uppercase) |

Both SB and src Footer + FooterLinkColumn: eslint `--max-warnings=0` pass.

## Host hits

| | Before | After |
|---|---:|---:|
| Occurrences | 1799 | 1774 |
| Files | 722 | 712 |
| `footer` tags (sentence) | 8 | 5 |

## Holds

- **Page-folder** (~80 nested page files) — moves forbidden  
- Most remaining blocks/layouts — domain hosts beyond these three owners  
- `layouts/Footer` connected shell — separate from block twin  
- src `FlashcardModeSwitch` — Tabs has no identity prop

## Gates

`tsc` · eslint owned files · sentence-hosts / public-contracts / frame-items / sentence-tier · principle-style · semantic-contracts · `audit:fe` · `git diff --check` — **all pass**

## Artifacts

`.artifacts/fe-refactor-audit/2026-08-09-b31h1-*`
