# BATCH 31r1 — IconTile / IdentityTile split

**Status:** blocked (no product edits; prior draft remains retracted)  
**Checkpoint:** `18f84f5b`  
**Re-attempt:** 2026-08-08 (session continuation) — same immutable contract, same blockers.

## Verdict

Approved destination `composites/identity/IdentityTile` **cannot** own `isSkeleton` chrome under the composite gates:

| Gate | Conflict |
|---|---|
| **COMPOSITE-3** | composites must not import vendor (`@heroui` Skeleton) |
| **COMPOSITE-10** | composite forwards `isSkeleton`; atoms draw shimmer shape |
| **COMPOSITE-8** | if composite declares `isSkeleton`, icon must be `ComponentType`, not `ReactNode` |

`audit:fe` failed COMPOSITE-10 on the earlier draft that drew `HeroSkeleton` inside the composite. That draft was retracted. Working tree still has **no** `composites/identity/` and **no** Storybook IdentityTile.

## Size contracts (approved — unchanged)

| Component | Path | Size map |
|---|---|---|
| `IconTile` (keep) | `atoms/display/IconTile` | sm 40 / md 64 / lg 80, all `rounded-full` |
| `IdentityTile` (new) | `composites/identity/IdentityTile` | sm 48 + `rounded-xl`, md 64 + `rounded-2xl`, lg 80 + `rounded-2xl` |

Atom `IconTile` left untouched. Block `blocks/identity/IconTile` still live (**35** import files under `src` / `.storybook`).

## Consumer migration ratchet

Unlocked block→IdentityTile candidates still fail `--max-warnings=0` on the **existing** file (pre-existing sentence-tier debt). Touching only the import still fails verification.

Held (batch-forbidden or locked): TopLearners internals, CommunityTab, LeaderboardListCard, page-folder consumers, Nivo/Nivoexpert.

**Deletion of `blocks/identity/IconTile` is impossible** while those holds remain. Retract law: do not leave a partial IdentityTile beside an undeletable block path.

## Honest path that keeps the approved composite import path

1. **Leaf atom** owns identity chrome + vendor shimmer (size map above), same pattern as atom `IconTile`.
2. **`composites/identity/IdentityTile`** composes that atom, forwards `isSkeleton`, uses `ComponentType` icon (COMPOSITE-8).
3. Separate zero-warn cleanup (or unlock) of consumers, then migrate; delete block only at zero live imports.

Filing the leaf atom as `atoms/display/IdentityTile` and making the composite a thin forwarder also works for gates, but invents a dual-home name; prefer a distinctly named chrome atom if the composite must stay the public word.

## Recommendation (amendment required)

Either:

- **A.** Amend filing to **`atoms/display/IdentityTile`** only (drop composite shelf for this leaf), or  
- **B.** Approve a **leaf atom + forwarding composite** pair under `composites/identity/IdentityTile`, and a follow-on batch for consumer zero-warn unlocks before block deletion.

Do not re-land a composite that imports `HeroSkeleton`.
