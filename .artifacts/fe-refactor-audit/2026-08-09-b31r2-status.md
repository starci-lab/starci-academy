# BATCH 31r2 — IdentityTile mass migration

**Status:** partial (atom landed; migration blocked by ratchet)  
**Checkpoint:** `18f84f5b`  
**Contract:** `.claude/fe/contracts/identity-tile.md`

## Delivered

| Item | Result |
|---|---|
| Storybook `atoms/display/IdentityTile` | landed + stories |
| Src mirror | byte-identical twin |
| Circular `IconTile` | unchanged |
| Consumer migrations | **0** (no zero-warn unlocked files) |
| Legacy `blocks/identity/IconTile` | **kept** (33 live import files) |

### Size map (IdentityTile)

```text
sm → 48px + rounded-xl
md → 64px + rounded-2xl
lg → 80px + rounded-2xl
```

API: `ComponentType` icon, `isSkeleton`, tones include `neutral`, no public CSS doors.

## Migration hold

Every legacy block consumer fails `eslint --max-warnings=0` on pre-existing debt (page-folder, sentence-tier, HeroUI, etc.). Per contract, incremental coexistence is approved — IdentityTile is **not** retracted.

Held clusters (see inventory / worker JSONs):

- blocks-consumers (8) including LeaderboardListCard (batch-forbidden)
- profile-pages-consumers (4)
- learning-pages-consumers (6)
- commerce-dashboard-consumers (7)
- remaining-unlocked-consumers (8)

## Gates

| Check | Result |
|---|---|
| `tsc --noEmit` | pass |
| eslint changed files | pass |
| plugin + storybook node tests | pass |
| `npm run audit:fe` | pass |
| `git diff --check` | pass |

## Completion

Not complete: legacy imports ≠ 0; unlocked consumers not migrated. Next: zero-warn unlock batches, then migrate and delete the block path.
