# BATCH 31r3 — Unlock and migrate block IdentityTile consumers

**Status:** partial (3 migrated, 3 held)  
**Checkpoint:** `8ccf8641` (`refactor(fe): migrate initial IdentityTile consumers`)  
**B31r2:** `94c5f8fb` (committed before this wave)  
**Contract:** `.claude/fe/contracts/identity-tile.md`

## Migrated (zero-warning)

| Target | Change | Where |
|---|---|---|
| `PitchCard` | `IdentityTile` `md`; `IdentityTileIcon` prop | `8ccf8641` |
| `PlaygroundCard/component` | `SurfaceCard` + `IdentityTile` `lg` | `8ccf8641` |
| `PremiumPaywall` | `IdentityTile` `sm` + `LockIcon`; `PriceTagInline isSkeleton`; ContentPaywall column (no `pt-6` / max-w Boxes) | working tree |

## Held (not edited)

| Target | Blocker |
|---|---|
| `ReadinessChecklist` | `ReactNode` icon API vs `IdentityTile` `ComponentType`; provider type leak outside manifest |
| `CartLine` | nested `min-w-0 flex-1` has no typed frame |
| `EnrollGate` | Medium float / `CARD_SURFACE` / fade need new vocabulary; SB twin drifted — drop-float rewrite not behaviorally exact |

## Imports

| | Count |
|---|---|
| Legacy block live consumers before wave | 33 |
| After this wave | 30 |
| Legacy block deleted | **no** |

## Gates

`tsc`, eslint on changed files, plugin + Storybook node tests, `audit:fe`, `git diff --check` — all pass.
