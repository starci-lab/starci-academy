# BATCH 31r4 — List density and base fill contracts

Status: **complete** (not committed)

Checkpoint named `8ccf8641`; work continued from later HEAD without checkout.

## Contracts landed

### A — ListRow density

| | |
|---|---|
| API | `density?: "default" \| "comfortable"` |
| default | existing `py-2` (unchanged) |
| comfortable | exact `p-3` |
| Home | composite `List.Row` (Storybook first, src mirror) |

`blocks/lists/ListRow` left untouched (still 9 sentence-tier warnings). The only exact `className="p-3"` consumer — `ReadinessChecklist` — migrates onto composite `ListRow`.

### B — FillAvailable `base`

| `at` | classes | `data-principle` |
|---|---|---|
| `base` | `min-w-0 flex-1` | `flex-fill-base` |
| `lg` | `min-h-0 @app-lg:flex-1` | `flex-fill` (unchanged) |

Registered in SB+src `_principles` / `_principle-style`, `patterns.mjs`, backend `registry.mjs` + spacing overview. Existing `at="lg"` consumers remain equivalent.

## Migrations

| Target | Before | After |
|---|---|---|
| ReadinessChecklist | `ListRow className="p-3"`, ReactNode icons, legacy `IconTile` | `density="comfortable"`, `IdentityTileIcon`, `IdentityTile size="sm"`, identity root |
| PlaygroundSessionProvider | JSX icon nodes | ComponentType icons (`RobotIcon`, `SiDocker`/`SiKubernetes`, `Ollama`, …) |
| CartLine | raw `min-w-0 flex-1`, HeroUI, legacy `IconTile` | `FillAvailable at="base"`, house Button/Typography, `IdentityTile` |

## Warnings (changed product files)

| File | before | after |
|---|---|---|
| `composites/lists/List` | 8 | 0 |
| `ReadinessChecklist` | 2 | 0 |
| `CartLine` | 3 | 0 |
| `PlaygroundSessionProvider` | 0 | 0 |

## Holds / overlap

- No holds. No retracts of IdentityTile or prior migrations.
- PremiumPaywall / EnrollGate intentionally out of scope (forbidden).
- Disjoint worker manifests — no file overlap.

## Gates

All required verification commands passed (`tsc`, eslint max-warnings=0 on changed files, plugin tests, principle-style, semantic-contracts, `audit:fe`, `git diff --check`).
