# B36+B37c handoff

## Verdict

**READY TO CERTIFY** — do not commit until explicitly asked.

## What this certify package contains

1. **B36** contract unlocks (NavbarFrame, GlyphMark, CallerIdentity, Auth panel/ModalShell, prior SurfaceList migrations) still on the worktree.
2. **B37** whole-composite ownership collapses (LabeledCard/SurfaceListCard → SurfaceCardList and related exact E/F migrations).
3. **B37c corrections**
   - Removed frozen-ruleset violation `starci-fe/no-redundant-labeled-surface`.
   - Compressed TrendingContents to `_TrendingContents → SurfaceCardList` (nav in connected `index.tsx`).

## Do not

- Re-register architectural ESLint rules while actionable debt remains.
- Start another mass LabeledCard/SurfaceListCard migration in this certify package.
- Commit `CLAUDE.md`, `.claude/fe/decision-ledger.json`, or pre-dirty mia-mia SiteFooter with this package.

## Next (separate batch)

- Co-migrate outside-owns row roots so the 6 audit-only residual shells can collapse.
- Delete `frameless` / legacy `SurfaceListCard` only after Decision E/F gates are met.

## Commit guidance (when asked)

One combined commit covering B36+B37+B37c, excluding pre-dirty holds listed above.
