# B36 handoff

## Verdict

**READY TO CERTIFY** at checkpoint `84b92cd77`.

## What landed

- **NavbarFrame** (named frame) + SB/src Navbar outer-root migration
- **GlyphMarkTone.muted** + exact muted size-5 consumer migrations
- **CallerIdentity** on PageHeader/Toolbar/Callout/Alert/FAB/ResponsiveBreadcrumb/SearchAutocomplete (+ atom-local `_identity` for ATOM-3)
- **AuthenticationPanel** content extracted; ModalShell owns modal chrome; LoginPage two-file + same panel
- **SurfaceCard.List** exact migrations (8); SurfaceListCard retained for held consumers

## Numbers

6265/1104 → 6215/1086 (−50/−18). Below expected band — see status for inventory vs exact-match explanation.

## Certify next (B36c)

1. Remeasure ESLint vs checkpoint
2. Exact scope ownership for all retained paths
3. Re-prove 14-door / forbidden / ChatPane unchanged where claimed
4. Diff-audit auth behavior (OAuth/OTP/remember-me) and Navbar sticky landmark
5. Confirm SurfaceListCard.className retention is justified by held consumers
6. Confirm Alert SB/src ATOM-11 parity stays green

## Do not start B37 until certified and committed.

## Suggested commit message (after cert)

```
refactor(fe): unlock NavbarFrame, GlyphMark muted, identity roots, auth panel

Land named NavbarFrame and muted GlyphMark; forward CallerIdentity on reusable
roots; separate AuthenticationPanel from ModalShell chrome; migrate exact
SurfaceListCard consumers to SurfaceCard.List.
```
