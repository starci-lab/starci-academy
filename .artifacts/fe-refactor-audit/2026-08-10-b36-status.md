# B36 status

## Verdict

**READY TO CERTIFY**

Do not commit until asked. Do not start B36c until certified.

## Normalized ESLint

| | Warnings | Files | Errors | a11y |
|---|---:|---:|---:|---:|
| Checkpoint `84b92cd77` | 6265 | 1104 | 0 | 11 |
| After B36 | 6215 | 1086 | 0 | 11 |
| Δ | **−50** | **−18** | 0 | 0 |

Below the 300–700 / 60–140 expected band because:

1. **SurfaceListCard** — ~75 import sites, but only **8** exact `SurfaceCard.List` migrations; majority held (ReactNode rows, accordion hybrid, per-row hooks). `className` door retained while held consumers remain.
2. **GlyphMark muted** — only exact `size-5` + `shrink-0` + `text-muted` triad migrated (**4** families); other inventory hits lacked `shrink-0` or were size-4/hover.
3. **Identity** — unlock on roots + **5** clean consumer stamps; composites `PageHeader` family held.
4. **Auth** — ownership separation (ModalShell + AuthenticationPanel), not a broad host burn.
5. **NavbarFrame** — outer sticky landmark closed; internal HeroUI vocabulary holds remain.

## Contracts

| Contract | Result |
|---|---|
| A NavbarFrame | SB+src named frame; SB/src `blocks/navigation/Navbar` migrated |
| B GlyphMark muted | `muted → text-muted`; consumers migrated where exact |
| C CallerIdentity | Forwarded on approved roots; atom-safe `_identity` for Alert/SearchAutocomplete |
| D Auth separation | `AuthenticationModal → ModalShell + AuthenticationPanel`; LoginPage same panel |
| E SurfaceListCard | 8 migrated / majority held; no new SurfaceCard variants |

## Forbidden proof

Zero B36 product diffs under nivo / nivoexpert / mia-mia (SiteFooter story pre-dirty only).

## Gates

`tsc`, plugin tests, principle-style, semantic-contracts, `audit:fe`, `git diff --check` — all **0**.

## Recommended commit message

```
refactor(fe): unlock NavbarFrame, GlyphMark muted, identity roots, auth panel

Land named NavbarFrame and muted GlyphMark; forward CallerIdentity on reusable
roots; separate AuthenticationPanel from ModalShell chrome; migrate exact
SurfaceListCard consumers to SurfaceCard.List.
```
