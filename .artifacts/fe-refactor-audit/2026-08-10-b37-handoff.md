# B37 handoff

## Verdict

**READY TO CERTIFY** — do not commit until an explicit commit ask.

## What landed

1. Phase 0 topology + zero-overlap manifests.
2. Storybook/src SurfaceCard / LabeledCard / SurfaceListCard contract notes (`frameless` migration-deprecated).
3. Exact A/C/G collapses onto `SurfaceCardList` owning `SurfaceLabelProps` + rows/skeleton/identity.
4. TrendingContents unlock host (`TrendingContentsSurface`) with the required single-list tree.
5. `starci-fe/no-redundant-labeled-surface` (warn) + fixtures/tests.
6. Gates all green.

## What deliberately remains

| Residual | Why |
|---|---|
| LabeledCard framed consumers | Class F legitimate generic framed bodies |
| frameless call sites | Class E / dynamic frameless / StatRibbon / collections — no invent |
| SurfaceListCard | Outside-owns row islands (`CourseRow`, `ChallengeCourseRow`, `EntityResultRow`, `JobListRow`, `ProblemRow`, `BookmarkCard`), ReactNode feeds, nested free-form rows |
| 6 rule warnings | Telemetry on held redundant shells — do not flip to error yet |

## Next batch candidates

1. Co-migrate outside-owns row roots (`EntityResultRow`, `CourseRow`, `ChallengeCourseRow`, Job/Problem/Bookmark rows) so parent shells can drop `SurfaceListCard`.
2. ActivityFeed / MindMapNodeDrawer / RelatedContentList after row-contract unlock.
3. Accordion misclassifications → `SurfaceCardAccordion` ownership pass.
4. Delete `frameless` only after residual E/H unlocks reach zero.
5. Delete legacy `SurfaceListCard` only after Decision F proof (parity + zero allowed/forbidden imports).
6. Flip `no-redundant-labeled-surface` to error when its exact live hit count is 0.

## Commit guidance (when asked)

Worktree still contains **B36 + B37**. Prefer either:

- commit B36 alone first (NavbarFrame / GlyphMark / identity / Auth / prior SurfaceList migrations), then B37; or
- one combined certify commit with an explicit B36+B37 message.

Exclude pre-dirty holds: `CLAUDE.md`, `.claude/fe/decision-ledger.json`, mia-mia SiteFooter story.

## Artifacts

See `2026-08-10-b37-status.json` for the full artifact roster and gate table.
