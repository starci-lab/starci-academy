# B38 — Close residual labeled-surface shells

**Verdict: NOT READY**

Checkpoint: `2a096210a`  
Do not commit.

## Summary

Closed **5 / 6** audit residuals with Exact A → `SurfaceCardList` ownership. **ProfileChallenges** remains open (held): collapse is correct, but the touched-file zero-warning ratchet cannot clear page-folder / AsyncContent / parallel-skeleton / classname debt without a broader page→blocks relocation.

## Classifications

| Residual | Class | Status | Composite owner |
|---|---|---|---|
| ActivityFeed | A | closed | per-day `SurfaceCardList` |
| RelatedContentList | A | closed | `SurfaceCardList` nested |
| MindMapNodeDrawer | A | closed | per-group `SurfaceCardList` |
| ProfileChallenges | A | **held** | still `LabeledCard` + `SurfaceListCard` |
| ProfileJobReadiness A | A | closed | one `SurfaceCardList` (+ move to blocks) |
| ProfileJobReadiness B | A | closed | same |

Class B/C/D: none.

## Anatomy (closed)

| Target | Before | After |
|---|---|---|
| ActivityFeed | LabeledCard frameless + SurfaceListCard + FeedItem | `SurfaceCardList` (label=day, subtleLabel, nested\|surface) |
| RelatedContentList | LabeledCard frameless + SurfaceListCard + button row | `SurfaceCardList` + body-only `EntityResultRow` + item `onPress` |
| MindMapNodeDrawer | per-group LabeledCard + SurfaceListCard; parallel skeleton | per-group `SurfaceCardList` + `isSkeleton` placeholders |
| ProfileJobReadiness | two LabeledCard/SurfaceListCard shells | one `SurfaceCardList` (label, skeleton, empty, error, tracks) at `blocks/profile/ProfileJobReadiness` |

## Deleted / moved

- Deleted page nest: `src/components/pages/ProfileOverviewPage/ProfileJobReadiness/`
- Added: `src/components/blocks/profile/ProfileJobReadiness/index.tsx`
- Zero-consumer proof: Overview imports `@/components/blocks/profile/ProfileJobReadiness` only
- No wrapper-only skeleton twins deleted beyond in-file branch removal
- ProfileChallenges migration **reverted** (no retained product diff)

## Storybook / src parity

- `SurfaceCardList`: **no contract edits** (class B empty; surface-contract-parity agent)
- ProfileJobReadiness / ActivityFeed / RelatedContentList: **no Storybook twin** (recorded)

## Holds (not claimed fixed)

1. **ProfileChallenges** — Exact A shell still in audit (`2026-08-10-b38-redundant-labeled-surface.json`)
2. **ContentAiChat** — co-consumer search migration retracted; optional `EntityResultRow.onSelect` + unused `ChatToolResult.icon?: unknown` bridge

## ESLint

| | Warnings | Files | Errors | a11y |
|---|---:|---:|---:|---:|
| Baseline (B37d) | 6174 | 1084 | 0 | 11 |
| After B38 | 6150 | 1080 | 0 | 11 |
| Δ | **−24** | **−4** | 0 | 0 |

All retained changed TS/TSX files: `eslint --max-warnings=0` **pass**.

## Gates

| Gate | Result |
|---|---|
| plugin public-contracts / sentence-tier / namespaces / authoring / contentpage | pass |
| principle-style / semantic-contracts | pass |
| `npm run audit:fe` | pass |
| `git diff --check` | pass |
| `tsc --noEmit` | known unrelated only: LandingPage/KnowledgeGraph; `graphql/clients/links/options.ts` |

## Overlaps / forbidden

- Product ownership disjoint after coordinator move of JobReadiness + Overview import
- No Nivo / mia-mia / teacher-held / LandingPage KnowledgeGraph / graphql options edits
- No new ESLint rules
- No commit

## Why not READY TO CERTIFY

One of the six required residuals (**ProfileChallenges**) is still open. Closing it needs a dedicated page-folder relocation + AsyncContent retirement vertical, not a residual-only shell swap.
