# B39 status — ReactNode contract closure (navigation + marketing)

**Verdict: READY TO CERTIFY** (partial closure)

Checkpoint before edit: `2a096210a20c83bcc125067a5d0da1c951fc21e2` (`mtp`)

## Summary

Inventoried all non-`children` ReactNode props under `blocks/navigation` and `blocks/marketing`. Applied only ratchet-safe dead closures. Every live host except `PitchCard` carries pre-existing HeroUI / `cn` / host-element debt; B39 forbids HeroUI migration and requires touched files to finish at `eslint --max-warnings=0`, so all other migrations and dead deletes were retracted.

## Counts (excluding `children`)

| Metric | Value |
|---|---|
| ReactNode props before | 45 |
| ReactNode props after | 39 |
| string applied | 0 |
| ComponentType applied | 0 |
| structured applied | 0 |
| rich retained | 2 |
| mixed held | 4 |
| dead deleted (applied) | 6 |
| dead deleted (retracted by ratchet) | 5 |
| migrate (retracted by ratchet) | 18 |

## Deleted (applied)

- `PitchCard.title` / `body` / `footer?` (props + render; shell kept)
- Entire orphan modules: `TopicLane/`, `MicroservicesScene/`, `MicroservicesDiagram/`

## Retained ReactNode (evidence)

- `TruthList.byline` — LandingPage rich founder signature fragment
- `HeroBanner.headline` — `t.rich` with accent markup

## Mixed held

- `FlexWrapButtonRadio.content`
- `SidebarNavItem.endContent`
- `TabsCardItem.label`
- `WorkSessionHeader.rightSlot`

## Blocker for retracted work

`zero-warn-ratchet ∩ pre-existing-architecture-debt ∩ no-heroui-migration`

Retracted dead deletes still on API: `FlexWrapButtonRadio.itemAction`, `SelectableCardItem.icon|badge`, `TabsCard.leftEnd`, `WorkSessionHeader.meta`.

## Verification

| Gate | Result |
|---|---|
| `eslint --max-warnings=0` PitchCard | pass |
| plugin public-contracts / sentence-tier / namespaces / authoring | pass |
| principle-style / semantic-contracts | pass |
| `npm run audit:fe` | pass |
| `git diff --check` | pass |
| `tsc --noEmit` | fail — 5 pre-existing errors outside B39 paths |

## Changed files

- `src/components/blocks/marketing/PitchCard/index.tsx`
- deleted `TopicLane/`, `MicroservicesScene/`, `MicroservicesDiagram/`
- `.claude/fe/decision-ledger.json` (B39 decision)

No commit.
