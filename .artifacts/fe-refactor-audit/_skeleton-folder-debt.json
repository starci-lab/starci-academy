{
  "starci-fe/no-inline-skeleton-branch": {
    "all": [
      {
        "file": "src/components/blocks/commerce/TierCardBase/index.tsx",
        "line": 81,
        "col": 22,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/commerce/TierCardBase/index.tsx",
        "line": 96,
        "col": 22,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/commerce/TierCardBase/index.tsx",
        "line": 113,
        "col": 30,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/dashboard/JobReadinessWidget/index.tsx",
        "line": 84,
        "col": 12,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/dashboard/WeeklyChallengeCard/index.tsx",
        "line": 172,
        "col": 24,
        "msg": "`data.endsInLabel != null || isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/dashboard/WeeklyGoals/index.tsx",
        "line": 104,
        "col": 20,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/dashboard/WeeklyGoals/index.tsx",
        "line": 143,
        "col": 20,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/ChallengeHeader/index.tsx",
        "line": 146,
        "col": 21,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/ChallengeScoreCard/index.tsx",
        "line": 87,
        "col": 14,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/ContentDiscussion/index.tsx",
        "line": 167,
        "col": 17,
        "msg": "`!isSkeleton && comments.length === 0 ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/ContentHeader/index.tsx",
        "line": 98,
        "col": 14,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/ContinueCard/CardBody.tsx",
        "line": 65,
        "col": 43,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/ContinueCard/ContinueCardItem/index.tsx",
        "line": 27,
        "col": 25,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/MockInterviewScorecard/index.tsx",
        "line": 287,
        "col": 14,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/ModuleChallengeList/index.tsx",
        "line": 96,
        "col": 33,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/ModuleContinueBand/index.tsx",
        "line": 125,
        "col": 22,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/ModuleLessonList/index.tsx",
        "line": 110,
        "col": 29,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/PersonalProjectDashboard/index.tsx",
        "line": 206,
        "col": 24,
        "msg": "`isSkeleton || currentTask ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/PlaygroundSetupSteps/index.tsx",
        "line": 176,
        "col": 24,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/PlaygroundSetupSteps/index.tsx",
        "line": 199,
        "col": 34,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/PlaygroundSetupSteps/index.tsx",
        "line": 245,
        "col": 24,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/SubmissionResultHeader/index.tsx",
        "line": 51,
        "col": 21,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/TaskBriefBody/index.tsx",
        "line": 189,
        "col": 17,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/personal-project/TaskActions/index.tsx",
        "line": 173,
        "col": 30,
        "msg": "`isPending ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/composites/buttons/InputButtonLike/index.tsx",
        "line": 95,
        "col": 30,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/composites/form/ImageDropzone/index.tsx",
        "line": 102,
        "col": 14,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/composites/form/ImageDropzone/index.tsx",
        "line": 115,
        "col": 17,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/composites/layout/Page/index.tsx",
        "line": 158,
        "col": 17,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/composites/layout/Page/index.tsx",
        "line": 201,
        "col": 17,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/overlays/drawers/MiniCartDrawer/component.tsx",
        "line": 139,
        "col": 16,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/overlays/modals/PaymentModal/component.tsx",
        "line": 393,
        "col": 9,
        "msg": "`method.isPending ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/ArchitecturePage/index.tsx",
        "line": 117,
        "col": 26,
        "msg": "`isLoading && !healthByName && !error ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/BlogPostPage/component.tsx",
        "line": 167,
        "col": 20,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/CommunityChatPage/ChatPane/component.tsx",
        "line": 117,
        "col": 17,
        "msg": "`isSkeleton || !row.author ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/CommunityChatPage/ChatPane/component.tsx",
        "line": 150,
        "col": 25,
        "msg": "`!isSkeleton && isEmpty ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/ContentPage/component.tsx",
        "line": 291,
        "col": 38,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/CourseDetailPage/component.tsx",
        "line": 106,
        "col": 26,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/CourseDetailPage/component.tsx",
        "line": 118,
        "col": 26,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/CourseDetailPage/component.tsx",
        "line": 123,
        "col": 26,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/DashboardPage/ContinueLearning/component.tsx",
        "line": 79,
        "col": 12,
        "msg": "`isSkeleton || resumeItems.length > 0 ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/DashboardPage/StreakStrip/component.tsx",
        "line": 145,
        "col": 37,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/DashboardPage/StreakStrip/component.tsx",
        "line": 150,
        "col": 37,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/DashboardPage/WeeklyChallengeCard/component.tsx",
        "line": 152,
        "col": 20,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/DashboardPage/WhoToFollow/component.tsx",
        "line": 88,
        "col": 24,
        "msg": "`isSkeleton || !user ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/DashboardPage/WhoToFollow/component.tsx",
        "line": 103,
        "col": 24,
        "msg": "`isSkeleton || !user ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewHistory/component.tsx",
        "line": 221,
        "col": 32,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewStats/component.tsx",
        "line": 191,
        "col": 22,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardStatsStrip/component.tsx",
        "line": 144,
        "col": 28,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/JobDetailPage/component.tsx",
        "line": 193,
        "col": 52,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/JobDetailPage/component.tsx",
        "line": 249,
        "col": 56,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/JobDetailPage/component.tsx",
        "line": 260,
        "col": 56,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/KpiPage/component.tsx",
        "line": 129,
        "col": 9,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/KpiPage/component.tsx",
        "line": 230,
        "col": 9,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/LandingPage/StatStrip/component.tsx",
        "line": 148,
        "col": 14,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/LandingPage/StatStrip/component.tsx",
        "line": 155,
        "col": 14,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/LeaguePage/GlobalBoard/component.tsx",
        "line": 202,
        "col": 28,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/LeaguePage/GlobalBoard/component.tsx",
        "line": 241,
        "col": 28,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/LeaguePage/WeeklyBoard/component.tsx",
        "line": 250,
        "col": 25,
        "msg": "`isSkeleton || !hero ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/LeaguePage/WeeklyBoard/component.tsx",
        "line": 266,
        "col": 25,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewSession/index.tsx",
        "line": 1556,
        "col": 66,
        "msg": "`isPending ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewSession/index.tsx",
        "line": 1577,
        "col": 70,
        "msg": "`isPending ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/PracticeHubPage/CodingLeaderboard/index.tsx",
        "line": 74,
        "col": 24,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      }
    ],
    "unlocked": [
      {
        "file": "src/components/blocks/commerce/TierCardBase/index.tsx",
        "line": 81,
        "col": 22,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/commerce/TierCardBase/index.tsx",
        "line": 96,
        "col": 22,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/commerce/TierCardBase/index.tsx",
        "line": 113,
        "col": 30,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/dashboard/JobReadinessWidget/index.tsx",
        "line": 84,
        "col": 12,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/dashboard/WeeklyChallengeCard/index.tsx",
        "line": 172,
        "col": 24,
        "msg": "`data.endsInLabel != null || isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/dashboard/WeeklyGoals/index.tsx",
        "line": 104,
        "col": 20,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/dashboard/WeeklyGoals/index.tsx",
        "line": 143,
        "col": 20,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/ChallengeHeader/index.tsx",
        "line": 146,
        "col": 21,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/ChallengeScoreCard/index.tsx",
        "line": 87,
        "col": 14,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/ContentDiscussion/index.tsx",
        "line": 167,
        "col": 17,
        "msg": "`!isSkeleton && comments.length === 0 ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/ContentHeader/index.tsx",
        "line": 98,
        "col": 14,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/ContinueCard/CardBody.tsx",
        "line": 65,
        "col": 43,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/ContinueCard/ContinueCardItem/index.tsx",
        "line": 27,
        "col": 25,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/MockInterviewScorecard/index.tsx",
        "line": 287,
        "col": 14,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/ModuleChallengeList/index.tsx",
        "line": 96,
        "col": 33,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/ModuleContinueBand/index.tsx",
        "line": 125,
        "col": 22,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/ModuleLessonList/index.tsx",
        "line": 110,
        "col": 29,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/PersonalProjectDashboard/index.tsx",
        "line": 206,
        "col": 24,
        "msg": "`isSkeleton || currentTask ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/PlaygroundSetupSteps/index.tsx",
        "line": 176,
        "col": 24,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/PlaygroundSetupSteps/index.tsx",
        "line": 199,
        "col": 34,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/PlaygroundSetupSteps/index.tsx",
        "line": 245,
        "col": 24,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/SubmissionResultHeader/index.tsx",
        "line": 51,
        "col": 21,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/TaskBriefBody/index.tsx",
        "line": 189,
        "col": 17,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/blocks/learn/personal-project/TaskActions/index.tsx",
        "line": 173,
        "col": 30,
        "msg": "`isPending ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/composites/buttons/InputButtonLike/index.tsx",
        "line": 95,
        "col": 30,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/composites/form/ImageDropzone/index.tsx",
        "line": 102,
        "col": 14,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/composites/form/ImageDropzone/index.tsx",
        "line": 115,
        "col": 17,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/composites/layout/Page/index.tsx",
        "line": 158,
        "col": 17,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/composites/layout/Page/index.tsx",
        "line": 201,
        "col": 17,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/overlays/drawers/MiniCartDrawer/component.tsx",
        "line": 139,
        "col": 16,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/overlays/modals/PaymentModal/component.tsx",
        "line": 393,
        "col": 9,
        "msg": "`method.isPending ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/ArchitecturePage/index.tsx",
        "line": 117,
        "col": 26,
        "msg": "`isLoading && !healthByName && !error ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/BlogPostPage/component.tsx",
        "line": 167,
        "col": 20,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/CommunityChatPage/ChatPane/component.tsx",
        "line": 117,
        "col": 17,
        "msg": "`isSkeleton || !row.author ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/CommunityChatPage/ChatPane/component.tsx",
        "line": 150,
        "col": 25,
        "msg": "`!isSkeleton && isEmpty ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/ContentPage/component.tsx",
        "line": 291,
        "col": 38,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/CourseDetailPage/component.tsx",
        "line": 106,
        "col": 26,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/CourseDetailPage/component.tsx",
        "line": 118,
        "col": 26,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/CourseDetailPage/component.tsx",
        "line": 123,
        "col": 26,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/DashboardPage/ContinueLearning/component.tsx",
        "line": 79,
        "col": 12,
        "msg": "`isSkeleton || resumeItems.length > 0 ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/DashboardPage/StreakStrip/component.tsx",
        "line": 145,
        "col": 37,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/DashboardPage/StreakStrip/component.tsx",
        "line": 150,
        "col": 37,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/DashboardPage/WeeklyChallengeCard/component.tsx",
        "line": 152,
        "col": 20,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/DashboardPage/WhoToFollow/component.tsx",
        "line": 88,
        "col": 24,
        "msg": "`isSkeleton || !user ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/DashboardPage/WhoToFollow/component.tsx",
        "line": 103,
        "col": 24,
        "msg": "`isSkeleton || !user ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewHistory/component.tsx",
        "line": 221,
        "col": 32,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewStats/component.tsx",
        "line": 191,
        "col": 22,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardStatsStrip/component.tsx",
        "line": 144,
        "col": 28,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/JobDetailPage/component.tsx",
        "line": 193,
        "col": 52,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/JobDetailPage/component.tsx",
        "line": 249,
        "col": 56,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/JobDetailPage/component.tsx",
        "line": 260,
        "col": 56,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/KpiPage/component.tsx",
        "line": 129,
        "col": 9,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/KpiPage/component.tsx",
        "line": 230,
        "col": 9,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/LeaguePage/GlobalBoard/component.tsx",
        "line": 202,
        "col": 28,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/LeaguePage/GlobalBoard/component.tsx",
        "line": 241,
        "col": 28,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/LeaguePage/WeeklyBoard/component.tsx",
        "line": 250,
        "col": 25,
        "msg": "`isSkeleton || !hero ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/LeaguePage/WeeklyBoard/component.tsx",
        "line": 266,
        "col": 25,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/PracticeHubPage/CodingLeaderboard/index.tsx",
        "line": 74,
        "col": 24,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      }
    ],
    "locked": [
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewSession/index.tsx",
        "line": 1556,
        "col": 66,
        "msg": "`isPending ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewSession/index.tsx",
        "line": 1577,
        "col": 70,
        "msg": "`isPending ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      }
    ],
    "hold": [
      {
        "file": "src/components/pages/LandingPage/StatStrip/component.tsx",
        "line": 148,
        "col": 14,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      },
      {
        "file": "src/components/pages/LandingPage/StatStrip/component.tsx",
        "line": 155,
        "col": 14,
        "msg": "`isSkeleton ? … : …` picks between two DIFFERENT elements — that is a resting shape written by hand at the call site, and it drifts from the real one the first time the real one changes. Give the component below an `isSkeleton` prop and pass the flag down; let it rest as ITSELF. A ternary is fine when both arms are the same component."
      }
    ]
  },
  "starci-fe/no-skeleton-twin-component": {
    "all": [
      {
        "file": "src/components/blocks/cards/CourseCardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`CourseCardSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/cv/CvBlocksWorkspace/CvWorkspaceSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`CvWorkspaceSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/ContentMap/ContentMapSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`ContentMapSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/MilestoneOutline/MilestoneOutlineSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`MilestoneOutlineSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/MockInterviewScorecard/ScoreRowSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`ScoreRowSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/PlaygroundSetupSteps/CommandSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`CommandSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/PlaygroundSetupSteps/OsTabsSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`OsTabsSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/lesson/ChallengeBody/ChallengeCardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`ChallengeCardSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/lesson/CodeBodySkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`CodeBodySkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/lesson/ContentBodySkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`ContentBodySkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/lesson/ContentHeaderSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`ContentHeaderSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/lesson/LessonBody/LessonCardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`LessonCardSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/personal-project/PersonalProjectDashboard/PersonalProjectDashboardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`PersonalProjectDashboardSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/personal-project/TaskResultsSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`TaskResultsSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/personal-project/TaskSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`TaskSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/overlays/modals/ManagePinnedProjectsModal/ManagePinnedListSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`ManagePinnedListSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/AdminAiBalancerPage/AdminAiBalancerSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`AdminAiBalancerSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/CommunityChatPage/ChatPane/ChatPaneSkeleton.tsx",
        "line": 1,
        "col": 1,
        "msg": "`ChatPaneSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/CourseCatalogPage/CatalogLineCardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`CatalogLineCardSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/DashboardPage/ChangelogList/ChangelogListSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`ChangelogListSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/DashboardPage/ContinueLearning/ContinueLearningSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`ContinueLearningSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/DashboardPage/FeedTabs/FeedTabsSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`FeedTabsSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/DashboardPage/LeagueCard/LeagueCardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`LeagueCardSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/DashboardPage/OverviewTab/OverviewContributions/OverviewContributionsSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`OverviewContributionsSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/DashboardPage/TopLearners/TopLearnersSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`TopLearnersSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/DashboardPage/TrendingContents/TrendingContentsSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`TrendingContentsSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/DashboardPage/WeeklyChallengeCard/WeeklyChallengeCardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`WeeklyChallengeCardSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/FlashcardsPage/DueReview/DueReviewSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`DueReviewSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardDeckList/FlashcardDeckListSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`FlashcardDeckListSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardQuizResult/FlashcardQuizResultSkeleton.tsx",
        "line": 1,
        "col": 1,
        "msg": "`FlashcardQuizResultSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewer/FlashcardReviewerSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`FlashcardReviewerSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardSessionStats/FlashcardSessionStatsSkeleton.tsx",
        "line": 1,
        "col": 1,
        "msg": "`FlashcardSessionStatsSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/FlashcardsPage/QuizSession/QuizSessionSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`QuizSessionSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/JobListPage/JobListRowSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`JobListRowSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/LeaguePage/GlobalBoard/GlobalBoardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`GlobalBoardSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/LeaguePage/WeeklyBoard/WeeklyBoardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`WeeklyBoardSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewSessionSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`MockInterviewSessionSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewSetupSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`MockInterviewSetupSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/PracticeProblemPage/PracticeProblemSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`PracticeProblemSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/SystemStatusPage/SystemStatusSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`SystemStatusSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      }
    ],
    "unlocked": [
      {
        "file": "src/components/blocks/cards/CourseCardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`CourseCardSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/cv/CvBlocksWorkspace/CvWorkspaceSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`CvWorkspaceSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/ContentMap/ContentMapSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`ContentMapSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/MilestoneOutline/MilestoneOutlineSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`MilestoneOutlineSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/MockInterviewScorecard/ScoreRowSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`ScoreRowSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/PlaygroundSetupSteps/CommandSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`CommandSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/PlaygroundSetupSteps/OsTabsSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`OsTabsSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/lesson/ChallengeBody/ChallengeCardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`ChallengeCardSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/lesson/CodeBodySkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`CodeBodySkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/lesson/ContentBodySkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`ContentBodySkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/lesson/ContentHeaderSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`ContentHeaderSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/lesson/LessonBody/LessonCardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`LessonCardSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/personal-project/PersonalProjectDashboard/PersonalProjectDashboardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`PersonalProjectDashboardSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/personal-project/TaskResultsSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`TaskResultsSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/blocks/learn/personal-project/TaskSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`TaskSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/overlays/modals/ManagePinnedProjectsModal/ManagePinnedListSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`ManagePinnedListSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/CommunityChatPage/ChatPane/ChatPaneSkeleton.tsx",
        "line": 1,
        "col": 1,
        "msg": "`ChatPaneSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/CourseCatalogPage/CatalogLineCardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`CatalogLineCardSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/DashboardPage/ChangelogList/ChangelogListSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`ChangelogListSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/DashboardPage/ContinueLearning/ContinueLearningSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`ContinueLearningSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/DashboardPage/FeedTabs/FeedTabsSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`FeedTabsSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/DashboardPage/LeagueCard/LeagueCardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`LeagueCardSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/DashboardPage/OverviewTab/OverviewContributions/OverviewContributionsSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`OverviewContributionsSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/DashboardPage/TopLearners/TopLearnersSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`TopLearnersSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/DashboardPage/TrendingContents/TrendingContentsSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`TrendingContentsSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/DashboardPage/WeeklyChallengeCard/WeeklyChallengeCardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`WeeklyChallengeCardSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/FlashcardsPage/DueReview/DueReviewSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`DueReviewSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardDeckList/FlashcardDeckListSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`FlashcardDeckListSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardQuizResult/FlashcardQuizResultSkeleton.tsx",
        "line": 1,
        "col": 1,
        "msg": "`FlashcardQuizResultSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewer/FlashcardReviewerSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`FlashcardReviewerSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardSessionStats/FlashcardSessionStatsSkeleton.tsx",
        "line": 1,
        "col": 1,
        "msg": "`FlashcardSessionStatsSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/JobListPage/JobListRowSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`JobListRowSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/LeaguePage/GlobalBoard/GlobalBoardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`GlobalBoardSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/LeaguePage/WeeklyBoard/WeeklyBoardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`WeeklyBoardSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewSetupSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`MockInterviewSetupSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/PracticeProblemPage/PracticeProblemSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`PracticeProblemSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/SystemStatusPage/SystemStatusSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`SystemStatusSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      }
    ],
    "locked": [
      {
        "file": "src/components/pages/FlashcardsPage/QuizSession/QuizSessionSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`QuizSessionSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewSessionSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`MockInterviewSessionSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      }
    ],
    "hold": [
      {
        "file": "src/components/pages/AdminAiBalancerPage/AdminAiBalancerSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`AdminAiBalancerSkeleton` is a hand-mirrored twin: a second description of a shape that already has one. Give the component it mirrors an `isSkeleton` prop and let it rest as ITSELF — the twin cannot be kept in step, it can only be noticed after it has already drifted. (The `Skeleton.*` primitives under `blocks/skeleton/` are the pieces you rest WITH, and are exempt.)"
      }
    ]
  },
  "starci-fe/no-parallel-skeleton": {
    "all": [
      {
        "file": ".storybook/components/atoms/forms/InputCurrency/InputCurrency.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/InputDate/InputDate.tsx",
        "line": 6,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/InputNumber/InputNumber.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/InputPassword/InputPassword.tsx",
        "line": 6,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/InputSearch/InputSearch.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/InputText/InputText.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/InputTextarea/InputTextarea.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/InputTime/InputTime.tsx",
        "line": 6,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/SelectCombobox/SelectCombobox.tsx",
        "line": 11,
        "col": 10,
        "msg": "Import `TriggerSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/SelectMulti/SelectMulti.tsx",
        "line": 9,
        "col": 10,
        "msg": "Import `TriggerSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/SelectSingle/SelectSingle.tsx",
        "line": 9,
        "col": 10,
        "msg": "Import `TriggerSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/starci/blocks/learn/MockInterviewScorecard/MockInterviewScorecard.tsx",
        "line": 13,
        "col": 10,
        "msg": "Import `ScoreRowSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/starci/blocks/learn/PlaygroundSetupSteps/PlaygroundSetupSteps.tsx",
        "line": 18,
        "col": 10,
        "msg": "Import `CommandSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/starci/blocks/learn/PlaygroundSetupSteps/PlaygroundSetupSteps.tsx",
        "line": 19,
        "col": 10,
        "msg": "Import `OsTabsSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/InputCurrency/index.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/InputDate/index.tsx",
        "line": 6,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/InputNumber/index.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/InputPassword/index.tsx",
        "line": 6,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/InputSearch/index.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/InputText/index.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/InputTextarea/index.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/InputTime/index.tsx",
        "line": 6,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/SelectCombobox/index.tsx",
        "line": 11,
        "col": 10,
        "msg": "Import `TriggerSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/SelectMulti/index.tsx",
        "line": 9,
        "col": 10,
        "msg": "Import `TriggerSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/SelectSingle/index.tsx",
        "line": 9,
        "col": 10,
        "msg": "Import `TriggerSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/MockInterviewScorecard/index.tsx",
        "line": 13,
        "col": 10,
        "msg": "Import `ScoreRowSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/PlaygroundSetupSteps/index.tsx",
        "line": 18,
        "col": 10,
        "msg": "Import `CommandSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/PlaygroundSetupSteps/index.tsx",
        "line": 19,
        "col": 10,
        "msg": "Import `OsTabsSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/lesson/ChallengeBody/index.tsx",
        "line": 8,
        "col": 10,
        "msg": "Import `ChallengeCardSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/lesson/ChallengeBody/index.tsx",
        "line": 80,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/blocks/learn/lesson/CodeExplainingBody/index.tsx",
        "line": 4,
        "col": 10,
        "msg": "Import `CodeBodySkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/lesson/CodeImplementationBody/index.tsx",
        "line": 4,
        "col": 10,
        "msg": "Import `CodeBodySkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/lesson/ContentBody/ContentBodyV2/index.tsx",
        "line": 11,
        "col": 5,
        "msg": "Import `ContentBodySkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/lesson/LessonBody/index.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `LessonCardSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/lesson/SandboxBody/index.tsx",
        "line": 6,
        "col": 10,
        "msg": "Import `CodeBodySkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/lesson/SandboxBody/index.tsx",
        "line": 214,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/blocks/learn/personal-project/PersonalProjectDashboard/index.tsx",
        "line": 15,
        "col": 10,
        "msg": "Import `PersonalProjectDashboardSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/personal-project/PersonalProjectDashboard/index.tsx",
        "line": 204,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/blocks/learn/personal-project/TaskResult/index.tsx",
        "line": 264,
        "col": 21,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/blocks/learn/personal-project/TaskResult/index.tsx",
        "line": 366,
        "col": 33,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/blocks/learn/personal-project/TaskResults/index.tsx",
        "line": 8,
        "col": 5,
        "msg": "Import `TaskResultsSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/personal-project/index.tsx",
        "line": 8,
        "col": 5,
        "msg": "Import `TaskSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/layouts/Navbar/NotificationBell/index.tsx",
        "line": 191,
        "col": 25,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/AdminAiBalancerPage/index.tsx",
        "line": 24,
        "col": 5,
        "msg": "Import `AdminAiBalancerSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/AiSettingsPage/index.tsx",
        "line": 143,
        "col": 25,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/AiUsagePage/AiUsageHistory/index.tsx",
        "line": 135,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/BookmarksPage/index.tsx",
        "line": 210,
        "col": 25,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/CommunityChatPage/index.tsx",
        "line": 10,
        "col": 10,
        "msg": "Import `ChatPaneSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/CvEditorPage/index.tsx",
        "line": 382,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/FlashcardsPage/QuizSession/index.tsx",
        "line": 48,
        "col": 10,
        "msg": "Import `QuizSessionSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/FlashcardsPage/index.tsx",
        "line": 11,
        "col": 10,
        "msg": "Import `FlashcardReviewerSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/FlashcardsPage/index.tsx",
        "line": 13,
        "col": 10,
        "msg": "Import `QuizSessionSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/FlashcardsPage/index.tsx",
        "line": 15,
        "col": 10,
        "msg": "Import `FlashcardQuizResultSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/FlashcardsPage/index.tsx",
        "line": 24,
        "col": 10,
        "msg": "Import `FlashcardSessionStatsSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/InstallmentPlansPage/index.tsx",
        "line": 152,
        "col": 25,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/CourseDetail/CourseDayTimeline/index.tsx",
        "line": 160,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/CourseDetail/index.tsx",
        "line": 131,
        "col": 29,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/CourseMilestoneOutline/index.tsx",
        "line": 160,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/CourseOutline/index.tsx",
        "line": 148,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/index.tsx",
        "line": 125,
        "col": 29,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewHistory/index.tsx",
        "line": 154,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewSession/index.tsx",
        "line": 71,
        "col": 10,
        "msg": "Import `MockInterviewSessionSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewStats/index.tsx",
        "line": 142,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/MockInterviewPage/index.tsx",
        "line": 11,
        "col": 10,
        "msg": "Import `MockInterviewSetupSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/MockInterviewPage/index.tsx",
        "line": 12,
        "col": 10,
        "msg": "Import `MockInterviewSessionSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/MyAttemptsPage/index.tsx",
        "line": 138,
        "col": 21,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/MyFeedbackPage/index.tsx",
        "line": 194,
        "col": 21,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/MySubmissionsPage/index.tsx",
        "line": 172,
        "col": 21,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/NotificationsPage/index.tsx",
        "line": 309,
        "col": 37,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/PracticeHubPage/ProblemCatalog/index.tsx",
        "line": 90,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/PracticeHubPage/ProgressCockpit/index.tsx",
        "line": 113,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/PracticeProblemPage/index.tsx",
        "line": 34,
        "col": 10,
        "msg": "Import `PracticeProblemSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/ProfileActivityPage/ProfileAchievements/index.tsx",
        "line": 238,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileActivityPage/ProfileActivity/index.tsx",
        "line": 120,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileChallengeManagePage/index.tsx",
        "line": 293,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileChallengeSubmissionPage/index.tsx",
        "line": 133,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileChallengesPage/ProfileChallenges/index.tsx",
        "line": 113,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileCodingProblemPage/index.tsx",
        "line": 121,
        "col": 29,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileOverviewPage/OverviewChallengeSkills/index.tsx",
        "line": 77,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileOverviewPage/OverviewCodeSkills/index.tsx",
        "line": 88,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileOverviewPage/ProfileJobReadiness/index.tsx",
        "line": 151,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileProjectRoadmapPage/index.tsx",
        "line": 167,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileProjectsPage/ProfilePinned/index.tsx",
        "line": 99,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileSkillsPage/ProfileCoding/index.tsx",
        "line": 213,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/RewardsPage/MyVouchers/index.tsx",
        "line": 69,
        "col": 33,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/RewardsPage/MyVouchers/index.tsx",
        "line": 152,
        "col": 33,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/RewardsPage/RewardCatalog/index.tsx",
        "line": 213,
        "col": 29,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/SecurityPage/index.tsx",
        "line": 256,
        "col": 45,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/SessionsPage/index.tsx",
        "line": 146,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/SystemStatusPage/index.tsx",
        "line": 15,
        "col": 10,
        "msg": "Import `SystemStatusSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/SystemStatusPage/index.tsx",
        "line": 109,
        "col": 45,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/SystemStatusPage/index.tsx",
        "line": 185,
        "col": 57,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      }
    ],
    "unlocked": [
      {
        "file": ".storybook/components/atoms/forms/InputCurrency/InputCurrency.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/InputDate/InputDate.tsx",
        "line": 6,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/InputNumber/InputNumber.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/InputPassword/InputPassword.tsx",
        "line": 6,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/InputSearch/InputSearch.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/InputText/InputText.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/InputTextarea/InputTextarea.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/InputTime/InputTime.tsx",
        "line": 6,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/SelectCombobox/SelectCombobox.tsx",
        "line": 11,
        "col": 10,
        "msg": "Import `TriggerSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/SelectMulti/SelectMulti.tsx",
        "line": 9,
        "col": 10,
        "msg": "Import `TriggerSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/atoms/forms/SelectSingle/SelectSingle.tsx",
        "line": 9,
        "col": 10,
        "msg": "Import `TriggerSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/starci/blocks/learn/MockInterviewScorecard/MockInterviewScorecard.tsx",
        "line": 13,
        "col": 10,
        "msg": "Import `ScoreRowSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/starci/blocks/learn/PlaygroundSetupSteps/PlaygroundSetupSteps.tsx",
        "line": 18,
        "col": 10,
        "msg": "Import `CommandSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": ".storybook/components/starci/blocks/learn/PlaygroundSetupSteps/PlaygroundSetupSteps.tsx",
        "line": 19,
        "col": 10,
        "msg": "Import `OsTabsSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/InputCurrency/index.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/InputDate/index.tsx",
        "line": 6,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/InputNumber/index.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/InputPassword/index.tsx",
        "line": 6,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/InputSearch/index.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/InputText/index.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/InputTextarea/index.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/InputTime/index.tsx",
        "line": 6,
        "col": 10,
        "msg": "Import `FieldSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/SelectCombobox/index.tsx",
        "line": 11,
        "col": 10,
        "msg": "Import `TriggerSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/SelectMulti/index.tsx",
        "line": 9,
        "col": 10,
        "msg": "Import `TriggerSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/atoms/forms/SelectSingle/index.tsx",
        "line": 9,
        "col": 10,
        "msg": "Import `TriggerSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/MockInterviewScorecard/index.tsx",
        "line": 13,
        "col": 10,
        "msg": "Import `ScoreRowSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/PlaygroundSetupSteps/index.tsx",
        "line": 18,
        "col": 10,
        "msg": "Import `CommandSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/PlaygroundSetupSteps/index.tsx",
        "line": 19,
        "col": 10,
        "msg": "Import `OsTabsSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/lesson/ChallengeBody/index.tsx",
        "line": 8,
        "col": 10,
        "msg": "Import `ChallengeCardSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/lesson/ChallengeBody/index.tsx",
        "line": 80,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/blocks/learn/lesson/CodeExplainingBody/index.tsx",
        "line": 4,
        "col": 10,
        "msg": "Import `CodeBodySkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/lesson/CodeImplementationBody/index.tsx",
        "line": 4,
        "col": 10,
        "msg": "Import `CodeBodySkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/lesson/ContentBody/ContentBodyV2/index.tsx",
        "line": 11,
        "col": 5,
        "msg": "Import `ContentBodySkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/lesson/LessonBody/index.tsx",
        "line": 5,
        "col": 10,
        "msg": "Import `LessonCardSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/lesson/SandboxBody/index.tsx",
        "line": 6,
        "col": 10,
        "msg": "Import `CodeBodySkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/lesson/SandboxBody/index.tsx",
        "line": 214,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/blocks/learn/personal-project/PersonalProjectDashboard/index.tsx",
        "line": 15,
        "col": 10,
        "msg": "Import `PersonalProjectDashboardSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/personal-project/PersonalProjectDashboard/index.tsx",
        "line": 204,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/blocks/learn/personal-project/TaskResult/index.tsx",
        "line": 264,
        "col": 21,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/blocks/learn/personal-project/TaskResult/index.tsx",
        "line": 366,
        "col": 33,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/blocks/learn/personal-project/TaskResults/index.tsx",
        "line": 8,
        "col": 5,
        "msg": "Import `TaskResultsSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/blocks/learn/personal-project/index.tsx",
        "line": 8,
        "col": 5,
        "msg": "Import `TaskSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/layouts/Navbar/NotificationBell/index.tsx",
        "line": 191,
        "col": 25,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/AdminAiBalancerPage/index.tsx",
        "line": 24,
        "col": 5,
        "msg": "Import `AdminAiBalancerSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/AiSettingsPage/index.tsx",
        "line": 143,
        "col": 25,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/AiUsagePage/AiUsageHistory/index.tsx",
        "line": 135,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/BookmarksPage/index.tsx",
        "line": 210,
        "col": 25,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/CommunityChatPage/index.tsx",
        "line": 10,
        "col": 10,
        "msg": "Import `ChatPaneSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/CvEditorPage/index.tsx",
        "line": 382,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/FlashcardsPage/index.tsx",
        "line": 11,
        "col": 10,
        "msg": "Import `FlashcardReviewerSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/FlashcardsPage/index.tsx",
        "line": 13,
        "col": 10,
        "msg": "Import `QuizSessionSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/FlashcardsPage/index.tsx",
        "line": 15,
        "col": 10,
        "msg": "Import `FlashcardQuizResultSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/FlashcardsPage/index.tsx",
        "line": 24,
        "col": 10,
        "msg": "Import `FlashcardSessionStatsSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/InstallmentPlansPage/index.tsx",
        "line": 152,
        "col": 25,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/CourseDetail/CourseDayTimeline/index.tsx",
        "line": 160,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/CourseDetail/index.tsx",
        "line": 131,
        "col": 29,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/CourseMilestoneOutline/index.tsx",
        "line": 160,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/CourseOutline/index.tsx",
        "line": 148,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/index.tsx",
        "line": 125,
        "col": 29,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewHistory/index.tsx",
        "line": 154,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewStats/index.tsx",
        "line": 142,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/MockInterviewPage/index.tsx",
        "line": 11,
        "col": 10,
        "msg": "Import `MockInterviewSetupSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/MockInterviewPage/index.tsx",
        "line": 12,
        "col": 10,
        "msg": "Import `MockInterviewSessionSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/MyAttemptsPage/index.tsx",
        "line": 138,
        "col": 21,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/MyFeedbackPage/index.tsx",
        "line": 194,
        "col": 21,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/MySubmissionsPage/index.tsx",
        "line": 172,
        "col": 21,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/NotificationsPage/index.tsx",
        "line": 309,
        "col": 37,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/PracticeHubPage/ProblemCatalog/index.tsx",
        "line": 90,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/PracticeHubPage/ProgressCockpit/index.tsx",
        "line": 113,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/PracticeProblemPage/index.tsx",
        "line": 34,
        "col": 10,
        "msg": "Import `PracticeProblemSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/ProfileActivityPage/ProfileAchievements/index.tsx",
        "line": 238,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileActivityPage/ProfileActivity/index.tsx",
        "line": 120,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileChallengeManagePage/index.tsx",
        "line": 293,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileChallengeSubmissionPage/index.tsx",
        "line": 133,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileChallengesPage/ProfileChallenges/index.tsx",
        "line": 113,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileCodingProblemPage/index.tsx",
        "line": 121,
        "col": 29,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileOverviewPage/OverviewChallengeSkills/index.tsx",
        "line": 77,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileOverviewPage/OverviewCodeSkills/index.tsx",
        "line": 88,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileOverviewPage/ProfileJobReadiness/index.tsx",
        "line": 151,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileProjectRoadmapPage/index.tsx",
        "line": 167,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileProjectsPage/ProfilePinned/index.tsx",
        "line": 99,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/ProfileSkillsPage/ProfileCoding/index.tsx",
        "line": 213,
        "col": 13,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/RewardsPage/MyVouchers/index.tsx",
        "line": 69,
        "col": 33,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/RewardsPage/MyVouchers/index.tsx",
        "line": 152,
        "col": 33,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/RewardsPage/RewardCatalog/index.tsx",
        "line": 213,
        "col": 29,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/SecurityPage/index.tsx",
        "line": 256,
        "col": 45,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/SessionsPage/index.tsx",
        "line": 146,
        "col": 17,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/SystemStatusPage/index.tsx",
        "line": 15,
        "col": 10,
        "msg": "Import `SystemStatusSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/SystemStatusPage/index.tsx",
        "line": 109,
        "col": 45,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      },
      {
        "file": "src/components/pages/SystemStatusPage/index.tsx",
        "line": 185,
        "col": 57,
        "msg": "Prop `skeleton={<…>}` is a hand-kept parallel skeleton tree that drifts from the real shape — thread `isSkeleton` to each leaf so shimmer mirrors the loaded tree."
      }
    ],
    "locked": [
      {
        "file": "src/components/pages/FlashcardsPage/QuizSession/index.tsx",
        "line": 48,
        "col": 10,
        "msg": "Import `QuizSessionSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewSession/index.tsx",
        "line": 71,
        "col": 10,
        "msg": "Import `MockInterviewSessionSkeleton` (relative) is a hand-kept parallel skeleton — thread `isSkeleton` to each leaf instead of keeping a second tree."
      }
    ],
    "hold": []
  },
  "starci-fe/export-matches-folder": {
    "all": [
      {
        "file": "src/components/atoms/data/Table/index.tsx",
        "line": 2,
        "col": 1,
        "msg": "`index.tsx` in folder `Table` does not export `Table` (exports: TableRoot, TableScrollContainer, TableContent, TableHeader, TableBody, TableColumn, TableRow, TableCell, meta) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/atoms/display/Progress/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Progress` does not export `Progress` (exports: ProgressBar, ProgressCircle, ProgressGauge, meta) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/atoms/feedback/AlertDialog/index.tsx",
        "line": 2,
        "col": 1,
        "msg": "`index.tsx` in folder `AlertDialog` does not export `AlertDialog` (exports: AlertDialogRoot, AlertDialogBackdrop, AlertDialogContainer, AlertDialogDialog, AlertDialogHeader, AlertDialogHeading, AlertDialogBody, AlertDialogFooter, meta) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/atoms/forms/ListBox/index.tsx",
        "line": 2,
        "col": 1,
        "msg": "`index.tsx` in folder `ListBox` does not export `ListBox` (exports: ListBoxRoot, ListBoxItem, meta) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/atoms/forms/Select/index.tsx",
        "line": 2,
        "col": 1,
        "msg": "`index.tsx` in folder `Select` does not export `Select` (exports: SelectRoot, SelectTrigger, SelectValue, SelectIndicator, SelectPopover, meta) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/atoms/navigation/Link/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Link` does not export `Link` (exports: LinkBack, LinkSeeMore, LinkBackProps, LinkSeeMoreProps, LinkSeeMoreSize, meta) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/atoms/overlay/Drawer/index.tsx",
        "line": 2,
        "col": 1,
        "msg": "`index.tsx` in folder `Drawer` does not export `Drawer` (exports: DrawerRoot, DrawerBackdrop, DrawerContent, DrawerDialog, DrawerHeader, DrawerBody, DrawerFooter, DrawerCloseTrigger, meta) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/atoms/overlay/Modal/index.tsx",
        "line": 2,
        "col": 1,
        "msg": "`index.tsx` in folder `Modal` does not export `Modal` (exports: ModalRoot, ModalBackdrop, ModalContainer, ModalDialog, ModalHeader, ModalBody, ModalFooter, ModalCloseTrigger, meta) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/cv/BlockRegistry/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `BlockRegistry` does not export `BlockRegistry` (exports: CV_BLOCK_TYPE_REGISTRY, CV_BLOCK_TYPE_ORDER, CV_BLOCK_TYPE_DEFAULT_TITLE_EN) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Accordion/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Accordion` does not export `Accordion` (exports: SkeletonAccordion) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Avatar/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Avatar` does not export `Avatar` (exports: SkeletonAvatar) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Badge/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Badge` does not export `Badge` (exports: SkeletonBadge) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Breadcrumbs/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Breadcrumbs` does not export `Breadcrumbs` (exports: SkeletonBreadcrumbs) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Button/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Button` does not export `Button` (exports: SkeletonButton) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Card/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Card` does not export `Card` (exports: SkeletonCard) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Checkbox/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Checkbox` does not export `Checkbox` (exports: SkeletonCheckbox) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Chip/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Chip` does not export `Chip` (exports: SkeletonChip) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Disclosure/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Disclosure` does not export `Disclosure` (exports: SkeletonDisclosure) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Input/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Input` does not export `Input` (exports: SkeletonInput) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Kbd/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Kbd` does not export `Kbd` (exports: SkeletonKbd) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/ListBox/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `ListBox` does not export `ListBox` (exports: SkeletonListBox) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/ListRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `ListRow` does not export `ListRow` (exports: SkeletonListRow) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Menu/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Menu` does not export `Menu` (exports: SkeletonMenu) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Meter/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Meter` does not export `Meter` (exports: SkeletonMeter) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Metric/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Metric` does not export `Metric` (exports: SkeletonMetric) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Pagination/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Pagination` does not export `Pagination` (exports: SkeletonPagination) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Paragraph/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Paragraph` does not export `Paragraph` (exports: SkeletonParagraph) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/ProgressBar/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `ProgressBar` does not export `ProgressBar` (exports: SkeletonProgressBar) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/RadioGroup/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `RadioGroup` does not export `RadioGroup` (exports: SkeletonRadioGroup) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/SegmentBar/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `SegmentBar` does not export `SegmentBar` (exports: SkeletonSegmentBar) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Select/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Select` does not export `Select` (exports: SkeletonSelect) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Slider/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Slider` does not export `Slider` (exports: SkeletonSlider) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Switch/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Switch` does not export `Switch` (exports: SkeletonSwitch) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Table/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Table` does not export `Table` (exports: SkeletonTable) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Tabs/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Tabs` does not export `Tabs` (exports: SkeletonTabs) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/TextArea/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `TextArea` does not export `TextArea` (exports: SkeletonTextArea) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Typography/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Typography` does not export `Typography` (exports: SkeletonTypography) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/UserCell/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `UserCell` does not export `UserCell` (exports: SkeletonUserCell) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/composites/chips/Chip/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Chip` does not export `Chip` (exports: ChipEnum, ChipHighlight, ChipRemovable) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/composites/data/KeyValue/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `KeyValue` does not export `KeyValue` (exports: meta, KeyValueRow, KeyValueList) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/composites/layout/Page/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Page` does not export `Page` (exports: meta, PageHeader, PageBottomBar) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/composites/lists/List/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `List` does not export `List` (exports: meta, ListRow, ListLabeled, ListMeta, ListToggleRow) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/overlays/modals/GlobalSearchModal/Content/Block/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Block` does not export `Block` (exports: GlobalSearchContentBlock) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/overlays/modals/GlobalSearchModal/Content/Empty/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Empty` does not export `Empty` (exports: GlobalSearchEmpty) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/overlays/modals/GlobalSearchModal/Content/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Content` does not export `Content` (exports: GlobalSearchContent) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/Header/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Header` does not export `Header` (exports: AdminMpegDashTestHeader) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/Header/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Header` does not export `Header` (exports: AdminUploadVideoHeader) — the named export must match the folder (structure-and-naming §5)."
      }
    ],
    "unlocked": [
      {
        "file": "src/components/atoms/data/Table/index.tsx",
        "line": 2,
        "col": 1,
        "msg": "`index.tsx` in folder `Table` does not export `Table` (exports: TableRoot, TableScrollContainer, TableContent, TableHeader, TableBody, TableColumn, TableRow, TableCell, meta) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/atoms/display/Progress/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Progress` does not export `Progress` (exports: ProgressBar, ProgressCircle, ProgressGauge, meta) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/atoms/feedback/AlertDialog/index.tsx",
        "line": 2,
        "col": 1,
        "msg": "`index.tsx` in folder `AlertDialog` does not export `AlertDialog` (exports: AlertDialogRoot, AlertDialogBackdrop, AlertDialogContainer, AlertDialogDialog, AlertDialogHeader, AlertDialogHeading, AlertDialogBody, AlertDialogFooter, meta) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/atoms/forms/ListBox/index.tsx",
        "line": 2,
        "col": 1,
        "msg": "`index.tsx` in folder `ListBox` does not export `ListBox` (exports: ListBoxRoot, ListBoxItem, meta) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/atoms/forms/Select/index.tsx",
        "line": 2,
        "col": 1,
        "msg": "`index.tsx` in folder `Select` does not export `Select` (exports: SelectRoot, SelectTrigger, SelectValue, SelectIndicator, SelectPopover, meta) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/atoms/navigation/Link/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Link` does not export `Link` (exports: LinkBack, LinkSeeMore, LinkBackProps, LinkSeeMoreProps, LinkSeeMoreSize, meta) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/atoms/overlay/Drawer/index.tsx",
        "line": 2,
        "col": 1,
        "msg": "`index.tsx` in folder `Drawer` does not export `Drawer` (exports: DrawerRoot, DrawerBackdrop, DrawerContent, DrawerDialog, DrawerHeader, DrawerBody, DrawerFooter, DrawerCloseTrigger, meta) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/atoms/overlay/Modal/index.tsx",
        "line": 2,
        "col": 1,
        "msg": "`index.tsx` in folder `Modal` does not export `Modal` (exports: ModalRoot, ModalBackdrop, ModalContainer, ModalDialog, ModalHeader, ModalBody, ModalFooter, ModalCloseTrigger, meta) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/cv/BlockRegistry/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `BlockRegistry` does not export `BlockRegistry` (exports: CV_BLOCK_TYPE_REGISTRY, CV_BLOCK_TYPE_ORDER, CV_BLOCK_TYPE_DEFAULT_TITLE_EN) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Accordion/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Accordion` does not export `Accordion` (exports: SkeletonAccordion) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Avatar/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Avatar` does not export `Avatar` (exports: SkeletonAvatar) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Badge/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Badge` does not export `Badge` (exports: SkeletonBadge) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Breadcrumbs/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Breadcrumbs` does not export `Breadcrumbs` (exports: SkeletonBreadcrumbs) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Button/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Button` does not export `Button` (exports: SkeletonButton) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Card/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Card` does not export `Card` (exports: SkeletonCard) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Checkbox/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Checkbox` does not export `Checkbox` (exports: SkeletonCheckbox) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Chip/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Chip` does not export `Chip` (exports: SkeletonChip) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Disclosure/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Disclosure` does not export `Disclosure` (exports: SkeletonDisclosure) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Input/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Input` does not export `Input` (exports: SkeletonInput) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Kbd/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Kbd` does not export `Kbd` (exports: SkeletonKbd) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/ListBox/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `ListBox` does not export `ListBox` (exports: SkeletonListBox) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/ListRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `ListRow` does not export `ListRow` (exports: SkeletonListRow) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Menu/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Menu` does not export `Menu` (exports: SkeletonMenu) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Meter/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Meter` does not export `Meter` (exports: SkeletonMeter) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Metric/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Metric` does not export `Metric` (exports: SkeletonMetric) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Pagination/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Pagination` does not export `Pagination` (exports: SkeletonPagination) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Paragraph/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Paragraph` does not export `Paragraph` (exports: SkeletonParagraph) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/ProgressBar/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `ProgressBar` does not export `ProgressBar` (exports: SkeletonProgressBar) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/RadioGroup/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `RadioGroup` does not export `RadioGroup` (exports: SkeletonRadioGroup) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/SegmentBar/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `SegmentBar` does not export `SegmentBar` (exports: SkeletonSegmentBar) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Select/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Select` does not export `Select` (exports: SkeletonSelect) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Slider/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Slider` does not export `Slider` (exports: SkeletonSlider) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Switch/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Switch` does not export `Switch` (exports: SkeletonSwitch) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Table/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Table` does not export `Table` (exports: SkeletonTable) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Tabs/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Tabs` does not export `Tabs` (exports: SkeletonTabs) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/TextArea/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `TextArea` does not export `TextArea` (exports: SkeletonTextArea) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/Typography/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Typography` does not export `Typography` (exports: SkeletonTypography) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/blocks/skeleton/Skeleton/UserCell/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `UserCell` does not export `UserCell` (exports: SkeletonUserCell) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/composites/chips/Chip/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Chip` does not export `Chip` (exports: ChipEnum, ChipHighlight, ChipRemovable) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/composites/data/KeyValue/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `KeyValue` does not export `KeyValue` (exports: meta, KeyValueRow, KeyValueList) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/composites/layout/Page/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Page` does not export `Page` (exports: meta, PageHeader, PageBottomBar) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/composites/lists/List/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `List` does not export `List` (exports: meta, ListRow, ListLabeled, ListMeta, ListToggleRow) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/overlays/modals/GlobalSearchModal/Content/Block/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Block` does not export `Block` (exports: GlobalSearchContentBlock) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/overlays/modals/GlobalSearchModal/Content/Empty/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Empty` does not export `Empty` (exports: GlobalSearchEmpty) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/overlays/modals/GlobalSearchModal/Content/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Content` does not export `Content` (exports: GlobalSearchContent) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/Header/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Header` does not export `Header` (exports: AdminMpegDashTestHeader) — the named export must match the folder (structure-and-naming §5)."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/Header/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`index.tsx` in folder `Header` does not export `Header` (exports: AdminUploadVideoHeader) — the named export must match the folder (structure-and-naming §5)."
      }
    ],
    "locked": [],
    "hold": []
  },
  "starci-fe/no-helper-folder-in-components": {
    "all": [
      {
        "file": "src/components/blocks/layout/shell/Navbar/AccountMenuDropdown/types/account-menu.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/layout/shell/Navbar/AccountMenuDropdown/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/layout/shell/Navbar/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/layout/shell/Navbar/types/navbar.ts",
        "line": 4,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/AiBalancer/utils/format-timestamp.ts",
        "line": 8,
        "col": 1,
        "msg": "`utils/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/ConceptMap/hooks/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/ConceptMap/hooks/useMindMapFitView.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/OnThisPage/hooks/useTableOfContents.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/lesson/AiLab/types/ai-lab.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/lesson/AiLab/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/lesson/constants/content-tabs.ts",
        "line": 1,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/lesson/hooks/useLessonNavigation.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/lesson/types/content.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/lesson/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/media/VideoRenderer/VideoControls/constants/index.ts",
        "line": 2,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/media/VideoRenderer/VideoControls/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/media/VideoRenderer/VideoControls/types/quality.ts",
        "line": 7,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/navigation/ProgrammingLanguageTabs/constants/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/navigation/ProgrammingLanguageTabs/constants/programming-language-tabs.ts",
        "line": 2,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/rendering/MarkdownContent/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/rendering/MarkdownContent/types/markdown-content.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/rendering/MarkdownContent/utils/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`utils/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/rendering/MarkdownContent/utils/markdown-table.ts",
        "line": 1,
        "col": 1,
        "msg": "`utils/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/rendering/PDFView/constants/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/rendering/PDFView/constants/pdf-view.ts",
        "line": 2,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/skeleton/AccordionSkeleton/types/accordion-skeleton.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/skeleton/AccordionSkeleton/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/layouts/LearnShellLayout/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/layouts/Navbar/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/layouts/Navbar/types/navbar.ts",
        "line": 4,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/overlays/modals/AiQuotaModal/hooks/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/overlays/modals/AiQuotaModal/hooks/useAiQuotaHistorySwr.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/overlays/modals/AiQuotaModal/hooks/useWindowResetLabel.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/types/credentials-state.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/overlays/modals/ManagePinnedProjectsModal/hooks/usePinnedProjectsManager.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/constants/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/constants/quick-test-urls.ts",
        "line": 1,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/types/admin-mpeg-dash-test.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/types/admin-upload-video.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/utils/format-size.ts",
        "line": 9,
        "col": 1,
        "msg": "`utils/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/utils/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`utils/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AiUsagePage/hooks/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AiUsagePage/hooks/quota-lane-variant.ts",
        "line": 2,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AiUsagePage/hooks/useQuotaLaneData.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/ArchitecturePage/hooks/useArchitectureNode.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/ArchitecturePage/hooks/useSystemHealthPoll.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/CourseDetailPage/hooks/useCourseEnrollment.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/CourseDetailPage/hooks/usePricingRows.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/DashboardPage/hooks/useDashboardTabUrlSync.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/FlashcardsPage/constants/index.ts",
        "line": 2,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/LandingPage/constants/index.ts",
        "line": 2,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/hooks/useSelectedCourse.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/PracticeHubPage/constants/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/PracticeHubPage/hooks/useCodingProblemsSwr.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/PracticeHubPage/hooks/useMyCodingProgressSwr.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/PracticeHubPage/hooks/usePracticeFilters.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/PracticeHubPage/hooks/usePracticeView.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/PracticeHubPage/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/PracticeHubPage/utils/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`utils/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/types/sepay-checkout.ts",
        "line": 7,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/utils/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`utils/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/utils/parse-bank-details.ts",
        "line": 1,
        "col": 1,
        "msg": "`utils/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      }
    ],
    "unlocked": [
      {
        "file": "src/components/blocks/layout/shell/Navbar/AccountMenuDropdown/types/account-menu.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/layout/shell/Navbar/AccountMenuDropdown/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/layout/shell/Navbar/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/layout/shell/Navbar/types/navbar.ts",
        "line": 4,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/AiBalancer/utils/format-timestamp.ts",
        "line": 8,
        "col": 1,
        "msg": "`utils/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/ConceptMap/hooks/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/ConceptMap/hooks/useMindMapFitView.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/OnThisPage/hooks/useTableOfContents.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/lesson/AiLab/types/ai-lab.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/lesson/AiLab/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/lesson/constants/content-tabs.ts",
        "line": 1,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/lesson/hooks/useLessonNavigation.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/lesson/types/content.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/learn/lesson/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/media/VideoRenderer/VideoControls/constants/index.ts",
        "line": 2,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/media/VideoRenderer/VideoControls/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/media/VideoRenderer/VideoControls/types/quality.ts",
        "line": 7,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/navigation/ProgrammingLanguageTabs/constants/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/navigation/ProgrammingLanguageTabs/constants/programming-language-tabs.ts",
        "line": 2,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/rendering/MarkdownContent/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/rendering/MarkdownContent/types/markdown-content.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/rendering/MarkdownContent/utils/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`utils/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/rendering/MarkdownContent/utils/markdown-table.ts",
        "line": 1,
        "col": 1,
        "msg": "`utils/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/rendering/PDFView/constants/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/rendering/PDFView/constants/pdf-view.ts",
        "line": 2,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/skeleton/AccordionSkeleton/types/accordion-skeleton.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/blocks/skeleton/AccordionSkeleton/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/layouts/LearnShellLayout/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/layouts/Navbar/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/layouts/Navbar/types/navbar.ts",
        "line": 4,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/overlays/modals/AiQuotaModal/hooks/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/overlays/modals/AiQuotaModal/hooks/useAiQuotaHistorySwr.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/overlays/modals/AiQuotaModal/hooks/useWindowResetLabel.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/types/credentials-state.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/overlays/modals/ManagePinnedProjectsModal/hooks/usePinnedProjectsManager.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/constants/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/constants/quick-test-urls.ts",
        "line": 1,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/types/admin-mpeg-dash-test.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/types/admin-upload-video.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/utils/format-size.ts",
        "line": 9,
        "col": 1,
        "msg": "`utils/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/utils/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`utils/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AiUsagePage/hooks/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AiUsagePage/hooks/quota-lane-variant.ts",
        "line": 2,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/AiUsagePage/hooks/useQuotaLaneData.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/ArchitecturePage/hooks/useArchitectureNode.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/ArchitecturePage/hooks/useSystemHealthPoll.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/CourseDetailPage/hooks/useCourseEnrollment.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/CourseDetailPage/hooks/usePricingRows.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/DashboardPage/hooks/useDashboardTabUrlSync.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/FlashcardsPage/constants/index.ts",
        "line": 2,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/LandingPage/constants/index.ts",
        "line": 2,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/hooks/useSelectedCourse.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/PracticeHubPage/constants/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`constants/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/PracticeHubPage/hooks/useCodingProblemsSwr.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/PracticeHubPage/hooks/useMyCodingProgressSwr.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/PracticeHubPage/hooks/usePracticeFilters.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/PracticeHubPage/hooks/usePracticeView.ts",
        "line": 1,
        "col": 1,
        "msg": "`hooks/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/PracticeHubPage/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/PracticeHubPage/utils/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`utils/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      }
    ],
    "locked": [],
    "hold": [
      {
        "file": "src/components/pages/SepayCheckoutPage/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/types/sepay-checkout.ts",
        "line": 7,
        "col": 1,
        "msg": "`types/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/utils/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`utils/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/utils/parse-bank-details.ts",
        "line": 1,
        "col": 1,
        "msg": "`utils/` under `src/components/**` — this is not component code, so it does not live in the component tree. A fetch is a `hooks/`, a pure function is a `modules/utils/`, a shape is a `modules/types/`, a copy or config map is a `resources/`. Left here it stays invisible to everyone who would have reused it, which is how the same difficulty map got written three times."
      }
    ]
  },
  "starci-fe/page-folder-two-files-only": {
    "all": [
      {
        "file": "src/components/layouts/Footer/FooterNavColumn/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Footer/` contains `FooterNavColumn/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/LearnShellLayout/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`layouts/LearnShellLayout/` contains `types/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/AccountMenuDropdown/AccountMenuAuthed/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `AccountMenuDropdown/AccountMenuAuthed/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/AccountMenuDropdown/AccountMenuGuest/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `AccountMenuDropdown/AccountMenuGuest/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/AccountMenuDropdown/AccountTrigger/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `AccountMenuDropdown/AccountTrigger/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/AccountMenuDropdown/DarkLightModeSwitch/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `AccountMenuDropdown/DarkLightModeSwitch/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/AccountMenuDropdown/GuestHeader/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `AccountMenuDropdown/GuestHeader/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/AccountMenuDropdown/UserSummary/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `AccountMenuDropdown/UserSummary/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/AccountMenuDropdown/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `AccountMenuDropdown/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/CartButton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `CartButton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/LanguageDropdown/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `LanguageDropdown/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/Logo/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `Logo/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/NavLinks/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `NavLinks/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/NotificationBell/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `NotificationBell/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/SearchButton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `SearchButton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `types/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/types/navbar.ts",
        "line": 4,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `types/navbar.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/useNavbarItems.ts",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `useNavbarItems.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/PublicProfileLayout/map.ts",
        "line": 1,
        "col": 1,
        "msg": "`layouts/PublicProfileLayout/` contains `map.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/SettingsLayout/map.ts",
        "line": 1,
        "col": 1,
        "msg": "`layouts/SettingsLayout/` contains `map.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/drawers/SubmissionAttemptsDrawer/map.ts",
        "line": 1,
        "col": 1,
        "msg": "`overlays/SubmissionAttemptsDrawer/` contains `map.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AiQuotaModal/hooks/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AiQuotaModal/` contains `hooks/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AiQuotaModal/hooks/useAiQuotaHistorySwr.ts",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AiQuotaModal/` contains `hooks/useAiQuotaHistorySwr.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AiQuotaModal/hooks/useWindowResetLabel.ts",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AiQuotaModal/` contains `hooks/useWindowResetLabel.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AiQuotaModal/map.ts",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AiQuotaModal/` contains `map.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/EmailField/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/CredentialsState/EmailField/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/OauthButtons/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/CredentialsState/OauthButtons/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/PasswordField/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/CredentialsState/PasswordField/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/RememberMeRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/CredentialsState/RememberMeRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/SignUpPrompt/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/CredentialsState/SignUpPrompt/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/CredentialsState/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/map.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/CredentialsState/map.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/types/credentials-state.ts",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/CredentialsState/types/credentials-state.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/CredentialsState/types/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/OtpState/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/OtpState/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignUpSection/OtpState/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignUpSection/OtpState/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignUpSection/OtpState/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignUpSection/OtpState/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignUpSection/RegistrationState/AgreeToTermsRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignUpSection/RegistrationState/AgreeToTermsRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignUpSection/RegistrationState/EmailField/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignUpSection/RegistrationState/EmailField/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignUpSection/RegistrationState/PasswordField/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignUpSection/RegistrationState/PasswordField/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignUpSection/RegistrationState/SignInPrompt/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignUpSection/RegistrationState/SignInPrompt/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignUpSection/RegistrationState/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignUpSection/RegistrationState/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignUpSection/RegistrationState/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignUpSection/RegistrationState/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignUpSection/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignUpSection/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/GlobalSearchModal/Content/Block/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/GlobalSearchModal/` contains `Content/Block/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/GlobalSearchModal/Content/Empty/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/GlobalSearchModal/` contains `Content/Empty/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/GlobalSearchModal/Content/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/GlobalSearchModal/` contains `Content/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/ManagePinnedProjectsModal/CourseProjectForm/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/ManagePinnedProjectsModal/` contains `CourseProjectForm/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/ManagePinnedProjectsModal/CourseProjectForm/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/ManagePinnedProjectsModal/` contains `CourseProjectForm/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/ManagePinnedProjectsModal/ExternalProjectForm/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/ManagePinnedProjectsModal/` contains `ExternalProjectForm/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/ManagePinnedProjectsModal/ExternalProjectForm/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/ManagePinnedProjectsModal/` contains `ExternalProjectForm/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/ManagePinnedProjectsModal/ManagePinnedListSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/ManagePinnedProjectsModal/` contains `ManagePinnedListSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/ManagePinnedProjectsModal/PinnedProjectCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/ManagePinnedProjectsModal/` contains `PinnedProjectCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/ManagePinnedProjectsModal/hooks/usePinnedProjectsManager.ts",
        "line": 1,
        "col": 1,
        "msg": "`overlays/ManagePinnedProjectsModal/` contains `hooks/usePinnedProjectsManager.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/PremiumGateModal/map.ts",
        "line": 1,
        "col": 1,
        "msg": "`overlays/PremiumGateModal/` contains `map.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminAiBalancerPage/AdminAiBalancerSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminAiBalancerPage/` contains `AdminAiBalancerSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminAiBalancerPage/ProviderSection/KeyRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminAiBalancerPage/` contains `ProviderSection/KeyRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminAiBalancerPage/ProviderSection/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminAiBalancerPage/` contains `ProviderSection/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminAiBalancerPage/TopBar/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminAiBalancerPage/` contains `TopBar/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/ConfigCard/RendererTypeButton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `ConfigCard/RendererTypeButton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/ConfigCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `ConfigCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/Header/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `Header/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/PreviewCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `PreviewCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/QuickTestUrls/QuickTestUrlButton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `QuickTestUrls/QuickTestUrlButton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/QuickTestUrls/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `QuickTestUrls/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/constants/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `constants/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/constants/quick-test-urls.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `constants/quick-test-urls.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/map.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `map.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/types/admin-mpeg-dash-test.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `types/admin-mpeg-dash-test.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `types/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/FileSelectionCard/DropZone/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `FileSelectionCard/DropZone/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/FileSelectionCard/ProcessResultBanner/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `FileSelectionCard/ProcessResultBanner/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/FileSelectionCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `FileSelectionCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/Header/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `Header/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/LoadingScreen/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `LoadingScreen/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/TopBar/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `TopBar/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/UploadProgressCard/ProviderUploadRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `UploadProgressCard/ProviderUploadRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/UploadProgressCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `UploadProgressCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/enums/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `enums/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/enums/upload-status.ts",
        "line": 7,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `enums/upload-status.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/map.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `map.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/types/admin-upload-video.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `types/admin-upload-video.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `types/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/utils/format-size.ts",
        "line": 9,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `utils/format-size.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/utils/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `utils/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AiUsagePage/AiUsageHistory/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AiUsagePage/` contains `AiUsageHistory/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AiUsagePage/hooks/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AiUsagePage/` contains `hooks/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AiUsagePage/hooks/quota-lane-variant.ts",
        "line": 2,
        "col": 1,
        "msg": "`pages/AiUsagePage/` contains `hooks/quota-lane-variant.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AiUsagePage/hooks/useQuotaLaneData.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AiUsagePage/` contains `hooks/useQuotaLaneData.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/ArchitectureMap/future-scene.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `ArchitectureMap/future-scene.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/ArchitectureMap/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `ArchitectureMap/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/ArchitectureMap/scene.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `ArchitectureMap/scene.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/ArchitectureRail/ArchitectureMobileNav/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `ArchitectureRail/ArchitectureMobileNav/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/ArchitectureRail/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `ArchitectureRail/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/CurlTester/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `CurlTester/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/CurlTester/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `CurlTester/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/MetricsInline/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `MetricsInline/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/NodeDissectionPanel/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `NodeDissectionPanel/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/constants.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `constants.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/hooks/useArchitectureNode.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `hooks/useArchitectureNode.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/hooks/useSystemHealthPoll.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `hooks/useSystemHealthPoll.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/metricsFormat.ts",
        "line": 15,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `metricsFormat.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/modules.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `modules.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/statusVisual.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `statusVisual.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/BlogListPage/CategoryFilter/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/BlogListPage/` contains `CategoryFilter/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/BlogListPage/FeaturedPost/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/BlogListPage/` contains `FeaturedPost/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/BlogListPage/Masthead/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/BlogListPage/` contains `Masthead/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/BlogListPage/Masthead/scene.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/BlogListPage/` contains `Masthead/scene.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/BlogListPage/StartHereAnchor/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/BlogListPage/` contains `StartHereAnchor/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/BlogListPage/TopicsStrip/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/BlogListPage/` contains `TopicsStrip/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/BlogPostPage/ReadingProgress/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/BlogPostPage/` contains `ReadingProgress/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/BlogPostPage/RelatedPosts/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/BlogPostPage/` contains `RelatedPosts/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/BookmarksPage/BookmarkCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/BookmarksPage/` contains `BookmarkCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ChallengeResultPage/map.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/ChallengeResultPage/` contains `map.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CommunityChatPage/ChatPane/ChatPaneSkeleton.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CommunityChatPage/` contains `ChatPane/ChatPaneSkeleton.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CommunityChatPage/ChatPane/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CommunityChatPage/` contains `ChatPane/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CommunityChatPage/ChatPane/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CommunityChatPage/` contains `ChatPane/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CommunityFeedPage/CommunityComposer/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CommunityFeedPage/` contains `CommunityComposer/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ContactPage/ContactChannels/FounderCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ContactPage/` contains `ContactChannels/FounderCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ContactPage/ContactChannels/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ContactPage/` contains `ContactChannels/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ContactPage/ContactFaq/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ContactPage/` contains `ContactFaq/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ContactPage/ContactForm/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ContactPage/` contains `ContactForm/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseCatalogPage/CatalogCourseCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseCatalogPage/` contains `CatalogCourseCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseCatalogPage/CatalogLineCardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseCatalogPage/` contains `CatalogLineCardSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseContents/map.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseContents/` contains `map.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseCtaButtons/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseCtaButtons/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseCurriculum/ModuleAccordionItem/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseCurriculum/ModuleAccordionItem/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseCurriculum/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseCurriculum/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseCurriculum/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseCurriculum/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseFaq/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseFaq/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseFaq/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseFaq/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseHero/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseHero/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseMobileEnrollBar/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseMobileEnrollBar/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CoursePrerequisites/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CoursePrerequisites/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CoursePrerequisites/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CoursePrerequisites/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CoursePricingRail/PhaseRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CoursePricingRail/PhaseRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CoursePricingRail/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CoursePricingRail/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseTrustStats/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseTrustStats/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseValueProps/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseValueProps/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseValueProps/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseValueProps/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/constants.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `constants.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/hooks/useCourseEnrollment.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `hooks/useCourseEnrollment.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/hooks/usePricingRows.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `hooks/usePricingRows.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/types.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `types.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CvEditorPage/CvEditorToolbarBar/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CvEditorPage/` contains `CvEditorToolbarBar/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CvGalleryPage/CvGallery/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CvGalleryPage/` contains `CvGallery/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CvGalleryPage/CvGallery/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CvGalleryPage/` contains `CvGallery/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/AiQuotaCard/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `AiQuotaCard/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/AiQuotaCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `AiQuotaCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ChangelogList/ChangelogListSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ChangelogList/ChangelogListSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ChangelogList/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ChangelogList/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ChangelogList/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ChangelogList/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/CommunityTab/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `CommunityTab/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ContinueLearning/ContinueLearningSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ContinueLearning/ContinueLearningSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ContinueLearning/ResumeCard/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ContinueLearning/ResumeCard/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ContinueLearning/ResumeCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ContinueLearning/ResumeCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ContinueLearning/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ContinueLearning/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ContinueLearning/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ContinueLearning/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ContinueLearning/useResumeItems.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ContinueLearning/useResumeItems.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/CoursesTab/MyCoursesProgress/CourseRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `CoursesTab/MyCoursesProgress/CourseRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/CoursesTab/MyCoursesProgress/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `CoursesTab/MyCoursesProgress/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/CoursesTab/MyCoursesProgress/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `CoursesTab/MyCoursesProgress/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/CoursesTab/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `CoursesTab/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/DailyQuest/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `DailyQuest/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/DailyQuest/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `DailyQuest/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/DashboardIdentity/IdentityStats/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `DashboardIdentity/IdentityStats/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/DashboardIdentity/IdentityStats/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `DashboardIdentity/IdentityStats/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/DashboardIdentity/ProfileMenuCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `DashboardIdentity/ProfileMenuCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/DashboardIdentity/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `DashboardIdentity/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/DashboardTabsBar/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `DashboardTabsBar/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ExploreTab/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ExploreTab/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/FeedTabs/FeedTabsSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `FeedTabs/FeedTabsSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/FeedTabs/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `FeedTabs/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/FeedTabs/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `FeedTabs/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/FlashcardReview/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `FlashcardReview/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/FlashcardReview/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `FlashcardReview/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/LeagueCard/LeagueCardContent/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `LeagueCard/LeagueCardContent/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/LeagueCard/LeagueCardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `LeagueCard/LeagueCardSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/LeagueCard/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `LeagueCard/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/LeagueCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `LeagueCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/LeagueTierBadge/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `LeagueTierBadge/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/OverviewTab/JobReadinessWidget/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `OverviewTab/JobReadinessWidget/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/OverviewTab/JobReadinessWidget/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `OverviewTab/JobReadinessWidget/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/OverviewTab/OverviewContributions/OverviewContributionsSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `OverviewTab/OverviewContributions/OverviewContributionsSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/OverviewTab/OverviewContributions/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `OverviewTab/OverviewContributions/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/OverviewTab/OverviewContributions/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `OverviewTab/OverviewContributions/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/OverviewTab/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `OverviewTab/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/QuickActions/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `QuickActions/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/RecommendedCourses/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `RecommendedCourses/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/RecommendedCourses/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `RecommendedCourses/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/StreakFreezeCard/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `StreakFreezeCard/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/StreakFreezeCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `StreakFreezeCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/StreakStrip/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `StreakStrip/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/StreakStrip/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `StreakStrip/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/TopLearners/TopLearnersSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `TopLearners/TopLearnersSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/TopLearners/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `TopLearners/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/TopLearners/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `TopLearners/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/TrendingContents/TrendingContentsSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `TrendingContents/TrendingContentsSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/TrendingContents/TrendingRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `TrendingContents/TrendingRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/TrendingContents/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `TrendingContents/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/TrendingContents/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `TrendingContents/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/UpcomingLivestreamCard/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `UpcomingLivestreamCard/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/UpcomingLivestreamCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `UpcomingLivestreamCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/WeeklyChallengeCard/WeeklyChallengeCardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `WeeklyChallengeCard/WeeklyChallengeCardSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/WeeklyChallengeCard/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `WeeklyChallengeCard/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/WeeklyChallengeCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `WeeklyChallengeCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/WeeklyGoals/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `WeeklyGoals/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/WeeklyGoals/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `WeeklyGoals/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/WeeklyGoals/map.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `WeeklyGoals/map.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/WhoToFollow/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `WhoToFollow/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/WhoToFollow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `WhoToFollow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/hooks/useDashboardTabUrlSync.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `hooks/useDashboardTabUrlSync.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/types.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `types.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/EditProfilePage/AvatarUploadModal/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/EditProfilePage/` contains `AvatarUploadModal/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/DueReviewHero/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `DueReviewHero/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/DueReviewHero/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `DueReviewHero/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/DueReview/DueReviewSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `DueReview/DueReviewSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/DueReview/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `DueReview/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/DueReview/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `DueReview/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardDeckList/FlashcardDeckListSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardDeckList/FlashcardDeckListSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardDeckList/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardDeckList/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardDeckList/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardDeckList/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardMobileNav/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardMobileNav/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardQuizResult/FlashcardQuizResultSkeleton.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardQuizResult/FlashcardQuizResultSkeleton.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardQuizResult/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardQuizResult/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardQuizResult/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardQuizResult/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardQuizResult/recapBlocks.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardQuizResult/recapBlocks.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewHistory/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardReviewHistory/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewHistory/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardReviewHistory/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewModeModal/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardReviewModeModal/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewStats/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardReviewStats/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewStats/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardReviewStats/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewer/FlashcardReviewerSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardReviewer/FlashcardReviewerSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewer/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardReviewer/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewer/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardReviewer/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardSessionStats/FlashcardSessionStatsSkeleton.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardSessionStats/FlashcardSessionStatsSkeleton.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardSessionStats/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardSessionStats/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardSessionStats/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardSessionStats/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardStatsStrip/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardStatsStrip/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardStatsStrip/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardStatsStrip/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardStudyRail/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardStudyRail/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardStudyRail/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardStudyRail/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/QuizSession/FlashcardQuizHistory/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `QuizSession/FlashcardQuizHistory/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/QuizSession/FlashcardQuizHistory/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `QuizSession/FlashcardQuizHistory/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/QuizSession/FlashcardQuizStats/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `QuizSession/FlashcardQuizStats/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/QuizSession/FlashcardQuizStats/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `QuizSession/FlashcardQuizStats/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/QuizSession/QuizSessionSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `QuizSession/QuizSessionSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/QuizSession/build-cloze.ts",
        "line": 20,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `QuizSession/build-cloze.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/QuizSession/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `QuizSession/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/constants/index.ts",
        "line": 2,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `constants/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/useFlashcardNav.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `useFlashcardNav.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/useStartFlashcardDueReviewSession.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `useStartFlashcardDueReviewSession.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/useStartFlashcardReviewSession.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `useStartFlashcardReviewSession.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/HeadhuntingCompanyPage/HeadhuntingCompanyBreadcrumbs/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/HeadhuntingCompanyPage/` contains `HeadhuntingCompanyBreadcrumbs/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/HeadhuntingCompanyPage/HeadhuntingCompanyConsultants/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/HeadhuntingCompanyPage/` contains `HeadhuntingCompanyConsultants/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/HeadhuntingCompanyPage/HeadhuntingCompanyConsultants/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/HeadhuntingCompanyPage/` contains `HeadhuntingCompanyConsultants/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/HeadhuntingCompanyPage/HeadhuntingCompanyLoadingState/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/HeadhuntingCompanyPage/` contains `HeadhuntingCompanyLoadingState/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/HeadhuntingCompanyPage/HeadhuntingCompanyProfile/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/HeadhuntingCompanyPage/` contains `HeadhuntingCompanyProfile/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/JobListPage/JobListRowSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/JobListPage/` contains `JobListRowSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/JobListPage/JobListRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/JobListPage/` contains `JobListRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/JobPostFormPage/ApplyMethodSection/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/JobPostFormPage/` contains `ApplyMethodSection/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/JobPostFormPage/CompanySection/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/JobPostFormPage/` contains `CompanySection/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/JobPostFormPage/PositionSection/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/JobPostFormPage/` contains `PositionSection/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/JobPostFormPage/SubmitSuccess/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/JobPostFormPage/` contains `SubmitSuccess/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/KpiPage/kpiMeta.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/KpiPage/` contains `kpiMeta.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LandingPage/KnowledgeGraph/ConceptNode.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LandingPage/` contains `KnowledgeGraph/ConceptNode.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LandingPage/KnowledgeGraph/ShuffleBeacon.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LandingPage/` contains `KnowledgeGraph/ShuffleBeacon.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LandingPage/KnowledgeGraph/data.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/LandingPage/` contains `KnowledgeGraph/data.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LandingPage/KnowledgeGraph/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LandingPage/` contains `KnowledgeGraph/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LandingPage/LearnLoopScroll/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LandingPage/` contains `LearnLoopScroll/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LandingPage/StatStrip/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LandingPage/` contains `StatStrip/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LandingPage/StatStrip/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LandingPage/` contains `StatStrip/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LandingPage/TalentMarketplace/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LandingPage/` contains `TalentMarketplace/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LandingPage/constants/index.ts",
        "line": 2,
        "col": 1,
        "msg": "`pages/LandingPage/` contains `constants/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LeaguePage/GlobalBoard/GlobalBoardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LeaguePage/` contains `GlobalBoard/GlobalBoardSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LeaguePage/GlobalBoard/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LeaguePage/` contains `GlobalBoard/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LeaguePage/GlobalBoard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LeaguePage/` contains `GlobalBoard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LeaguePage/WeeklyBoard/WeeklyBoardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LeaguePage/` contains `WeeklyBoard/WeeklyBoardSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LeaguePage/WeeklyBoard/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LeaguePage/` contains `WeeklyBoard/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LeaguePage/WeeklyBoard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LeaguePage/` contains `WeeklyBoard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/CourseDetail/CourseDayTimeline/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LearningHistoryPage/` contains `CourseDetail/CourseDayTimeline/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/CourseDetail/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LearningHistoryPage/` contains `CourseDetail/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/CourseMilestoneOutline/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LearningHistoryPage/` contains `CourseMilestoneOutline/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/CourseOutline/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LearningHistoryPage/` contains `CourseOutline/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/CourseOutline/map.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/LearningHistoryPage/` contains `CourseOutline/map.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/hooks/useSelectedCourse.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/LearningHistoryPage/` contains `hooks/useSelectedCourse.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/InterviewerPresence/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `InterviewerPresence/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewDiagram/BoxNode.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewDiagram/BoxNode.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewDiagram/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewDiagram/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewDiagram/serialize.ts",
        "line": 2,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewDiagram/serialize.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewGatePreview/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewGatePreview/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewHistory/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewHistory/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewResult/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewResult/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewScorecard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewScorecard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewSessionSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewSessionSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewSession/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewSession/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewSetupSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewSetupSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewStats/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewStats/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewTrackSnapshot/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewTrackSnapshot/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewWorkspace/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewWorkspace/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/VoiceHero/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `VoiceHero/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/VoiceUnavailableModal/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `VoiceUnavailableModal/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/interviewPersona.ts",
        "line": 2,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `interviewPersona.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/mapAttemptToGradeResult.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `mapAttemptToGradeResult.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/types.ts",
        "line": 10,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `types.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/OauthRedirectPage/enums/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/OauthRedirectPage/` contains `enums/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/OauthRedirectPage/enums/oauth-action.ts",
        "line": 6,
        "col": 1,
        "msg": "`pages/OauthRedirectPage/` contains `enums/oauth-action.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/OauthRedirectPage/map.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/OauthRedirectPage/` contains `map.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/CodingLeaderboard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `CodingLeaderboard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/PracticeFilters/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `PracticeFilters/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/PracticeRail/PracticeMobileNav/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `PracticeRail/PracticeMobileNav/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/PracticeRail/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `PracticeRail/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/ProblemCatalog/ProblemRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `ProblemCatalog/ProblemRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/ProblemCatalog/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `ProblemCatalog/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/ProgressCockpit/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `ProgressCockpit/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/constants/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `constants/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/hooks/useCodingProblemsSwr.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `hooks/useCodingProblemsSwr.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/hooks/useMyCodingProgressSwr.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `hooks/useMyCodingProgressSwr.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/hooks/usePracticeFilters.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `hooks/usePracticeFilters.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/hooks/usePracticeView.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `hooks/usePracticeView.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `types/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/utils/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `utils/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeProblemPage/PracticeProblemSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeProblemPage/` contains `PracticeProblemSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileActivityPage/ProfileAchievements/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileActivityPage/` contains `ProfileAchievements/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileActivityPage/ProfileActivity/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileActivityPage/` contains `ProfileActivity/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileChallengesPage/ProfileChallenges/ChallengeCourseRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileChallengesPage/` contains `ProfileChallenges/ChallengeCourseRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileChallengesPage/ProfileChallenges/groupByCourse.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileChallengesPage/` contains `ProfileChallenges/groupByCourse.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileChallengesPage/ProfileChallenges/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileChallengesPage/` contains `ProfileChallenges/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileOverviewPage/OverviewChallengeSkills/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileOverviewPage/` contains `OverviewChallengeSkills/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileOverviewPage/OverviewCodeSkills/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileOverviewPage/` contains `OverviewCodeSkills/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileOverviewPage/OverviewContributions/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileOverviewPage/` contains `OverviewContributions/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileOverviewPage/OverviewCourses/CourseRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileOverviewPage/` contains `OverviewCourses/CourseRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileOverviewPage/OverviewCourses/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileOverviewPage/` contains `OverviewCourses/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileOverviewPage/ProfileJobReadiness/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileOverviewPage/` contains `ProfileJobReadiness/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileProjectsPage/ProfileCapstone/ProjectCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileProjectsPage/` contains `ProfileCapstone/ProjectCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileProjectsPage/ProfileCapstone/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileProjectsPage/` contains `ProfileCapstone/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileProjectsPage/ProfilePinned/PinnedProjectCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileProjectsPage/` contains `ProfilePinned/PinnedProjectCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileProjectsPage/ProfilePinned/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileProjectsPage/` contains `ProfilePinned/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileSkillsPage/ProfileCoding/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileSkillsPage/` contains `ProfileCoding/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/RewardsPage/MyVouchers/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/RewardsPage/` contains `MyVouchers/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/RewardsPage/RewardCatalog/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/RewardsPage/` contains `RewardCatalog/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/CheckoutExpired/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SepayCheckoutPage/` contains `CheckoutExpired/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/DetailRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SepayCheckoutPage/` contains `DetailRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/EnrolledSuccess/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SepayCheckoutPage/` contains `EnrolledSuccess/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/OrderSummary/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SepayCheckoutPage/` contains `OrderSummary/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/QrPanel/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SepayCheckoutPage/` contains `QrPanel/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/SepayCheckoutPage/` contains `types/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/types/sepay-checkout.ts",
        "line": 7,
        "col": 1,
        "msg": "`pages/SepayCheckoutPage/` contains `types/sepay-checkout.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/utils/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/SepayCheckoutPage/` contains `utils/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/utils/parse-bank-details.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/SepayCheckoutPage/` contains `utils/parse-bank-details.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SystemStatusPage/AiKeyGroup/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SystemStatusPage/` contains `AiKeyGroup/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SystemStatusPage/ComponentCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SystemStatusPage/` contains `ComponentCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SystemStatusPage/OverallBanner/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SystemStatusPage/` contains `OverallBanner/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SystemStatusPage/SystemStatusSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SystemStatusPage/` contains `SystemStatusSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SystemStatusPage/map.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SystemStatusPage/` contains `map.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      }
    ],
    "unlocked": [
      {
        "file": "src/components/layouts/Footer/FooterNavColumn/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Footer/` contains `FooterNavColumn/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/LearnShellLayout/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`layouts/LearnShellLayout/` contains `types/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/AccountMenuDropdown/AccountMenuAuthed/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `AccountMenuDropdown/AccountMenuAuthed/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/AccountMenuDropdown/AccountMenuGuest/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `AccountMenuDropdown/AccountMenuGuest/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/AccountMenuDropdown/AccountTrigger/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `AccountMenuDropdown/AccountTrigger/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/AccountMenuDropdown/DarkLightModeSwitch/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `AccountMenuDropdown/DarkLightModeSwitch/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/AccountMenuDropdown/GuestHeader/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `AccountMenuDropdown/GuestHeader/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/AccountMenuDropdown/UserSummary/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `AccountMenuDropdown/UserSummary/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/AccountMenuDropdown/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `AccountMenuDropdown/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/CartButton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `CartButton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/LanguageDropdown/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `LanguageDropdown/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/Logo/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `Logo/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/NavLinks/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `NavLinks/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/NotificationBell/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `NotificationBell/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/SearchButton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `SearchButton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `types/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/types/navbar.ts",
        "line": 4,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `types/navbar.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/Navbar/useNavbarItems.ts",
        "line": 1,
        "col": 1,
        "msg": "`layouts/Navbar/` contains `useNavbarItems.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/PublicProfileLayout/map.ts",
        "line": 1,
        "col": 1,
        "msg": "`layouts/PublicProfileLayout/` contains `map.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/layouts/SettingsLayout/map.ts",
        "line": 1,
        "col": 1,
        "msg": "`layouts/SettingsLayout/` contains `map.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/drawers/SubmissionAttemptsDrawer/map.ts",
        "line": 1,
        "col": 1,
        "msg": "`overlays/SubmissionAttemptsDrawer/` contains `map.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AiQuotaModal/hooks/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AiQuotaModal/` contains `hooks/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AiQuotaModal/hooks/useAiQuotaHistorySwr.ts",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AiQuotaModal/` contains `hooks/useAiQuotaHistorySwr.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AiQuotaModal/hooks/useWindowResetLabel.ts",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AiQuotaModal/` contains `hooks/useWindowResetLabel.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AiQuotaModal/map.ts",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AiQuotaModal/` contains `map.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/EmailField/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/CredentialsState/EmailField/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/OauthButtons/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/CredentialsState/OauthButtons/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/PasswordField/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/CredentialsState/PasswordField/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/RememberMeRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/CredentialsState/RememberMeRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/SignUpPrompt/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/CredentialsState/SignUpPrompt/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/CredentialsState/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/map.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/CredentialsState/map.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/types/credentials-state.ts",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/CredentialsState/types/credentials-state.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/CredentialsState/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/CredentialsState/types/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/OtpState/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/OtpState/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignInSection/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignInSection/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignUpSection/OtpState/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignUpSection/OtpState/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignUpSection/OtpState/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignUpSection/OtpState/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignUpSection/RegistrationState/AgreeToTermsRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignUpSection/RegistrationState/AgreeToTermsRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignUpSection/RegistrationState/EmailField/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignUpSection/RegistrationState/EmailField/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignUpSection/RegistrationState/PasswordField/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignUpSection/RegistrationState/PasswordField/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignUpSection/RegistrationState/SignInPrompt/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignUpSection/RegistrationState/SignInPrompt/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignUpSection/RegistrationState/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignUpSection/RegistrationState/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignUpSection/RegistrationState/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignUpSection/RegistrationState/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/AuthenticationModal/SignUpSection/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/AuthenticationModal/` contains `SignUpSection/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/GlobalSearchModal/Content/Block/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/GlobalSearchModal/` contains `Content/Block/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/GlobalSearchModal/Content/Empty/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/GlobalSearchModal/` contains `Content/Empty/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/GlobalSearchModal/Content/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/GlobalSearchModal/` contains `Content/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/ManagePinnedProjectsModal/CourseProjectForm/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/ManagePinnedProjectsModal/` contains `CourseProjectForm/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/ManagePinnedProjectsModal/CourseProjectForm/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/ManagePinnedProjectsModal/` contains `CourseProjectForm/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/ManagePinnedProjectsModal/ExternalProjectForm/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/ManagePinnedProjectsModal/` contains `ExternalProjectForm/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/ManagePinnedProjectsModal/ExternalProjectForm/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/ManagePinnedProjectsModal/` contains `ExternalProjectForm/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/ManagePinnedProjectsModal/ManagePinnedListSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/ManagePinnedProjectsModal/` contains `ManagePinnedListSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/ManagePinnedProjectsModal/PinnedProjectCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`overlays/ManagePinnedProjectsModal/` contains `PinnedProjectCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/ManagePinnedProjectsModal/hooks/usePinnedProjectsManager.ts",
        "line": 1,
        "col": 1,
        "msg": "`overlays/ManagePinnedProjectsModal/` contains `hooks/usePinnedProjectsManager.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/overlays/modals/PremiumGateModal/map.ts",
        "line": 1,
        "col": 1,
        "msg": "`overlays/PremiumGateModal/` contains `map.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminAiBalancerPage/ProviderSection/KeyRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminAiBalancerPage/` contains `ProviderSection/KeyRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminAiBalancerPage/TopBar/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminAiBalancerPage/` contains `TopBar/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/ConfigCard/RendererTypeButton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `ConfigCard/RendererTypeButton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/ConfigCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `ConfigCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/Header/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `Header/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/QuickTestUrls/QuickTestUrlButton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `QuickTestUrls/QuickTestUrlButton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/QuickTestUrls/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `QuickTestUrls/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/constants/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `constants/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/constants/quick-test-urls.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `constants/quick-test-urls.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/map.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `map.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/types/admin-mpeg-dash-test.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `types/admin-mpeg-dash-test.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `types/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/FileSelectionCard/ProcessResultBanner/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `FileSelectionCard/ProcessResultBanner/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/Header/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `Header/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/LoadingScreen/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `LoadingScreen/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/TopBar/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `TopBar/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/UploadProgressCard/ProviderUploadRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `UploadProgressCard/ProviderUploadRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/enums/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `enums/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/enums/upload-status.ts",
        "line": 7,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `enums/upload-status.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/map.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `map.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/types/admin-upload-video.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `types/admin-upload-video.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `types/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/utils/format-size.ts",
        "line": 9,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `utils/format-size.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/utils/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `utils/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AiUsagePage/AiUsageHistory/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AiUsagePage/` contains `AiUsageHistory/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AiUsagePage/hooks/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AiUsagePage/` contains `hooks/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AiUsagePage/hooks/quota-lane-variant.ts",
        "line": 2,
        "col": 1,
        "msg": "`pages/AiUsagePage/` contains `hooks/quota-lane-variant.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AiUsagePage/hooks/useQuotaLaneData.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/AiUsagePage/` contains `hooks/useQuotaLaneData.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/ArchitectureMap/future-scene.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `ArchitectureMap/future-scene.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/ArchitectureMap/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `ArchitectureMap/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/ArchitectureMap/scene.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `ArchitectureMap/scene.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/ArchitectureRail/ArchitectureMobileNav/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `ArchitectureRail/ArchitectureMobileNav/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/ArchitectureRail/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `ArchitectureRail/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/CurlTester/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `CurlTester/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/CurlTester/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `CurlTester/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/MetricsInline/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `MetricsInline/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/NodeDissectionPanel/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `NodeDissectionPanel/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/constants.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `constants.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/hooks/useArchitectureNode.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `hooks/useArchitectureNode.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/hooks/useSystemHealthPoll.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `hooks/useSystemHealthPoll.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/metricsFormat.ts",
        "line": 15,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `metricsFormat.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/modules.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `modules.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ArchitecturePage/statusVisual.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ArchitecturePage/` contains `statusVisual.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/BlogListPage/CategoryFilter/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/BlogListPage/` contains `CategoryFilter/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/BlogListPage/FeaturedPost/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/BlogListPage/` contains `FeaturedPost/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/BlogListPage/Masthead/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/BlogListPage/` contains `Masthead/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/BlogListPage/Masthead/scene.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/BlogListPage/` contains `Masthead/scene.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/BlogListPage/StartHereAnchor/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/BlogListPage/` contains `StartHereAnchor/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/BlogListPage/TopicsStrip/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/BlogListPage/` contains `TopicsStrip/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/BlogPostPage/ReadingProgress/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/BlogPostPage/` contains `ReadingProgress/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/BlogPostPage/RelatedPosts/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/BlogPostPage/` contains `RelatedPosts/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/BookmarksPage/BookmarkCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/BookmarksPage/` contains `BookmarkCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ChallengeResultPage/map.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/ChallengeResultPage/` contains `map.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CommunityChatPage/ChatPane/ChatPaneSkeleton.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CommunityChatPage/` contains `ChatPane/ChatPaneSkeleton.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CommunityChatPage/ChatPane/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CommunityChatPage/` contains `ChatPane/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CommunityChatPage/ChatPane/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CommunityChatPage/` contains `ChatPane/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CommunityFeedPage/CommunityComposer/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CommunityFeedPage/` contains `CommunityComposer/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ContactPage/ContactChannels/FounderCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ContactPage/` contains `ContactChannels/FounderCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ContactPage/ContactChannels/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ContactPage/` contains `ContactChannels/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ContactPage/ContactFaq/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ContactPage/` contains `ContactFaq/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ContactPage/ContactForm/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ContactPage/` contains `ContactForm/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseCatalogPage/CatalogCourseCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseCatalogPage/` contains `CatalogCourseCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseCatalogPage/CatalogLineCardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseCatalogPage/` contains `CatalogLineCardSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseContents/map.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseContents/` contains `map.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseCtaButtons/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseCtaButtons/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseCurriculum/ModuleAccordionItem/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseCurriculum/ModuleAccordionItem/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseCurriculum/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseCurriculum/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseCurriculum/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseCurriculum/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseFaq/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseFaq/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseFaq/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseFaq/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseHero/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseHero/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseMobileEnrollBar/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseMobileEnrollBar/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CoursePrerequisites/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CoursePrerequisites/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CoursePrerequisites/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CoursePrerequisites/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CoursePricingRail/PhaseRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CoursePricingRail/PhaseRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CoursePricingRail/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CoursePricingRail/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseTrustStats/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseTrustStats/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseValueProps/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseValueProps/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/CourseValueProps/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `CourseValueProps/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/constants.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `constants.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/hooks/useCourseEnrollment.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `hooks/useCourseEnrollment.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/hooks/usePricingRows.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `hooks/usePricingRows.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CourseDetailPage/types.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/CourseDetailPage/` contains `types.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CvEditorPage/CvEditorToolbarBar/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CvEditorPage/` contains `CvEditorToolbarBar/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CvGalleryPage/CvGallery/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CvGalleryPage/` contains `CvGallery/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/CvGalleryPage/CvGallery/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/CvGalleryPage/` contains `CvGallery/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/AiQuotaCard/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `AiQuotaCard/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/AiQuotaCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `AiQuotaCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ChangelogList/ChangelogListSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ChangelogList/ChangelogListSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ChangelogList/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ChangelogList/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ChangelogList/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ChangelogList/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/CommunityTab/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `CommunityTab/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ContinueLearning/ContinueLearningSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ContinueLearning/ContinueLearningSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ContinueLearning/ResumeCard/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ContinueLearning/ResumeCard/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ContinueLearning/ResumeCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ContinueLearning/ResumeCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ContinueLearning/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ContinueLearning/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ContinueLearning/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ContinueLearning/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ContinueLearning/useResumeItems.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ContinueLearning/useResumeItems.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/CoursesTab/MyCoursesProgress/CourseRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `CoursesTab/MyCoursesProgress/CourseRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/CoursesTab/MyCoursesProgress/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `CoursesTab/MyCoursesProgress/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/CoursesTab/MyCoursesProgress/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `CoursesTab/MyCoursesProgress/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/CoursesTab/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `CoursesTab/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/DailyQuest/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `DailyQuest/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/DailyQuest/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `DailyQuest/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/DashboardIdentity/IdentityStats/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `DashboardIdentity/IdentityStats/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/DashboardIdentity/IdentityStats/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `DashboardIdentity/IdentityStats/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/DashboardIdentity/ProfileMenuCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `DashboardIdentity/ProfileMenuCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/DashboardIdentity/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `DashboardIdentity/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/DashboardTabsBar/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `DashboardTabsBar/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/ExploreTab/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `ExploreTab/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/FeedTabs/FeedTabsSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `FeedTabs/FeedTabsSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/FeedTabs/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `FeedTabs/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/FeedTabs/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `FeedTabs/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/FlashcardReview/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `FlashcardReview/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/FlashcardReview/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `FlashcardReview/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/LeagueCard/LeagueCardContent/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `LeagueCard/LeagueCardContent/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/LeagueCard/LeagueCardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `LeagueCard/LeagueCardSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/LeagueCard/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `LeagueCard/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/LeagueCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `LeagueCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/LeagueTierBadge/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `LeagueTierBadge/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/OverviewTab/JobReadinessWidget/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `OverviewTab/JobReadinessWidget/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/OverviewTab/JobReadinessWidget/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `OverviewTab/JobReadinessWidget/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/OverviewTab/OverviewContributions/OverviewContributionsSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `OverviewTab/OverviewContributions/OverviewContributionsSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/OverviewTab/OverviewContributions/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `OverviewTab/OverviewContributions/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/OverviewTab/OverviewContributions/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `OverviewTab/OverviewContributions/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/OverviewTab/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `OverviewTab/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/QuickActions/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `QuickActions/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/RecommendedCourses/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `RecommendedCourses/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/RecommendedCourses/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `RecommendedCourses/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/StreakFreezeCard/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `StreakFreezeCard/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/StreakFreezeCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `StreakFreezeCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/StreakStrip/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `StreakStrip/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/StreakStrip/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `StreakStrip/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/TopLearners/TopLearnersSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `TopLearners/TopLearnersSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/TopLearners/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `TopLearners/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/TopLearners/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `TopLearners/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/TrendingContents/TrendingContentsSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `TrendingContents/TrendingContentsSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/TrendingContents/TrendingRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `TrendingContents/TrendingRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/TrendingContents/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `TrendingContents/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/TrendingContents/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `TrendingContents/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/UpcomingLivestreamCard/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `UpcomingLivestreamCard/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/UpcomingLivestreamCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `UpcomingLivestreamCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/WeeklyChallengeCard/WeeklyChallengeCardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `WeeklyChallengeCard/WeeklyChallengeCardSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/WeeklyChallengeCard/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `WeeklyChallengeCard/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/WeeklyChallengeCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `WeeklyChallengeCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/WeeklyGoals/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `WeeklyGoals/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/WeeklyGoals/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `WeeklyGoals/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/WeeklyGoals/map.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `WeeklyGoals/map.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/WhoToFollow/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `WhoToFollow/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/WhoToFollow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `WhoToFollow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/hooks/useDashboardTabUrlSync.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `hooks/useDashboardTabUrlSync.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/DashboardPage/types.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/DashboardPage/` contains `types.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/EditProfilePage/AvatarUploadModal/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/EditProfilePage/` contains `AvatarUploadModal/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/DueReviewHero/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `DueReviewHero/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/DueReviewHero/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `DueReviewHero/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/DueReview/DueReviewSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `DueReview/DueReviewSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/DueReview/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `DueReview/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/DueReview/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `DueReview/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardDeckList/FlashcardDeckListSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardDeckList/FlashcardDeckListSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardDeckList/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardDeckList/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardDeckList/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardDeckList/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardMobileNav/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardMobileNav/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardQuizResult/FlashcardQuizResultSkeleton.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardQuizResult/FlashcardQuizResultSkeleton.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardQuizResult/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardQuizResult/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardQuizResult/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardQuizResult/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardQuizResult/recapBlocks.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardQuizResult/recapBlocks.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewHistory/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardReviewHistory/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewHistory/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardReviewHistory/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewModeModal/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardReviewModeModal/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewStats/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardReviewStats/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewStats/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardReviewStats/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewer/FlashcardReviewerSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardReviewer/FlashcardReviewerSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewer/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardReviewer/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardReviewer/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardReviewer/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardSessionStats/FlashcardSessionStatsSkeleton.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardSessionStats/FlashcardSessionStatsSkeleton.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardSessionStats/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardSessionStats/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardSessionStats/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardSessionStats/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardStatsStrip/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardStatsStrip/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardStatsStrip/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardStatsStrip/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardStudyRail/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardStudyRail/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/FlashcardStudyRail/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `FlashcardStudyRail/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/constants/index.ts",
        "line": 2,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `constants/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/useFlashcardNav.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `useFlashcardNav.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/useStartFlashcardDueReviewSession.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `useStartFlashcardDueReviewSession.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/useStartFlashcardReviewSession.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `useStartFlashcardReviewSession.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/HeadhuntingCompanyPage/HeadhuntingCompanyBreadcrumbs/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/HeadhuntingCompanyPage/` contains `HeadhuntingCompanyBreadcrumbs/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/HeadhuntingCompanyPage/HeadhuntingCompanyConsultants/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/HeadhuntingCompanyPage/` contains `HeadhuntingCompanyConsultants/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/HeadhuntingCompanyPage/HeadhuntingCompanyConsultants/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/HeadhuntingCompanyPage/` contains `HeadhuntingCompanyConsultants/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/HeadhuntingCompanyPage/HeadhuntingCompanyLoadingState/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/HeadhuntingCompanyPage/` contains `HeadhuntingCompanyLoadingState/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/HeadhuntingCompanyPage/HeadhuntingCompanyProfile/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/HeadhuntingCompanyPage/` contains `HeadhuntingCompanyProfile/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/JobListPage/JobListRowSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/JobListPage/` contains `JobListRowSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/JobListPage/JobListRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/JobListPage/` contains `JobListRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/JobPostFormPage/ApplyMethodSection/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/JobPostFormPage/` contains `ApplyMethodSection/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/JobPostFormPage/CompanySection/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/JobPostFormPage/` contains `CompanySection/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/JobPostFormPage/PositionSection/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/JobPostFormPage/` contains `PositionSection/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/JobPostFormPage/SubmitSuccess/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/JobPostFormPage/` contains `SubmitSuccess/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/KpiPage/kpiMeta.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/KpiPage/` contains `kpiMeta.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LandingPage/KnowledgeGraph/data.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/LandingPage/` contains `KnowledgeGraph/data.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LandingPage/KnowledgeGraph/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LandingPage/` contains `KnowledgeGraph/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LandingPage/StatStrip/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LandingPage/` contains `StatStrip/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LandingPage/constants/index.ts",
        "line": 2,
        "col": 1,
        "msg": "`pages/LandingPage/` contains `constants/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LeaguePage/GlobalBoard/GlobalBoardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LeaguePage/` contains `GlobalBoard/GlobalBoardSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LeaguePage/GlobalBoard/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LeaguePage/` contains `GlobalBoard/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LeaguePage/GlobalBoard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LeaguePage/` contains `GlobalBoard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LeaguePage/WeeklyBoard/WeeklyBoardSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LeaguePage/` contains `WeeklyBoard/WeeklyBoardSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LeaguePage/WeeklyBoard/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LeaguePage/` contains `WeeklyBoard/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LeaguePage/WeeklyBoard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LeaguePage/` contains `WeeklyBoard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/CourseDetail/CourseDayTimeline/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LearningHistoryPage/` contains `CourseDetail/CourseDayTimeline/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/CourseDetail/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LearningHistoryPage/` contains `CourseDetail/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/CourseMilestoneOutline/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LearningHistoryPage/` contains `CourseMilestoneOutline/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/CourseOutline/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LearningHistoryPage/` contains `CourseOutline/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/CourseOutline/map.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/LearningHistoryPage/` contains `CourseOutline/map.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LearningHistoryPage/hooks/useSelectedCourse.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/LearningHistoryPage/` contains `hooks/useSelectedCourse.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/InterviewerPresence/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `InterviewerPresence/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewDiagram/BoxNode.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewDiagram/BoxNode.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewDiagram/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewDiagram/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewDiagram/serialize.ts",
        "line": 2,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewDiagram/serialize.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewHistory/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewHistory/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewResult/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewResult/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewScorecard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewScorecard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewSetupSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewSetupSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewStats/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewStats/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewTrackSnapshot/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewTrackSnapshot/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewWorkspace/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewWorkspace/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/VoiceHero/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `VoiceHero/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/VoiceUnavailableModal/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `VoiceUnavailableModal/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/interviewPersona.ts",
        "line": 2,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `interviewPersona.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/mapAttemptToGradeResult.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `mapAttemptToGradeResult.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/types.ts",
        "line": 10,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `types.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/OauthRedirectPage/enums/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/OauthRedirectPage/` contains `enums/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/OauthRedirectPage/enums/oauth-action.ts",
        "line": 6,
        "col": 1,
        "msg": "`pages/OauthRedirectPage/` contains `enums/oauth-action.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/OauthRedirectPage/map.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/OauthRedirectPage/` contains `map.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/CodingLeaderboard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `CodingLeaderboard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/PracticeFilters/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `PracticeFilters/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/PracticeRail/PracticeMobileNav/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `PracticeRail/PracticeMobileNav/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/PracticeRail/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `PracticeRail/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/ProblemCatalog/ProblemRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `ProblemCatalog/ProblemRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/ProblemCatalog/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `ProblemCatalog/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/ProgressCockpit/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `ProgressCockpit/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/constants/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `constants/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/hooks/useCodingProblemsSwr.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `hooks/useCodingProblemsSwr.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/hooks/useMyCodingProgressSwr.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `hooks/useMyCodingProgressSwr.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/hooks/usePracticeFilters.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `hooks/usePracticeFilters.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/hooks/usePracticeView.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `hooks/usePracticeView.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `types/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeHubPage/utils/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeHubPage/` contains `utils/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/PracticeProblemPage/PracticeProblemSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/PracticeProblemPage/` contains `PracticeProblemSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileActivityPage/ProfileAchievements/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileActivityPage/` contains `ProfileAchievements/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileActivityPage/ProfileActivity/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileActivityPage/` contains `ProfileActivity/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileChallengesPage/ProfileChallenges/ChallengeCourseRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileChallengesPage/` contains `ProfileChallenges/ChallengeCourseRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileChallengesPage/ProfileChallenges/groupByCourse.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileChallengesPage/` contains `ProfileChallenges/groupByCourse.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileChallengesPage/ProfileChallenges/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileChallengesPage/` contains `ProfileChallenges/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileOverviewPage/OverviewChallengeSkills/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileOverviewPage/` contains `OverviewChallengeSkills/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileOverviewPage/OverviewCodeSkills/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileOverviewPage/` contains `OverviewCodeSkills/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileOverviewPage/OverviewContributions/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileOverviewPage/` contains `OverviewContributions/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileOverviewPage/OverviewCourses/CourseRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileOverviewPage/` contains `OverviewCourses/CourseRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileOverviewPage/OverviewCourses/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileOverviewPage/` contains `OverviewCourses/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileOverviewPage/ProfileJobReadiness/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileOverviewPage/` contains `ProfileJobReadiness/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileProjectsPage/ProfileCapstone/ProjectCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileProjectsPage/` contains `ProfileCapstone/ProjectCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileProjectsPage/ProfileCapstone/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileProjectsPage/` contains `ProfileCapstone/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileProjectsPage/ProfilePinned/PinnedProjectCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileProjectsPage/` contains `ProfilePinned/PinnedProjectCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileProjectsPage/ProfilePinned/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileProjectsPage/` contains `ProfilePinned/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/ProfileSkillsPage/ProfileCoding/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/ProfileSkillsPage/` contains `ProfileCoding/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/RewardsPage/MyVouchers/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/RewardsPage/` contains `MyVouchers/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/RewardsPage/RewardCatalog/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/RewardsPage/` contains `RewardCatalog/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SystemStatusPage/AiKeyGroup/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SystemStatusPage/` contains `AiKeyGroup/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SystemStatusPage/ComponentCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SystemStatusPage/` contains `ComponentCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SystemStatusPage/OverallBanner/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SystemStatusPage/` contains `OverallBanner/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SystemStatusPage/SystemStatusSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SystemStatusPage/` contains `SystemStatusSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SystemStatusPage/map.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SystemStatusPage/` contains `map.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      }
    ],
    "locked": [
      {
        "file": "src/components/pages/FlashcardsPage/QuizSession/FlashcardQuizHistory/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `QuizSession/FlashcardQuizHistory/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/QuizSession/FlashcardQuizHistory/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `QuizSession/FlashcardQuizHistory/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/QuizSession/FlashcardQuizStats/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `QuizSession/FlashcardQuizStats/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/QuizSession/FlashcardQuizStats/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `QuizSession/FlashcardQuizStats/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/QuizSession/QuizSessionSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `QuizSession/QuizSessionSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/QuizSession/build-cloze.ts",
        "line": 20,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `QuizSession/build-cloze.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/FlashcardsPage/QuizSession/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/FlashcardsPage/` contains `QuizSession/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LandingPage/LearnLoopScroll/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LandingPage/` contains `LearnLoopScroll/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewSessionSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewSessionSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewSession/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewSession/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      }
    ],
    "hold": [
      {
        "file": "src/components/pages/AdminAiBalancerPage/AdminAiBalancerSkeleton/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminAiBalancerPage/` contains `AdminAiBalancerSkeleton/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminAiBalancerPage/ProviderSection/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminAiBalancerPage/` contains `ProviderSection/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminMpegDashTestPage/PreviewCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminMpegDashTestPage/` contains `PreviewCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/FileSelectionCard/DropZone/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `FileSelectionCard/DropZone/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/FileSelectionCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `FileSelectionCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/AdminUploadVideoPage/UploadProgressCard/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/AdminUploadVideoPage/` contains `UploadProgressCard/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LandingPage/KnowledgeGraph/ConceptNode.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LandingPage/` contains `KnowledgeGraph/ConceptNode.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LandingPage/KnowledgeGraph/ShuffleBeacon.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LandingPage/` contains `KnowledgeGraph/ShuffleBeacon.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LandingPage/StatStrip/component.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LandingPage/` contains `StatStrip/component.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/LandingPage/TalentMarketplace/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/LandingPage/` contains `TalentMarketplace/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/MockInterviewPage/MockInterviewGatePreview/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/MockInterviewPage/` contains `MockInterviewGatePreview/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/CheckoutExpired/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SepayCheckoutPage/` contains `CheckoutExpired/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/DetailRow/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SepayCheckoutPage/` contains `DetailRow/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/EnrolledSuccess/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SepayCheckoutPage/` contains `EnrolledSuccess/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/OrderSummary/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SepayCheckoutPage/` contains `OrderSummary/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/QrPanel/index.tsx",
        "line": 1,
        "col": 1,
        "msg": "`pages/SepayCheckoutPage/` contains `QrPanel/index.tsx` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/types/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/SepayCheckoutPage/` contains `types/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/types/sepay-checkout.ts",
        "line": 7,
        "col": 1,
        "msg": "`pages/SepayCheckoutPage/` contains `types/sepay-checkout.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/utils/index.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/SepayCheckoutPage/` contains `utils/index.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      },
      {
        "file": "src/components/pages/SepayCheckoutPage/utils/parse-bank-details.ts",
        "line": 1,
        "col": 1,
        "msg": "`pages/SepayCheckoutPage/` contains `utils/parse-bank-details.ts` — a screen folder holds its two halves ONLY (`component.tsx` = the shape, `index.tsx` = the wiring). Whatever this is, it has a real home: a component of its own goes to `blocks/<category>/`, a fetch goes to `hooks/`, a pure helper to `modules/utils/`, a shape to `modules/types/`, copy or a config map to `resources/`. \"Only this screen uses it\" is how a folder becomes a second codebase."
      }
    ]
  }
}