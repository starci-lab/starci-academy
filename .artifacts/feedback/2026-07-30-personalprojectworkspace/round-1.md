# Vòng 1 — 2026-07-30

Ma trận chạy qua Workflow B2a+B2b (fan-out theo TRỤC, 30 agent: 15 B2a + 15 B2b — mọi trục đều
có ít nhất 1 vùng NGHI_NGO vì phiên này KHÔNG có baseline pixel thật, xem `session.md`).

**Tổng: 156 ĐẠT · 35 LỆCH · 1 CÂM · 63 N/A = 255/255 ô (17 vùng × 15 trục, không ô trống).**

⚠️ **Chưa có số đo pixel thật (Browser pane không hiển thị lúc B0/B1)** — mọi phán quyết LỆCH
dưới đây dựa trên ĐỌC SOURCE THẬT (class Tailwind, props, comment trong code), không phải
`getComputedStyle`. Đủ để trình thầy chọn hướng, nhưng nếu cần xác nhận bằng số đo pixel thì
phải đo lại qua Storybook thật trước khi áp.

## Ma trận cuối

| Vùng | flow | prom | async | frame | naming | seam | inset | surf | text | icon | color | button | press | md | skel |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| D1 | ĐẠT | ĐẠT | N/A | ĐẠT | LỆCH | LỆCH | N/A | N/A | ĐẠT | ĐẠT | ĐẠT | N/A | N/A | LỆCH | ĐẠT |
| D2 | ĐẠT | ĐẠT | ĐẠT | LỆCH | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | LỆCH | ĐẠT |
| D3 | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | N/A | N/A | ĐẠT | N/A | ĐẠT | N/A | N/A | ĐẠT | LỆCH |
| D4 | ĐẠT | LỆCH | ĐẠT | ĐẠT | LỆCH | ĐẠT | ĐẠT | LỆCH | ĐẠT | ĐẠT | LỆCH | N/A | ĐẠT | LỆCH | ĐẠT |
| T1 | ĐẠT | N/A | N/A | ĐẠT | ĐẠT | LỆCH | N/A | N/A | ĐẠT | ĐẠT | ĐẠT | N/A | N/A | LỆCH | ĐẠT |
| T2 | ĐẠT | N/A | N/A | N/A | ĐẠT | LỆCH | N/A | ĐẠT | N/A | ĐẠT | ĐẠT | N/A | ĐẠT | LỆCH | N/A |
| T3 | ĐẠT | N/A | ĐẠT | N/A | ĐẠT | LỆCH | ĐẠT | ĐẠT | ĐẠT | N/A | ĐẠT | N/A | N/A | ĐẠT | ĐẠT |
| T4 | LỆCH | N/A | ĐẠT | ĐẠT | ĐẠT | LỆCH | ĐẠT | LỆCH | LỆCH | ĐẠT | ĐẠT | N/A | ĐẠT | ĐẠT | ĐẠT |
| T5 | LỆCH | ĐẠT | ĐẠT | ĐẠT | ĐẠT | LỆCH | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | N/A | ĐẠT | ĐẠT | ĐẠT |
| T6 | ĐẠT | ĐẠT | N/A | ĐẠT | ĐẠT | LỆCH | ĐẠT | ĐẠT | LỆCH | LỆCH | LỆCH | ĐẠT | ĐẠT | N/A | ĐẠT |
| T7 | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | LỆCH | ĐẠT | LỆCH | N/A | N/A | ĐẠT | ĐẠT |
| T8 | N/A | N/A | N/A | ĐẠT | ĐẠT | ĐẠT | ĐẠT | N/A | N/A | N/A | N/A | N/A | N/A | N/A | ĐẠT |
| R1 | ĐẠT | ĐẠT | N/A | ĐẠT | ĐẠT | LỆCH | N/A | N/A | ĐẠT | ĐẠT | ĐẠT | N/A | ĐẠT | LỆCH | ĐẠT |
| R2 | ĐẠT | ĐẠT | ĐẠT | N/A | ĐẠT | ĐẠT | N/A | N/A | N/A | ĐẠT | ĐẠT | ĐẠT | N/A | N/A | ĐẠT |
| R3 | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | LỆCH | ĐẠT | LỆCH | N/A | N/A | ĐẠT | ĐẠT |
| R4 | LỆCH | LỆCH | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | LỆCH | ĐẠT | N/A | N/A | ĐẠT | ĐẠT |
| R5 | N/A | CÂM | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | ĐẠT | N/A | N/A | ĐẠT | ĐẠT |


## Chi tiết ô LỆCH (35)

### T4 × reading-flow
- **đang là**: SurfaceCardAccordion (2 accordion nested cho criteria + code) dựng qua AccordionFrame/AccordionFrameSkeleton trong SurfaceCard.tsx. Trigger của mỗi row là react-aria `Button` thật (`accordion.js:136`, AccordionTrigger = <Button>) — tức một `<button>` thật, có UA default `text-align:center`. Code đã tự đè bằng `<StackV gap="flush" className="min-w-0 flex-1 text-left">` ở CẢ hai nơi: dòng 1653 (AccordionFrame, bản thật) và dòng 1723 (AccordionFrameSkeleton, bản shimmer) — dùng lớp VẬT LÝ `text-left`.
- **đúng phải là**: Cây quyết định §2 câu 5: nội dung nằm trong <button> pressable toàn khối → PHẢI tự set text-start (không phải text-left) đè UA mặc định — đúng theo Vạch cấm #2 (context.md §6 dòng 88) và BẪY 2 (§4 dòng 142-146, đã có neo `SurfaceCard.tsx` trong danh sách 8 chỗ vật lý, chỉ khác số dòng đã trôi do file lớn thêm — cùng SITE, không phải phát hiện mới, canon đã ghi nhận nhưng chưa gate).
- **sửa ở tầng**: composite (SurfaceCard.tsx — hàm nội bộ `AccordionFrame`/`AccordionFrameSkeleton`, dùng chung bởi mọi `SurfaceCardAccordion` trong hệ thống, không riêng T4)
- **chỗ khác giống**: Đổi `text-left` → `text-start` tại 2 dòng đó trong SurfaceCard.tsx. Không đổi hiển thị (LTR render giống hệt), chỉ đổi vốn từ vật lý→logic để nhất quán với SettingsSidebarNav.tsx:194/ChipButtonList.tsx:77-78 (đã dùng logic đúng) và để tự lật khi RTL — sửa 1 chỗ tại composite sẽ fix luôn mọi vùng khác dùng SurfaceCardAccordion (kể cả R4 bên dưới).

### T5 × reading-flow
- **đang là**: ContentRelatedList dùng SurfaceCardList → mỗi row qua `ListFreeRow` (SurfaceCard.tsx dòng ~1451): `itemClassName` dùng chung `"relative block w-full p-3 text-left"` cho cả 3 nhánh render (RowAnchor <a>, <button>, <div>) — ở đây row có `href` nên thực tế render `<a>` (RowAnchor), nhưng class `text-left` (vật lý) vẫn bị set thay vì `text-start` (logic).
- **đúng phải là**: Vạch cấm #2 (context.md §6): dùng text-left (vật lý) thay vì text-start (logic) — dù nhánh `<a>` không có UA-default center nên hậu quả thị giác = 0, đây vẫn là site vật lý đã canon liệt kê (7 chỗ SurfaceCard.tsx, số dòng đã trôi so với bản canon cũ 1427→1451).
- **sửa ở tầng**: composite (SurfaceCard.tsx — hàm nội bộ `ListFreeRow`, dùng chung bởi mọi `SurfaceCardList`, không riêng T5)
- **chỗ khác giống**: Đổi `text-left` → `text-start` tại dòng đó. Cùng 1 sửa tại composite cũng fix cho mọi SurfaceCardList khác đang gọi qua ListFreeRow.

### R4 × reading-flow
- **đang là**: SubmissionFindingsList dựng trên SurfaceCardAccordion (cùng AccordionFrame/AccordionFrameSkeleton như T4) — cùng site `text-left` (vật lý) tại SurfaceCard.tsx dòng 1653/1723 thay vì `text-start` (logic), vì trigger cũng là react-aria `Button` thật.
- **đúng phải là**: Giống hệt T4 — Vạch cấm #2 + BẪY 2, cùng 1 site composite, không phải lỗi riêng của R4/SubmissionFindingsList.
- **sửa ở tầng**: composite (SurfaceCard.tsx — `AccordionFrame`/`AccordionFrameSkeleton`, SAME site đã nêu ở T4 — sửa 1 lần dùng chung)
- **chỗ khác giống**: Không sửa lặp lại — cùng 1 chỗ sửa với T4 sẽ tự fix R4 vì cả hai đều gọi qua SurfaceCardAccordion.

### D4 × prominence
- **đang là**: CardBody dùng chung cho `.Item` (ContinueCard.tsx dòng 153-155) luôn render subtitle qua ĐÚNG MỘT nhánh `<Typography size="xs" color="muted" truncate text={subtitle} />`, bất kể subtitleState. PersonalProjectDashboard.tsx (Body, dòng 287) đã làm phẳng `subtitleState` thành chuỗi chữ qua bảng `TASK_SUBTITLE` (dòng 185-190: active/done/locked/todo → 4 câu chữ khác nhau) TRƯỚC khi đưa vào `ContinueCardItem` — atom nhận một `string` trần, không còn biết đây là state nào, nên mọi state đều lãnh cùng một cơ chế `muted`, không escalate.
- **đúng phải là**: Cây §2 câu 3: chữ mang tín hiệu tương tác/thương hiệu (active, pinned, verified...) mà không cần đóng khung → `accent`. Dòng subtitle "Nhiệm vụ tiếp theo" ở state `active` — literally CÙNG câu chữ với subtitle của Hero (NEXT_TASK_SUBTITLE) — mang đúng tín hiệu "đây là mục đang làm/mục hiện tại", giống hệt loại tín hiệu active/pinned được neo tại `SurfaceCard.tsx:1372`, nên phải lên `accent`; còn done/locked/todo mới đúng là dữ kiện trung tính → `muted`. Bẫy §4 mục 4 cũng chạm tới: cùng một info-type (trạng thái nhiệm vụ trong list) phải escalate bằng TONE của MỘT cơ chế xuyên state — ở đây còn tệ hơn ví dụ nêu trong bẫy (không hề escalate, mọi state phẳng lì cùng một cơ chế, mất khả năng phân biệt việc active với việc locked/todo/done bằng mắt).
- **sửa ở tầng**: block — 2 điểm: (1) ContinueCard.tsx's CardBody phải nhận một tín hiệu tone cho subtitle thay vì hardcode color="muted"; (2) PersonalProjectDashboard.tsx's Body không được flatten subtitleState thành chuỗi chữ trước khi gọi ContinueCardItem — phải truyền đủ để CardBody tự quyết màu (giống cách timeLeft/urgent đã làm ở dòng 58-61, 140-151 của cùng file).
- **chỗ khác giống**: Đây là bẫy dạng escalate-theo-state (bẫy #4), không phải cặp trái-phải trong 1 hàng (bẫy #3) — 4 tile trong Grid là các state SONG SONG của cùng info-type, không phải label/value của cùng 1 dòng.

### R4 × prominence
- **đang là**: findingIcon() (SubmissionFindingsList.tsx dòng 184-195) tô severity bằng `className={cn("size-3.5 shrink-0", toneClassName)}` viết tay trực tiếp lên icon Phosphor, với toneClassName ∈ {text-danger, text-warning, text-muted} lấy từ bảng cục bộ SEVERITY_VISUAL (dòng 98-102) — hoàn toàn ngoài 3 atom Typography/Chip/Button. Icon này đứng riêng ở `titleStart`, KHÔNG có Chip bọc — trong khi cùng hàng trigger, `titleEnd` (vị trí đối xứng) lại dùng ĐÚNG `<Chip tone="default" text={finding.location}>` (findingLocationChip, dòng 198-206).
- **đúng phải là**: Cây §2 câu 2: severity (high/medium/low) là enum/status CỐ ĐỊNH → phải là `chip`; §1 cho phép chip nhận icon TRẠNG THÁI cơ bản (Warning/Info thuộc nhóm này), nên icon severity đủ điều kiện đi vào một Chip thay vì icon trần tô màu tay. §6 vạch cấm #6 cấm đúng việc này ('viết class màu tay ngoài 3 atom để giả một trong bốn cơ chế'). Case đối chứng trong CÙNG codebase: `ChallengeDeliverableList.tsx` dòng 37-43 giải quyết NHU CẦU GIỐNG HỆT (status icon riding titleStart, tone độc lập với title) bằng primitive đã được thầy chốt (`markIcon`/`MarkTone`/`ListMark`, export tại `SurfaceCard.tsx:1831-1849`) — SubmissionFindingsList không gọi primitive này, tự chế lại bằng class tay cục bộ.
- **sửa ở tầng**: block — SubmissionFindingsList.tsx's findingIcon/SEVERITY_VISUAL nên đi qua Chip (hoặc mở rộng markIcon/MarkTone nếu 3 mức severity cần icon khác bộ check/cross/pending hiện có) thay vì className tay.
- **chỗ khác giống**: MapPinIcon/LightbulbIcon trong findingPanel (dòng 227, 247) cũng tô text-muted tay, NHƯNG đây là icon bám màu của Typography muted đứng NGAY CẠNH trong cùng StackH (đúng bẫy §4.5 'leadingIcon bám label của ngữ cảnh đứng cạnh'), nên KHÔNG tính là lệch — chỉ severity trigger icon mới lệch vì nó đứng song song với một Chip THẬT ở titleEnd cùng hàng, phơi ra sự bất đối xứng cơ chế.

### D2 × frame
- **đang là**: `ContinueCardHero` (dùng bởi D2, file thật `blocks/learn/ContinueCard/ContinueCard.tsx` dòng 181-208) truyền `contentClassName={cn("relative flex flex-col gap-3 overflow-hidden", className)}` thẳng vào `SurfaceCard`. `contentClassName` là prop "Extra classes on the surface (content) wrapper" của `SurfaceCard` (SurfaceCard.tsx dòng 283-286) — tức chuỗi này đáp thẳng lên className thật của node DOM mà SurfaceCard render (SurfaceCard.tsx dòng 371/398/443). Bên trong, `CardBody` trả về MỘT fragment 3 phần tử khác kiểu (StackH-row tiêu đề/meta, ProgressMeter-hoặc-skeleton, div CTA) — chính 3 phần tử này đang được xếp dọc bằng `flex flex-col gap-3` viết tay, không qua khung nào trong 5 khung của thang.
- **đúng phải là**: Cây quyết định §2: 3 phần tử arbitrary/khác kiểu, thứ tự mang nghĩa (tiêu đề→progress→CTA) → dừng ở câu 5 → phải là `StackV`. Vạch cấm #1 cấm đúng chính mẫu `flex`+`gap-*` trong className ở tầng block (`ContinueCard.tsx` nằm trong `blocks/learn/`, tier="block" theo `check-seams.mjs`, nằm trong `HAND_ROLL_RULED`). `gap-3` (0.75rem) đúng bằng bậc `grouped` (bậc 3) trong `SeamScale` — tức nếu chuyển đúng sẽ là `StackV gap="grouped"`, không phải số tay. Gate `check-seams.mjs` KHÔNG bắt được vì regex chỉ khớp `className=` (dòng 120: `/className=(?:"([^"]*)"|\{cn\(\s*"([^"]*)")/`), không khớp `contentClassName=` — đúng loại lỗ hổng gate mà chính context.md mục 6 dòng 74 đã ghi "CHƯA — gate cần viết" (dù đó nói về bẫy `@container`+padding, cùng bản chất: gate mù trước tên-prop-khác-mang-className).
- **sửa ở tầng**: block
- **chỗ khác giống**: Bỏ `flex flex-col gap-3` khỏi `contentClassName`; bọc phần trả về của `CardBody` (dùng bởi cả `.Hero` lẫn `.Item`, cùng lỗi) trong `<StackV gap="grouped">`; giữ lại `relative overflow-hidden` như class thuần định vị (không phải bố cục) trên `contentClassName` nếu vẫn cần cho light-streak tuyệt đối của `.Hero`.

### D1 × naming
- **đang là**: PersonalProjectDashboard.tsx dòng 64-71: `export interface PersonalProjectDashboardCrumb { key: string; label: string; onPress?: () => void }`, dùng làm phần tử của `breadcrumbItems?: Array<PersonalProjectDashboardCrumb>` (dòng 129) — không mang hậu tố `Item` dù đúng là PHẦN TỬ CỦA MẢNG.
- **đúng phải là**: Cây quyết định §2 câu 6 (naming/context.md): 'Là phần tử của mảng items? ⇒ XItem.' Neo thật CÙNG feature (§5 đã kiểm trước khi kết luận): PersonalProjectTaskPage.tsx — file dựng cùng lượt PersonalProjectWorkspace — đặt ĐÚNG hậu tố cho ca y hệt: `PersonalProjectTaskLegacyCriterionItem` (dòng 119, dùng cho `Array<PersonalProjectTaskLegacyCriterionItem>`) và `PersonalProjectTaskLegacyCodeImplementationItem` (dòng 131). Nên đổi `PersonalProjectDashboardCrumb` → `PersonalProjectDashboardCrumbItem`.
- **sửa ở tầng**: block
- **chỗ khác giống**: Cùng một lỗi lặp lại ở type `PersonalProjectDashboardTask` (vùng D4, cùng file) — nên đổi tên đồng bộ cả hai type trong một lượt sửa, đừng sửa riêng lẻ.

### D4 × naming
- **đang là**: PersonalProjectDashboard.tsx dòng 98-107: `export interface PersonalProjectDashboardTask { id: string; sortIndex: number; title: string; subtitleState: PersonalProjectTaskSubtitleState }`, dùng làm phần tử của `tasks: Array<PersonalProjectDashboardTask>` (dòng 143) — không mang hậu tố `Item` dù đúng là phần tử mảng hiển thị trong KeepGoingGrid.
- **đúng phải là**: Cùng phép phân định như D1: cây §2 câu 6 (naming/context.md) — phần tử mảng ⇒ hậu tố `Item`. Neo thật cùng feature: `PersonalProjectTaskLegacyCriterionItem`/`PersonalProjectTaskLegacyCodeImplementationItem` trong PersonalProjectTaskPage.tsx làm đúng. Nên đổi thành `PersonalProjectDashboardTaskItem` (hoặc tên phản ánh vai trò tile, ví dụ `PersonalProjectKeepGoingTaskItem`).
- **sửa ở tầng**: block
- **chỗ khác giống**: Sửa cùng lúc với D1 (`PersonalProjectDashboardCrumb`) — cùng file, cùng loại lỗi, tránh vỡ đồng bộ giữa hai type khi chỉ sửa một.

### D1 × seam
- **đang là**: PersonalProjectDashboard.tsx dòng 330: `<StackV gap="section">` bọc [`PageHeader`, `AsyncContent`] — seam header↔nội dung = `section` (gap-6, 24px).
- **đúng phải là**: Phải là `gap="page"` (gap-8, 32px). Neo thật CHÍNH component: `src/.../PersonalProjectDashboard/index.tsx` dòng 178-181 dùng `gap-10` (40px) cho đúng seam này, tự comment "page-heading debt" — nghĩa là 40 SAI (ngoài thang 6 bậc, §1) nhưng KHÔNG SAI HƯỚNG: đây là mẫu "header-cluster tách khỏi nội dung" đã có tiền lệ chuẩn hoá NGAY TRONG CÙNG hệ thống — `CourseContents.tsx` dòng 130-138 comment rõ "`gap-10` → `gap="page"`. `10` KHÔNG nằm trên thang" cho đúng quan hệ header↔content này. 32 gần 40 hơn 24 (lệch 8 so với lệch 16) và đã có neo Storybook thật dùng đúng phép quy đổi đó. §2 cây quyết định câu 5 cũng xác nhận: header-cluster và toàn bộ thân trang là hai khối chức năng riêng, không phải hai vùng của một khối.
- **sửa ở tầng**: block
- **chỗ khác giống**: Cùng lỗi lặp lại ở R1 (PersonalProjectResultScreen) — real `TaskResult/index.tsx` dòng 246 cũng dùng `gap-10` cho đúng seam header↔content, comment thẳng "PageHeader (header) → content cluster, gap-10". Nên sửa D1 và R1 cùng lúc, cùng lý do.

### T1 × seam
- **đang là**: PersonalProjectTaskPage.tsx dòng 322: `<StackV gap="page">` bọc TOÀN BỘ readingColumn — [`PageHeader`, `FeedbackCallout`?, `SurfaceCard`(brief)?, `SurfaceCard`(legacy)?, `ContentRelatedList`] — mọi seam trong cột đọc = `page` (gap-8, 32px).
- **đúng phải là**: Phải là `gap="section"` (gap-6, 24px). Neo thật: `src/components/features/learn/PersonalProject/index.tsx` dòng 78 — `Task` component bọc [title+desc-block, `TaskLockedAlert`, `TaskBrief`, `LabeledCard`(legacy), `RelatedContentList`] bằng ĐÚNG MỘT `gap-6` (24px), KHÔNG có tầng gap-10/page riêng cho title+desc — khác hẳn D1/R1 vì trong nhánh Task thật, title+desc chỉ là ITEM ĐẦU của nhịp gap-6, không được nâng thành tier riêng (breadcrumb mới tách riêng ở ngoài, tại `PersonalProjectWorkspace/index.tsx` dòng 66 với `gap-3`, đã khớp đúng `gap="grouped"` PageHeader tự áp nội bộ). Đây không phải suy chéo (bẫy §4.5) — đọc trực tiếp src của CHÍNH branch Task, khác branch Dashboard/Result nên khác giá trị là đúng, không phải nhầm lẫn.
- **sửa ở tầng**: screen
- **chỗ khác giống**: Sửa 1 chỗ (dòng 322) tự động sửa luôn T2/T3/T4/T5 vì cùng một StackV.

### T2 × seam
- **đang là**: FeedbackCallout nằm trực tiếp trong StackV gap="page" của readingColumn (cùng dòng 322 nêu ở T1).
- **đúng phải là**: Seam trước/sau FeedbackCallout phải là `section` (gap-6), theo đúng root cause đã nêu ở T1 — real `TaskLockedAlert` cũng nằm trong nhịp `gap-6` đồng nhất của `Task/index.tsx` dòng 78.
- **sửa ở tầng**: screen
- **chỗ khác giống**: Cùng gốc với T1 — một lần sửa dòng 322 xử lý cả T2.

### T3 × seam
- **đang là**: SurfaceCard "Hướng dẫn" cũng là con trực tiếp của StackV gap="page" (dòng 322).
- **đúng phải là**: Phải là `section`, cùng root cause T1 — real `TaskBrief` cũng nằm trong nhịp `gap-6` đồng nhất.
- **sửa ở tầng**: screen
- **chỗ khác giống**: Cùng gốc T1.

### T4 × seam
- **đang là**: SurfaceCard "Tiêu chí đánh giá (bản cũ)" là con của StackV gap="page" (dòng 322); BÊN TRONG nó có thêm `<StackV gap="section">` (dòng 365) bọc 2 `SurfaceCardAccordion`.
- **đúng phải là**: Seam NGOÀI (trước/sau card legacy so với anh em) phải là `section`, cùng root cause T1. Seam TRONG (giữa 2 accordion) đối chiếu real `LabeledCard contentClassName="flex flex-col gap-4"` (Task/index.tsx dòng 93, 16px) — 16px KHÔNG nằm trên thang 6 bậc và không có comment tự nhận debt như trường hợp gap-10, nên KHÔNG đủ cơ sở quy đổi dứt khoát về `grouped`(12) hay `section`(24) chỉ từ con số. Phần TRONG này cần thêm neo (tiền lệ khác trong hệ thống dùng gap-4 hay B4 hỏi thầy) trước khi chốt — không đoán liều.
- **sửa ở tầng**: screen
- **chỗ khác giống**: Seam ngoài sửa cùng T1. Seam trong (2 accordion) nên tách thành một câu hỏi CAM riêng nếu cần chốt dứt khoát.

### T5 × seam
- **đang là**: ContentRelatedList là con cuối của StackV gap="page" (dòng 322).
- **đúng phải là**: Phải là `section`, cùng root cause T1 — real `RelatedContentList` cũng ở trong nhịp `gap-6` đồng nhất, dòng 78-109 `Task/index.tsx`.
- **sửa ở tầng**: screen
- **chỗ khác giống**: Cùng gốc T1.

### T6 × seam
- **đang là**: PersonalProjectTaskPage.tsx dòng 416: một `<StackV gap="section">` DUY NHẤT (24px, flat) bọc cả ba: `InputText`(URL) → `ListRow`(cài đặt) → `StackH`(3 nút) — nhịp seam đều 24/24, không phân biệt nhóm.
- **đúng phải là**: Real src `TaskSubmissionPanel/index.tsx` dòng 56-84 có HAI TẦNG rõ: `LabeledCard contentClassName="flex flex-col gap-6"` bọc [nhóm-cấu-hình, `TaskActions`] ở `section`(24) — NHƯNG bên trong nhóm-cấu-hình, `PersonalProjectSubmission` (URL) và nút mở settings lại chỉ cách nhau `gap-3` (12px = `grouped`, dòng 63), tự comment "URL + settings row are one config GROUP". Đây đúng bẫy §4.6 (nhịp gần-đều = dấu hiệu sai) — nhịp phải kể ra được NHÓM (12/24) chứ không phẳng (24/24). Cần lồng: `StackV gap="grouped"` ôm [InputText, ListRow], rồi `StackV gap="section"` ôm [nhóm đó, StackH(3 nút)].
- **sửa ở tầng**: screen

### R1 × seam
- **đang là**: PersonalProjectResultScreen.tsx dòng 287: `<StackV gap="section">` bọc PHẲNG cả [`SubmissionResultHeader`, `SubmissionAttemptSelector`, cụm-nội-dung-có-điều-kiện] ở cùng một mức `section` (24px) — seam header↔phần còn lại cũng chỉ 24px.
- **đúng phải là**: Phải là `page` (32px) cho riêng seam header↔phần-còn-lại. Neo thật CHÍNH component: `src/.../TaskResult/index.tsx` dòng 244-246, comment thẳng "PageHeader (header) → content cluster, gap-10" — cùng mẫu debt gap-10 như D1, quy đổi theo đúng tiền lệ đã chuẩn hoá ở `CourseContents.tsx` (gap-10 → `page`). Từ `SubmissionAttemptSelector` trở đi (R2→R3→R4→R5) real src dùng `gap-6` đồng nhất (dòng 246 div nội dung, và dòng 313 nhóm selectedAttempt) nên GIỮ NGUYÊN `section` cho các seam đó là đúng — chỉ seam đầu tiên (ngay sau header) cần nâng lên `page`.
- **sửa ở tầng**: block
- **chỗ khác giống**: Cùng gốc với D1 — sửa cùng lúc, cùng lý do (mẫu gap-10 "page-heading debt" lặp lại y hệt ở cả hai màn Dashboard và Result của cùng PersonalProjectWorkspace).

### D4 × surface
- **đang là**: SurfaceCard label={keepGoingLabel(...)} (PersonalProjectDashboard.tsx:272-296) không truyền variant ⇒ mặc định "surface" (bg-surface shadow-surface đầy đủ), bọc quanh <Grid> chứa các ContinueCardItem — mỗi ContinueCardItem (ContinueCard.tsx:217-224) tự dựng SurfaceCard riêng, CŨNG hardcode mặc định variant="surface" (không hề expose prop variant ra ngoài). Kết quả: một card có bóng bọc quanh N card con cũng có bóng riêng, mỗi card con chỉ chiếm một ô nhỏ (2 cột) trong thân card cha.
- **đúng phải là**: Cây §2 câu 1→câu 2: card con CÓ mặt cha bọc trực tiếp (SurfaceCard keepGoingGrid), và vì N tile chỉ chiếm một phần nhỏ mỗi ô (không "gần trọn thân") nên về lý thuyết phải đi variant="nested" cho card con — NHƯNG neo thật ưu tiên #1 (§5) cho câu trả lời dứt khoát hơn: `src/.../PersonalProjectDashboard/index.tsx:245-249` bọc đúng các tile này bằng `<LabeledCard label=... frameless contentClassName="grid gap-3 @app-sm:grid-cols-2">` — khung NGOÀI hoàn toàn KHÔNG vẽ mặt (frameless), chỉ còn label; mỗi `ContinueCard variant="item"` dựng trên `SectionCard` (khung viền riêng, không dựa vào cha). Đây đúng là ca cây §2 câu 2 mô tả: khung lồng chiếm gần trọn thân cha ⇒ khung cha THỪA, phải bỏ (frameless), để card con tự đứng khung riêng — hiện code đang làm NGƯỢC (cả hai lớp đều tự vẽ mặt), là biến thể N-tile của bẫy 'card ma' §4.1.
- **sửa ở tầng**: block
- **chỗ khác giống**: Cần sửa ở CẢ hai chỗ: (1) PersonalProjectDashboard.tsx's Body — bỏ mặt/bóng của SurfaceCard bọc ngoài KeepGoingGrid, chỉ giữ label (label-only, không khung); (2) ContinueCard.tsx's `.Item` — hiện chưa có đường nào để đổi variant vì SurfaceCard bên trong nó hardcode mặc định. Ghi chú thêm: bản Storybook SurfaceCard.* CHƯA có chế độ "frameless" (đã grep `frameless` trong SurfaceCard.tsx — 0 hit), trong khi src thật có `LabeledCard frameless` — đây là một GAP năng lực thật sự giữa hai hệ thống, nên nêu lại ở bước feedback-end để cân nhắc bổ sung capability này cho SurfaceCard.Base/.List thay vì chỉ patch tại chỗ.

### T4 × surface
- **đang là**: SurfaceCard label="Tiêu chí đánh giá (bản cũ)" isSkeleton (readingColumn, PersonalProjectTaskPage.tsx:363-388) không truyền variant ⇒ mặc định "surface" (khung ngoài đầy đủ bg-surface shadow-surface), bọc một StackV gap="section" chứa ĐÚNG HAI SurfaceCardAccordion variant="nested" (tiêu chí + code) — không còn nội dung nào khác trong card ngoài.
- **đúng phải là**: Cây §2 câu 2: hai accordion lồng chiếm gần trọn thân card cha (không có nội dung nào khác cạnh chúng) ⇒ khung cha là THỪA, phải bỏ (frameless/label-only), và khi đó hai accordion phải là variant="surface" (khung riêng của chính chúng) chứ không phải "nested" (vì lúc này chúng không còn mặt cha tô màu nào bọc trực tiếp nữa). Neo thật §5 ưu tiên #1 xác nhận dứt khoát: `src/.../PersonalProject/index.tsx:92-96` bọc ĐÚNG hai component tương đương (`TaskCriteriaList` + `TaskCodeImplementations`) bằng `<LabeledCard label={t("task.criteriaTitle")} frameless contentClassName="flex flex-col gap-4">`, với comment tường minh: "One LabeledCard for both (frameless — criteria accordion + the implementation guide already frame themselves)". Code Storybook hiện tại làm NGƯỢC hoàn toàn hướng đã chốt ở src: vừa giữ khung ngoài (đáng lẽ phải bỏ) vừa hạ hai accordion xuống "nested" (đáng lẽ phải là "surface" tự đứng).
- **sửa ở tầng**: page
- **chỗ khác giống**: Bỏ khung/mặt của SurfaceCard bọc ngoài (chỉ giữ label "Tiêu chí đánh giá (bản cũ)" dạng heading, không sơn bg-surface/shadow-surface — cùng GAP "chưa có chế độ frameless" đã nêu ở D4, cần capability mới hoặc dùng Typography label trần thay SurfaceCard tại đây); đổi CẢ HAI SurfaceCardAccordion từ variant="nested" sang mặc định (bỏ prop variant, tức "surface") để mỗi accordion tự đứng khung riêng — khớp đúng "criteria accordion + code guide đã tự khung hoá" mà src đã ghi chú.

### T4 × text
- **đang là**: Trong `readingColumn`, nhánh `codeItems` của accordion "Tiêu chí đánh giá (bản cũ)": `Typography size="xs" weight="medium" color="muted" text="Hướng dẫn"` (PersonalProjectTaskPage.tsx:307) và cùng công thức cho `text="Ví dụ"` (dòng 311).
- **đúng phải là**: Ground truth thật của đúng 2 nhãn phân biệt "Hướng dẫn"/"Ví dụ" là component `ImplementationCard` mà `src`'s `TaskCodeImplementations` gọi thẳng (`src/components/features/learn/LessonReader/CodeImplementationBody/ImplementationCard/index.tsx:28,34`): `<h3 className="text-sm font-semibold mb-2">` — nghĩa là size phải là `sm` (14px, KHÔNG PHẢI `xs` 12px) và màu phải là MẶC ĐỊNH/foreground (KHÔNG có class muted nào, KHÔNG PHẢI `color="muted"`). Weight `semibold` ở scale body FOLD render y hệt `medium` theo §3c của canon nên giữ `weight="medium"` không sai — chỉ size và color lệch. Theo §5 NEO THẬT ưu tiên 1 (bảng vai trò → type HeroUI thật từ src, ưu tiên cao nhất khi porting 1 dòng chữ cụ thể), src thắng cây quyết định trừu tượng.
- **sửa ở tầng**: page — inline JSX ngay tại private helper `readingColumn` của `PersonalProjectTaskPage.tsx` (dòng 307 và 311), không phải lỗi ở atom/composite.
- **chỗ khác giống**: Có thể tách 2 dòng này thành 1 helper dùng chung (đang lặp y hệt cho mỗi `codeItems.map`) khi có nhu cầu DRY, nhưng đó là vấn đề khác, không thuộc phạm vi trục text.

### T6 × text
- **đang là**: `ListRow` "Cài đặt chấm điểm" truyền `meta={`${panel.settingsLangLabel} · ${panel.settingsBranch}`}` — một CHUỖI TRẦN, KHÔNG bọc `Typography` nào (PersonalProjectTaskPage.tsx:431, trong private helper `submissionPanel`).
- **đúng phải là**: `ListRow`'s `content` render `{meta}` trực tiếp (List.tsx:189-194), KHÔNG tự bọc Typography cho caller — nghĩa là chuỗi trần sẽ không mang size/weight/color nào của hệ thống (khác hẳn `title`/`subtitle` vốn đi qua `TitledText`). Ground truth thật `src/components/features/learn/PersonalProject/TaskSubmissionPanel/index.tsx:79`: `<span className="... text-xs text-muted">` — đúng Tier D (`xs`, regular, muted). Mọi call-site khác của `ListRow`/`meta` trong `.storybook` (ngoài `_legacy`) đều bọc sẵn node có kiểu (Chip, DifficultyChip, hoặc `<Typography type="body-xs" color="muted">` — vd `SurfaceCardNested.stories.tsx:207`, `ListRow.stories.tsx:182,382,449`) — đây là call-site DUY NHẤT trong code thật (không phải story) phá quy ước đó. Phải sửa thành `meta={<Typography size="xs" color="muted" truncate text={`${panel.settingsLangLabel} · ${panel.settingsBranch}`} />}`.
- **sửa ở tầng**: page — call site tại private helper `submissionPanel` của `PersonalProjectTaskPage.tsx:431`.
- **chỗ khác giống**: Có thể sửa ở tầng composite (`ListRow` tự bọc string thô bằng Typography xs+muted mặc định khi `meta` là string, tương tự cách `TitledText` tự lo phần subtitle) để chặn lỗi này tái diễn ở call site tương lai — nhưng đổi hợp đồng hiện tại của `meta` (đang nhận ReactNode tuỳ ý gồm Chip), nên sửa tại call site an toàn hơn cho vòng này.

### T7 × text
- **đang là**: `SubmissionScoreCard.tsx`: điểm hero = `Typography size="h2" weight="bold"` (dòng 144-152); mẫu số `/ {maxScore}` = `Typography size="sm" color="muted"` (dòng 154-162); dòng "cần thêm N điểm" = `Typography size="xs" color="muted"` (dòng 173-179).
- **đúng phải là**: File header của chính block này (dòng 20-22) tự nhận đang "mirroring the real SubmissionResult component" — ground truth `src/components/features/learn/Challenge/SubmissionResult/index.tsx:312-330`: điểm hero = `text-4xl font-bold` (36px ⇒ mức `h1` của atom, KHÔNG PHẢI `h2` 30px); mẫu số `/{maxScore}` = `text-base text-muted` (16px ⇒ `base`, KHÔNG PHẢI `sm` 14px); dòng "cần thêm N điểm" = `Typography type="body-sm" color="muted"` (14px ⇒ `sm`, KHÔNG PHẢI `xs` 12px). Cả 3 chỗ đều lùi đúng MỘT bậc size so với ground truth — hiện tượng có HỆ THỐNG trong toàn khối (mọi con số đều nhỏ hơn thật 1 bậc), không phải 1 điểm chọn nhầm rời rạc. (`shortFeedback` size="sm" và `timeAgo` size="xs" trong cùng file thì KHỚP đúng src, không nằm trong lỗi này.)
- **sửa ở tầng**: block — `SubmissionScoreCard.tsx` (`.storybook/components/starci/blocks/learn/SubmissionScoreCard/SubmissionScoreCard.tsx`), component dùng chung nên sửa 1 chỗ khớp cả T7 (Task panel) và R3 (Result screen).
- **chỗ khác giống**: Không có phương án khác — chỉ cần nâng đúng 3 prop `size` (h2→h1, sm→base, xs→sm) tại 3 vị trí nêu trên.

### R3 × text
- **đang là**: PersonalProjectResultScreen.tsx gọi thẳng `SubmissionScoreCard` (dòng 310-324) — CÙNG một file/cùng 3 vị trí đã nêu ở T7: điểm hero `size="h2" weight="bold"`, mẫu số `/maxScore` `size="sm"`, dòng "cần thêm N điểm" `size="xs"`.
- **đúng phải là**: Giống hệt T7 — ground truth `SubmissionResult/index.tsx:312-330` yêu cầu `h1`/`base`/`sm` tương ứng, không phải `h2`/`sm`/`xs`. Đây KHÔNG phải lỗi riêng của route Result — lỗi nằm trong bản thân component `SubmissionScoreCard`, ảnh hưởng đồng thời cả T7 (Task panel) và R3 (Result screen) vì cả hai gọi CHUNG một block, không phải hai bản sao khác nhau.
- **sửa ở tầng**: block — `SubmissionScoreCard.tsx`, sửa 1 lần tại nguồn sẽ tự khớp cả T7 lẫn R3.
- **chỗ khác giống**: Không cần sửa gì riêng ở PersonalProjectResultScreen.tsx — đây thuần là hệ quả của lỗi tại T7.

### T6 × icon
- **đang là**: Trong `submissionPanel` (PersonalProjectTaskPage.tsx dòng 427-437), ListRow "Cài đặt chấm điểm": `leading={<GearSixIcon aria-hidden focusable="false" className="size-4" />}` (KHÔNG có `weight`) và `trailing={<CaretRightIcon aria-hidden focusable="false" weight="bold" className="size-4" />}`.
- **đúng phải là**: Title của ListRow đi qua TitledText size="row" mặc định → `titleSize: "sm"` (text-sm) — đã đọc TitledText.tsx dòng 58-65. Vị trí là DIV chứ không phải TEXT: chính RowSkeleton của List.tsx (dòng 122-123) đã tự mirror ô icon dẫn đầu bằng `HeroSkeleton className="size-5 shrink-0 rounded"` — tức chủ sở hữu hình (List.Row) đã tự xác nhận size-5 là kích thước THẬT của ô leading (§5 NEO ưu tiên #2: đo primitive đang sở hữu, không đoán). Theo bảng §1c, DIV + text-sm (line-height) = size-5, và theo §3.2, size-5 ⇒ weight="regular" (không bold). Vậy: GearSixIcon phải là `size-5` + `weight="regular"` (hiện thiếu cả size lẫn weight đúng); CaretRightIcon (caret điều hướng tĩnh, §2a-3′/§4.2) cũng phải tra theo CÙNG hàng DIV/text-sm ⇒ `size-5` + `weight="regular"` (hiện size-4 + bold sai cả hai). size-4 không khớp bất kỳ cột nào (TEXT hay DIV) của text-sm trong bảng §1c nên chắc chắn sai bất kể cách xác định vị trí.
- **sửa ở tầng**: page
- **chỗ khác giống**: Sửa ngay tại 2 dòng JSX trong `submissionPanel` (className="size-4"→"size-5", GearSixIcon thêm weight="regular" hoặc bỏ hẳn nếu Phosphor mặc định regular, CaretRightIcon đổi weight="bold"→"regular"). Về lâu dài, `List.Row`'s `leading`/`trailing` đang là ReactNode thô (không có atom nào ép size như Chip/Button) — cân nhắc nâng cấp List.Row nhận IconComponent + tự ép size/weight để callsite không tự đoán sai nữa (không thuộc scope sửa của phiên này).

### R4 × icon
- **đang là**: SubmissionFindingsList.tsx, hàm `findingIcon()` (dòng 184-195) — icon mức độ nghiêm trọng (`WarningCircleIcon`/`InfoIcon`) được set cứng `className="size-3.5 shrink-0 ..."` + `weight="bold"`, gắn vào `titleStart` của SurfaceCardAccordionItem — tức nằm NGAY TRONG cùng `Accordion.Trigger` với caret mở/đóng của chính hàng đó.
- **đúng phải là**: SurfaceCard.tsx (dòng 1674-1677, comment tại chỗ) đã tự xác lập: `Accordion.Trigger` có `px-4 py-4` CỐ ĐỊNH (không hug-content) ⇒ vị trí DIV, và title cùng hàng là `text-sm` (Typography size="sm" dòng 1657) ⇒ tra line-height ⇒ `size-5` + weight mặc định "regular" (đã áp đúng cho caret `Accordion.Indicator className="size-5"` không kèm weight). `titleStart` là icon khác nằm CÙNG một Ô/control đó nên phải theo ĐÚNG công thức DIV+text-sm=size-5/regular như caret bên cạnh nó — hiện tại nó lại dùng công thức TEXT (size-3.5/bold), lệch cả size lẫn weight, và gây hai icon cùng hàng khác cỡ/nét dù cùng một Ô cố định.
- **sửa ở tầng**: block
- **chỗ khác giống**: Sửa trong `findingIcon()` (SubmissionFindingsList.tsx): `className="size-3.5 shrink-0"` → `"size-5 shrink-0"`, bỏ `weight="bold"` (để mặc định regular, theo §3.2 vì size-5 ≥ size-5). Không đụng icon MapPinIcon/LightbulbIcon trong `findingPanel()` — hai icon đó nằm trong `Accordion.Body` (vùng nội dung mở ra, không phải trigger cố định), cạnh Typography/MarkdownContent chạy chữ, đúng vị trí TEXT nên size-3/size-3.5 hiện tại không có dấu hiệu sai.

### D4 × color
- **đang là**: TASK_SUBTITLE (PersonalProjectDashboard.tsx dòng 185-190) gán 4 câu chữ theo subtitleState (active/done/locked/todo: 'Nhiệm vụ tiếp theo'/'Đã hoàn thành'/'Đang khoá'/'Chưa bắt đầu'), nhưng cả 4 đều đi qua CÙNG một nhánh render của CardBody (ContinueCard.tsx dòng 153-155): `<Typography size="xs" color="muted" truncate text={subtitle}/>`. Không có icon, không có tone rẽ nhánh — MỘT màu muted duy nhất cho cả 4 trạng thái khác nhau.
- **đúng phải là**: Cây §2 câu 1/1a: 'locked' và 'done' là TRẠNG THÁI THẬT của dữ liệu (đổi task → trạng thái đổi theo, không phải do người dựng thích tô) → phải rẽ warning/success tương ứng, không dừng ở câu 3 (muted). Neo thật CÙNG hệ thống, thậm chí CÙNG phiên PersonalProjectWorkspace: (1) `KeepGoingPath.tsx` dòng 80-99 — component song sinh y hệt shape 'thẻ nhiệm vụ mang trạng thái active/done/todo/locked' — chốt rõ locked=`text-warning-soft-foreground` với lý do tường minh 'KHÔNG phải muted — muted lock sẽ vô hình, đây là SALES DOOR, phải thấy được', done=`text-success-soft-foreground`, active=`text-accent-soft-foreground`. (2) `ContentRelatedList.tsx` dòng 121 (= vùng T5/R5 trong CHÍNH phiên này) cũng chốt locked=`color="warning"`, không phải muted. (3) T2 (chính task page này) tô status="warning" cho banner 'chưa mở khoá' của CÙNG khái niệm 'task locked'. D4 là nơi DUY NHẤT trong toàn phiên biểu diễn 'locked' bằng muted — lệch nội bộ ngay trong cùng một luồng nghiệp vụ.
- **sửa ở tầng**: block
- **chỗ khác giống**: Không viết class tay trên subtitle. Mở khả năng subtitle/leading mang tone theo state trong `ContinueCard.tsx` (CardBody/ContinueCardItem) — ví dụ thêm state→tone table tương tự `KeepGoingPath.CONTENT_LEADING`, để tối thiểu 'locked' lên warning và 'done' lên success, nhất quán với KeepGoingPath/ContentRelatedList/T2 cùng phiên.

### T6 × color
- **đang là**: ListRow 'Cài đặt chấm điểm' (PersonalProjectTaskPage.tsx dòng 428-436, hàm submissionPanel): `meta={`${panel.settingsLangLabel} · ${panel.settingsBranch}`}` là một STRING TRẦN truyền thẳng vào prop `meta`. `Row` trong List.tsx (dòng 189-193) render `{meta}` nguyên văn, không bọc Typography/class màu nào quanh nó — chữ ra màu mặc định/kế thừa (foreground), KHÔNG phải muted.
- **đúng phải là**: Cây §2 câu 3: đây là META — thông tin phụ đính kèm hàng cài đặt (ngôn ngữ chấm + nhánh git), phải muted. Neo thật: MỌI lần khác trong hệ thống truyền `meta` dạng chữ đều bọc tường minh `<Typography color="muted">` — `PageHeader.stories.tsx:93` (`meta={<Typography type="body-xs" color="muted">24 Modules · 87 Lessons · 32 hours</Typography>}`), `ModalShell.stories.tsx:311` (`<Typography type="body-sm" color="muted">1.200.000đ</Typography>`), `SurfaceCardNested.stories.tsx:207,214` (`<Typography type="body-xs" color="muted">2 items</Typography>`). Cùng dạng lỗi cấu trúc với bẫy #6 context.md §4 (round-6 Typography.tsx, neo ChallengePage/Graded 2026-07-30): bỏ qua bọc màu tường minh cho nhánh 'không truyền color' = ambient/vendor CSS thắng thay vì COLOR_CLS['muted'] được áp có chủ đích.
- **sửa ở tầng**: page
- **chỗ khác giống**: Bọc chuỗi meta bằng `<Typography size="xs" color="muted" text={`${panel.settingsLangLabel} · ${panel.settingsBranch}`} />` trong hàm `submissionPanel` (PersonalProjectTaskPage.tsx) trước khi truyền vào `ListRow`, thay vì truyền string trần.

### T7 × color
- **đang là**: `MODEL_CATEGORY_MAP` (SubmissionScoreCard.tsx dòng 63-69) ánh xạ `AiModelCategory` — 5 bậc THỨ TỰ liên tục theo chi phí/chất lượng (free < economy < balanced < premium < frontier) — thẳng vào 5 tone trạng thái của `EnumChip`/`Chip`: free→default, economy→success, balanced→accent, premium→warning, frontier→danger. File tự thừa nhận đây là compromise ('Revisit if a shared tier-ramp primitive lands at this tier later').
- **đúng phải là**: context.md §4 bẫy #2: 'thang có ≥4 bậc THỨ TỰ liên tục (không phải 1 trạng thái nhị/tam phân) thì đó là TIER, không đi qua thang 6 giá trị của trục này' — dấu hiệu nhận biết khớp chính xác 5 bậc của AiModelCategory. Neo thật đối chứng NGAY TRONG canon: `DifficultyChip.tsx`/`DIFFICULTY_COLOR` cố tình KHÔNG dùng success/warning/danger cho 4 bậc độ khó, mà tự xây 1 dải Tailwind riêng (emerald→amber→orange→rose) để tránh vừa đụng ý nghĩa trạng thái vừa tránh trùng màu. `MODEL_CATEGORY_MAP` không làm vậy — nó mượn thẳng success/warning/danger (vốn mang nghĩa 'đã đúng'/'cần chú ý'/'đã lỗi') để gắn cho một tier CHI PHÍ, đọc sai: một model 'economy' không phải là 'đã thành công', một model 'frontier' (đắt nhất, tốt nhất) không phải là 'lỗi'.
- **sửa ở tầng**: block
- **chỗ khác giống**: Theo đúng tiền lệ `DifficultyChip`: xây một dải màu Tailwind riêng cho 5 bậc AiModelCategory (không đi qua tone success/warning/danger/accent/default của EnumChip/Chip) — hand-roll className tại `SubmissionScoreCard.tsx`, hoặc chờ 'tier-ramp primitive' như comment đã tự ghi.

### R3 × color
- **đang là**: SubmissionScoreCard tại Result screen dùng CHUNG `MODEL_CATEGORY_MAP` với T7 (SubmissionScoreCard.tsx dòng 63-69) — free→default, economy→success, balanced→accent, premium→warning, frontier→danger cho EnumChip.
- **đúng phải là**: Cùng lý do đã nêu ở T7: 5 bậc AiModelCategory là TIER liên tục, theo context.md §4 bẫy #2 phải có dải màu riêng (như DifficultyChip), không mượn 5 tone trạng thái success/warning/danger/accent/default — mượn khiến 'economy' đọc nhầm thành 'đã đúng' và 'frontier' đọc nhầm thành 'đã lỗi'.
- **sửa ở tầng**: block
- **chỗ khác giống**: Cùng phương án với T7 — sửa một lần ở `SubmissionScoreCard.tsx` sẽ tự động khớp cả R3 vì hai vùng dùng chung component/bảng màu.

### D1 × markdown
- **đang là**: PersonalProjectDashboard.tsx → PageHeader: `title` (string, page identifier) renders fine via bare `<Typography size="h3" weight="bold" text={title}/>` (Page.tsx Header, no parseInlineCode) — no issue. But `description` (typed `string` on this block's own props, forwarded to PageHeader.description) renders through the SAME frame's bare `<Typography size="sm" color="muted" text={description}/>` (Page.tsx lines ~172-179) with NO `parseInlineCode` and NOT through composite `RichText` — zero markdown capability at all, not even backtick.
- **đúng phải là**: Cây §2: description is not an identifier and needs no block structure ⇒ tier `richtext nhỏ` (bold/italic/link allowed via composite `RichText`). Canon §3c row "description" + bẫy #2 already documents this EXACT gap ("render qua Typography thường, KHÔNG qua composite RichText vì RichText CHƯA có consumer thật") — this vùng is a live instance of that catalogued debt, not a new bug.
- **sửa ở tầng**: frame — `Page.tsx`'s `Header` (exported `PageHeader`), the ONE shared page-chrome frame used by every screen, not specific to this block.
- **chỗ khác giống**: Same root cause resurfaces at T1 (Task PageHeader) and R1 (Result PageHeader) in this same session — all three point at ONE fix location. A real fix means either making `RichText` the description's real renderer (its first true consumer app-wide) or, if plain-by-design is the actual intent, documenting that decision explicitly the way `SubmissionScoreCard.shortFeedback` already does (see T7).

### D2 × markdown
- **đang là**: ContinueCard.tsx CardBody: `title` (`${sortIndex}. ${title}`, identifier) via bare Typography — fine (title tier). `subtitle` (`NEXT_TASK_SUBTITLE = "Nhiệm vụ tiếp theo"`, typed `string` in `ContinueCardDataProps`) renders via bare `<Typography size="xs" color="muted" truncate text={subtitle}/>` (ContinueCard.tsx line 154) — no `parseInlineCode`, no `RichText`.
- **đúng phải là**: Canon §3c row "subtitle" → tier `richtext nhỏ` (đi kèm title, mô tả thêm 1 dòng). §3c notes ContinueCard's subtitle TYPE is already correct (`string`) but does not confirm the render MECHANISM — reading the code shows the same bare-Typography gap as description (bẫy #2's general principle: type-correct ≠ mechanism-correct).
- **sửa ở tầng**: block — `ContinueCard.tsx`'s shared `CardBody` (used by both `.Hero` and `.Item`).
- **chỗ khác giống**: Same file/fix as D4's subtitle finding below (both go through this one CardBody). Today's actual subtitle strings never need bold/link, so risk is low in practice — but per bẫy #5, absence of markdown syntax in current data is not proof the mechanism is safe; the gap is in what the TYPE allows to remain unenforced, not in today's literal content.

### D4 × markdown
- **đang là**: SurfaceCard.label = keepGoingLabel(milestoneLabel) (e.g. "Tiếp tục · Milestone 2") → SurfaceCardHeader's bare Typography (surface-card-header.tsx:80-88, no parseInlineCode) — fine, matches title tier (card-section identifier). ContinueCardItem.subtitle = TASK_SUBTITLE[state] (one of 4 fixed strings) → SAME ContinueCard.tsx CardBody mechanism as D2 — bare Typography, no parseInlineCode/RichText.
- **đúng phải là**: label: correctly title tier, no change needed. subtitle: same as D2 — should be `richtext nhỏ` via RichText per §3c row "subtitle", currently rendered with zero formatting capability.
- **sửa ở tầng**: block — `ContinueCard.tsx`'s `CardBody` (identical file/location as D2's fix — one fix covers both vùng).
- **chỗ khác giống**: D2 and D4 are literally the same component (`ContinueCardHero`/`ContinueCardItem` share `CardBody`) — do not fix twice.

### T1 × markdown
- **đang là**: PersonalProjectTaskPage.tsx readingColumn → PageHeader: `title` wrapped in a raw `<span data-anat-part>{task.title}</span>` before reaching PageHeader (harmless for markdown tier — still ends up as literal text through Typography). `task.description` (string, optional) passed straight through to PageHeader.description → same Page.tsx Header bare-Typography mechanism as D1 (no parseInlineCode, no RichText).
- **đúng phải là**: Identical to D1: description should be `richtext nhỏ` tier via `RichText`, currently zero-formatting.
- **sửa ở tầng**: frame — same `Page.tsx` `Header` as D1/R1 (one shared file).
- **chỗ khác giống**: Third confirmed sighting this session of the exact same PageHeader.description gap (D1 dashboard, T1 task, R1 result) — this now clears the feedback-end skill's own "two independent sources" bar for promoting it from a documented risk (bẫy #2) to a confirmed, cross-screen recurring instance worth a canon update.

### T2 × markdown
- **đang là**: FeedbackCallout(status="warning", title="Xem trước nhiệm vụ chưa mở khoá", description="Bạn cần hoàn thành nhiệm vụ hiện tại trước khi làm được nhiệm vụ này.") forwards both straight to atom `Alert`, which renders them via RAW HeroUI `<HeroAlert.Title>`/`<HeroAlert.Description>` (Alert.tsx ~210-214) — literal text, no `Typography`, no `parseInlineCode`, no `RichText` at all. This is a deeper bypass than D1/T1/R1 (those at least go through the `Typography` atom).
- **đúng phải là**: title: fine at its ceiling (identifier, title tier). description: canon assigns `richtext nhỏ` tier (§2/§3c) — current mechanism provides none. Feedback.tsx's own file header calls this a "DELIBERATE EXCEPTION" (keeps HeroUI's own compound instead of the `Typography` atom) but that note is about INHERITING THE STATUS COLOR CONTRACT, not about deliberately flattening markdown — the two concerns are separate and only one was actually decided.
- **sửa ở tầng**: atom — `Alert.tsx`'s title/description rendering.
- **chỗ khác giống**: Harder fix than D1/T1/R1: swapping in `RichText` here requires first confirming it can inherit HeroUI Alert's built-in color-by-status the way `HeroAlert.Description` does today, or reintroducing a hand-fed color table that was previously removed as drift — a real architecture call, not a 1-line swap. Off-axis note only (not part of this verdict): the domain doc for this vùng asked whether FeedbackCallout has an action slot for "nhảy về nhiệm vụ hiện tại" — it does (`actionLabel`/`onAction`), but PersonalProjectTaskPage.tsx's own file header documents NOT using it here as a deliberate SCOPE CUT (button/async axis, not markdown).

### R1 × markdown
- **đang là**: SubmissionResultHeader.tsx → PageHeader: `title` wrapped in a raw `<span>` (harmless), `description` (from PersonalProjectResultScreenProps.description, file header explicitly says "handed down as plain strings — the block does not reshape them") forwarded straight to Page.tsx Header's bare Typography — same zero-formatting mechanism as D1/T1.
- **đúng phải là**: Identical to D1/T1 — description should be `richtext nhỏ` tier via RichText.
- **sửa ở tầng**: frame — same `Page.tsx` `Header` as D1/T1 (one shared file, one fix).
- **chỗ khác giống**: This is the THIRD confirmed instance this session (D1, T1, R1) of the identical PageHeader.description gap — sufficient independent sightings to promote from "documented risk" (bẫy #2) to a confirmed cross-screen pattern worth a canon update at feedback-end, rather than fixing 3 call sites independently.

### D3 × skeleton
- **đang là**: Trong hàm `Body` của PersonalProjectDashboard.tsx (dòng 255-262), lời gọi `<ProgressMeter value={stats.done} max={stats.total || 1} label={PROGRESS_LABEL} showValue showAnatomy={showAnatomy} anatPart={...} />` KHÔNG truyền `isSkeleton`. `ProgressMeter` (composites/stats/ProgressMeter/ProgressMeter.tsx dòng 47-79) đã có discriminated union `isSkeleton` riêng và tự vẽ mirror 2-node (label bar h-3 w-24 + track bar h-1 w-full) khi `isSkeleton=true` (dòng 73-79). Vì prop không được chuyền, `isSkeleton` mặc định `false` tại call-site này — kể cả khi `Body` đang chạy trong nhánh `AsyncContent.skeleton` (isSkeleton local=true, stats giả `{done:0,total:0,...}`), `ProgressMeter` vẫn vẽ MỘT THANH TIẾN ĐỘ THẬT ở 0% (value=0, max=1) kèm nhãn/percent thật thay vì shimmer — một hình thật lẫn vào bộ khung đang tải.
- **đúng phải là**: Theo cây quyết định §2 Q2 ('Phần hiện trong nhánh skeleton có phải một component khác ĐÃ CÓ isSkeleton riêng không?' → CÓ ⇒ hình 5, chỉ chuyền cờ xuống, KHÔNG tự vẽ số liệu giả) — ProgressMeter là đúng ca hình 5, y hệt cách SurfaceCard/Typography/ContinueCardHero/ContinueCardItem đã làm NGAY TRONG CÙNG HÀM Body (tất cả đều nhận `isSkeleton={isSkeleton}`). Phải sửa lời gọi thành `<ProgressMeter value={stats.done} max={stats.total || 1} label={PROGRESS_LABEL} showValue isSkeleton={isSkeleton} showAnatomy={showAnatomy} anatPart={...} />`.
- **sửa ở tầng**: block — PersonalProjectDashboard.tsx, hàm `Body` (chỉ sửa lời gọi component tại call-site; `ProgressMeter.tsx` chính nó đã đúng chuẩn, không cần đụng)
- **chỗ khác giống**: Không có nơi nào khác cần sửa — ProgressMeter.tsx (composite gốc) đã có union isSkeleton hoàn chỉnh từ trước; đây thuần là một chỗ quên chuyền cờ tại một call-site.

## Ô CÂM (1)

### R5 × prominence
PersonalProjectResultScreen.tsx dòng 333-342: ContentRelatedList chỉ render khi `!isPassing` — điều kiện NGƯỢC với domain-doc guess ('đạt → cuối màn hiện lối đi tiếp'). Với storyId được giao (`--passing`, tức isPassing=true), khối này KHÔNG render trong story đó — vùng R5 không có DOM thật để chấm điểm prominence trên đúng story đã chỉ định. Cũng xác nhận luôn: 'lối đi tiếp sang nhiệm vụ kế' mà domain doc nhắc KHÔNG PHẢI ContentRelatedList mà là leaf riêng `MilestoneUpNextCard` (dòng 116-163, hiện khi `showNextTaskHandoff = isPassing && nextTask != null`, dòng 283+344-351) — cây quyết định của MilestoneUpNextCard (Button variant="primary" cho CTA 'Đi tới nhiệm vụ tiếp theo', eyebrow muted, title default) đúng thang tương tự D2/ContinueCardHero nên không có nghi vấn. Cần B4: (1) B1 xác nhận lại storyId đúng cho R5 (một story isPassing=false, ví dụ --failing) trước khi đo prominence trên khung hình thật; (2) nếu chỉ cần xét cơ chế nội tại của ContentRelatedList (độc lập context) thì nó là CÙNG FILE với T5 (đã chấm DAT ở đó) nên khi render đúng, kết luận kế thừa DAT — nhưng không tự kết luận thay B1 vì nghi ngờ gốc của B1 (ContentRelatedList = lối đi tiếp) đã bị source bác bỏ, cần thầy xác nhận lại phạm vi vùng trước khi đóng sổ mục này.

## Ô N/A và lý do (63)

| Vùng | Trục | Lý do |
|---|---|---|
| T8 | reading-flow | SplitWorkspace là khung 2-slot CỐ ĐỊNH của trang (main/aside được đặt tên tường minh, không phải track biến thiên kiểu StackH/Cluster/StackV có quyết định justify/align theo Thang B). Việc cột phải sticky là chuyện trục frame/seam, không phải quyết định căn-đọc của reading-flow — trục này không áp cho loại khung này. |
| R5 | reading-flow | Câu hỏi thật của vùng này là TRÌNH TỰ HIỂN THỊ CÓ ĐIỀU KIỆN (component nào bật khi pass/fail) — không phải câu hỏi canh CHỮ (Thang A) hay canh KHỐI trong 1 track (Thang B) mà context.md định nghĩa; ContentRelatedList/MilestoneUpNextCard loại trừ nhau nên không có quyết định start/center/end/between nào đang diễn ra ở đây. Bản thân ContentRelatedList (đã audit ở T5, LECH text-left) không có vấn đề canh lề MỚI nào riêng ở R5. Phát hiện về trình tự/điều kiện nên chuyển cho trục khác (structure/async), không kết luận ở trục reading-flow. |
| T1 | prominence | chỉ breadcrumb/title/description mặc định (baseline), không có chip/accent/button nào cần chọn cơ chế nổi — trục prominence chưa bắt đầu tính (§1 dòng 25-28). |
| T2 | prominence | FeedbackCallout (composites/feedback/Feedback.tsx) THẬT SỰ có action slot (`actionLabel`/`onAction`, dòng 93-99 + 156-161) dựng Button tô skin theo `status` — không phải component thiếu năng lực. Nhưng PersonalProjectTaskPage.tsx (readingColumn, dòng 348-355) cố ý KHÔNG truyền actionLabel/onAction; file header (dòng 66-69) ghi rõ đây là SCOPE CUT có chủ đích: nút 'nhảy về nhiệm vụ hiện tại' cần một target task id mà prop list của màn này chưa mang, nên bị cắt phạm vi thay vì đoán prop. Trục prominence hỏi 'cơ chế nào cho một phần tử ĐANG render' — không áp được lên một phần tử bị cắt phạm vi có ghi chú rõ ràng trong source; đây không phải lỗi chọn sai cơ chế nổi bật. |
| T3 | prominence | SurfaceCard label + MarkdownContent là container/label mặc định, không có yếu tố chip/accent/button cần escalate trong mô tả đã có. |
| T4 | prominence | 2 SurfaceCardAccordion lồng nhau là container/label mặc định, không có chip/accent/button nào được mô tả cần chọn cơ chế nổi. |
| T8 | prominence | Container+SplitWorkspace là khung layout 2 cột (đối tượng của trục frame/seam), không có phần tử text/chip/button nào ở tầng khung này để áp thang prominence. |
| D1 | async | mô tả nói rõ trail/title 'ngồi NGOÀI AsyncContent' (luôn biết trước khi milestone fetch xong), isSkeleton chỉ chạm Chip = shimmer từng phần không đổi cấu trúc → khớp dứt khoát cây 2 Q3 ('không phải trục này'). |
| T1 | async | 'isSkeleton chạm title/description qua Typography skeleton riêng' = shimmer từng phần, không đổi cấu trúc → khớp dứt khoát cây 2 Q3. |
| T2 | async | Vùng này KHÔNG đi qua bốn nhánh error/loading/empty/content của AsyncContent, và FeedbackCallout không mang bất kỳ prop async nào để kiểm — điều kiện `!isSkeleton && isLocked` là một cờ NGHIỆP VỤ (đã mở khoá hay chưa) dùng isSkeleton chỉ để tạm ẩn banner lúc đang tải, không phải một trong bốn nhánh của trục này nên không áp được thang/cây quyết định của context.md — do đó NA cho trục async, không phải LECH/CAM. |
| T6 | async | Đúng §2 Q3: đây là vùng chỉ shimmer từng phần (form luôn hiện, chỉ đổi vỏ khi isSkeleton), context.md dòng 3-6 tự loại nhánh này ra khỏi phạm vi trục async — nên NA, không phải DAT/LECH. |
| T8 | async | SplitWorkspace + Container là khung layout thuần (main/aside cố định), không phải một 'vùng dữ liệu' có nhánh error/loading/empty/content riêng của chính nó — trục async không áp cho khung composition. |
| R1 | async | Giống T6/T1 — đây là identity header luôn hiện, chỉ shimmer-part-by-part, không có nhánh error/loading/empty/content nào để áp cây quyết định của trục async → NA. |
| T2 | frame | Trục frame không có gì để phán quyết ở vùng này: không phải quyết định chọn Stack/Cluster/Grid/Split/Container nào cả, chỉ là một lệnh gọi composite lá với props đơn. |
| T3 | frame | SurfaceCard label='Hướng dẫn' chỉ ôm ĐÚNG MỘT child (MarkdownContent) — không có quyết định giữa Stack/Cluster/Grid/Split nào được thực thi ở vùng này (khác N con thì mọi khung cũng render giống nhau với N=1), nên trục frame không áp cho lựa chọn tại call-site này. |
| R2 | frame | Đọc trọn SubmissionAttemptSelector.tsx xác nhận trên trục frame nó dùng đúng Cluster (skeleton, N-cùng-kiểu) và giao phần layout thật cho atom ButtonRadioGroup (atom được miễn trừ luật hand-roll) — không có gì để LECH; phần ngưỡng số lượng nằm ngoài trục này. |
| D1 | inset | PageHeader (composite '@sb-components/composites/layout/Page/Page' — Header/PageHeader.tsx) không có prop `padding` nào trong PageHeaderOwnProps, và đọc trọn hàm render (dòng 123-195) không có bất kỳ class `p-*` nào — chỉ có `gap` (StackV/StackH gap='grouped'/'related'). File header ghi rõ: 'This frame carries no card wrapper — the caller places it directly inside a Container or wraps it in a SectionCard when a framed surface is required.' PersonalProjectDashboard.tsx cũng không bọc PageHeader trong Container/SurfaceCard nào ở tầng của chính nó (breadcrumb chỉ có `<div className="w-fit">`, không phải padding). Vùng này không có 'bề mặt' nào mang padding để soi trục inset — trục không áp dụng, không phải thiếu dữ liệu. |
| D3 | inset | ProgressMeter (progress bar) + Typography (dòng thống kê) đều là atom-tier leaf ngồi trực tiếp trong StackV của Body, không tự sở hữu khung/bề mặt riêng — trục inset không áp cho hình học nội bộ atom (§6 mục 7, atom được miễn), khoảng cách giữa hai leaf này là seam (gap) chứ không phải inset. |
| T1 | inset | Cùng lý do D1: readingColumn gọi `<PageHeader breadcrumb=... title=... description=...>` (PersonalProjectTaskPage.tsx dòng 323-346), không truyền padding — mà PageHeader (Page.tsx) hoàn toàn không có prop/class padding nào (chỉ gap). Trục inset không có bề mặt nào để soi ở vùng này. |
| T2 | inset | PersonalProjectTaskPage.tsx dòng 348-355: `<FeedbackCallout status="warning" title=... description=... anatPart=...>` không có `className` hay override nào. Đọc trọn `Feedback.tsx` Callout (dòng 134-172): component này KHÔNG tự vẽ padding — nó chỉ forward toàn bộ sang `<Alert status tone="soft" .../>`, một ATOM. Theo canon §6 mục 7 (atom được MIỄN InsetScale, atom tự lo hình học nội bộ), padding thật của banner nằm ở tầng atom `Alert.tsx` ngoài phạm vi gate/trục này ở tầng composite/block — không có quyết định inset nào để soi tại tầng đang xét, và cũng không phải thiếu số đo (kiến trúc cố ý đẩy xuống atom). |
| R1 | inset | SubmissionResultHeader.tsx dòng 87-110 gọi `<PageHeader breadcrumb=... title=... description=...>`, không truyền padding — và PageHeader (Page.tsx) không có bất kỳ prop/class padding nào ở bất cứ đâu trong hàm render (chỉ `gap`). Không có bề mặt mang inset ở vùng này để soi — NA cùng lý do D1/T1, không phải thiếu số đo. |
| R2 | inset | Đọc trọn SubmissionAttemptSelector.tsx: toàn bộ cây (AsyncContent > ButtonRadioGroup/Cluster/Chip/Button) không có bất kỳ `SurfaceCard`/`padding`/class `p-*` nào ở tầng composite hay block — chỉ có `gap` (Cluster gap='related'). `ButtonRadioGroup`/`Button`/`Chip` là ATOM, được miễn InsetScale theo canon §6 mục 7 (atom tự lo hình học, ví dụ neo `Input.tsx`/`Select.tsx` pr-9). Không có quyết định inset nào ở tầng block để soi — không phải thiếu dữ liệu, mà kiến trúc của vùng này thực sự không chạm trục padding ở tầng đang xét. |
| D1 | surface | PageHeader = breadcrumb/title/description/Chip trạng thái, không phải khung mặt (card) — không có variant surface/nested nào để chọn theo cây §2. Chip pill bên trong dùng rounded-full theo vai atom cố định (§2 câu 4), không phải quyết định riêng của node này. |
| D3 | surface | ProgressMeter + dòng Typography thống kê không có khung mặt riêng (không SurfaceCard/border/shadow nào được mô tả) — chỉ là meter + text nằm trực tiếp trong Body. |
| T1 | surface | giống D1 — PageHeader chỉ có breadcrumb/title/description, không phải khung mặt cần bo góc/viền/bóng riêng. |
| T8 | surface | SplitWorkspace (Container padding=roomy bọc main/aside) là khung LAYOUT 2 cột, không phải một khung mặt (card) có border/bo/bóng riêng — không áp cây §2. Tính sticky là hành vi vị trí, không thuộc trục surface. |
| R1 | surface | SubmissionResultHeader là identity band header (backLabel/title/description), cùng dạng PageHeader — không có khung mặt riêng để áp thang §1/cây §2. |
| R2 | surface | Đọc toàn bộ SubmissionAttemptSelector.tsx: chỉ compose `ButtonRadioGroup` + `Chip` bên trong `AsyncContent`, không có SurfaceCard/card face nào, không một `rounded-*`/`shadow-*`/`border` tự vẽ nào ở tầng block này — không có câu hỏi bo góc/viền/bóng/nền nào để trục 'surface' phán quyết. Ghi chú riêng: mối nghi B2a nêu (ngưỡng '5 chip + nút +N' so với Challenge) là câu hỏi vét-cạn/đếm-trạng-thái, KHÔNG thuộc trục surface — không xử lý lại ở đây. |
| T2 | text | FeedbackCallout (`.Callout`) KHÔNG render title/description qua atom `Typography` mà qua `HeroAlert.Title`/`HeroAlert.Description` gốc của HeroUI — đây là NGOẠI LỆ ĐÃ GHI RÕ trong doc của Feedback.tsx (dòng 38-42): "DELIBERATE EXCEPTION: .Callout keeps HeroUI's Alert.Title/Alert.Description vì HeroUI tự mang theo hợp đồng COLOR-BY-STATUS". Trục text/context.md chỉ định nghĩa/áp cho `TypographySize`+`weight` của atom `Typography.tsx` — HeroAlert.Title/Description không có prop size/weight nào để đối chiếu với bảng tier, nên trục này không có gì để phán quyết ở vùng T2. |
| T8 | text | SplitWorkspace là khung layout thuần (Container + main/aside), tự nó không sở hữu text nào — toàn bộ chữ đã tách vào các vùng con readingColumn (T1-T6) và submissionPanel (T7). Trục text không áp cho khung layout. |
| R2 | text | SubmissionAttemptSelector.tsx KHÔNG có bất kỳ lời gọi `Typography` atom nào — toàn bộ chữ đi qua `Chip` (nhãn "Lần N · điểm", `attemptChip`, dòng 124-131) và `Button`/`ButtonRadioGroup` (label "+N", overflowLabel). Cả hai đều là atom khác sở hữu kiểu chữ nội bộ riêng, không phải call-site chọn size/weight qua Typography. Trục text/context.md chỉ áp cho props `size`/`weight` của atom Typography.tsx nên không có gì để đối chiếu ở vùng này. |
| D3 | icon | props liệt kê đầy đủ ở mức primitive (ProgressMeter value/max/label/showValue; Typography size=xs color=muted text=statsLine) — không có icon component nào trong khối này, chỉ progress bar + dòng chữ thống kê thuần. |
| T3 | icon | SurfaceCard label='Hướng dẫn' ôm MarkdownContent — mô tả liệt kê đủ (label + isSkeleton + nội dung markdown), không phải Accordion/control có indicator, không có icon nào được nêu. |
| T8 | icon | Container + SplitWorkspace chỉ là khung layout 2 cột (main/aside), không mang icon riêng — icon (nếu có) thuộc về các vùng con T1-T7 đã tách riêng. |
| T8 | color | Container + SplitWorkspace là khung layout thuần (chia 2 cột), tự nó không có Typography/Alert/Chip hay text/icon nào cần tô màu — màu thuộc về các vùng con T1-T7 đã xét riêng. |
| D1 | button | vùng chỉ có Breadcrumbs + Typography (title/description) + Chip trạng thái GitHub — không có Button component nào, trục button không áp. |
| D3 | button | chỉ có ProgressMeter (đọc, không tương tác) + dòng Typography thống kê — không có Button. |
| D4 | button | Trục 'button' (ButtonVariant/ButtonSize) không áp dụng cho vùng này vì không có phần tử Button nào được dựng — quyết định dùng Link thay Button đã được ghi rõ, có chủ đích (teacher finalized 2026-07-26), không phải thiếu sót cần đối chiếu variant/size. |
| T1 | button | PageHeader chỉ gồm Breadcrumbs + title + description — không có Button. |
| T2 | button | Trục 'button' không áp dụng: không có Button nào render trong vùng T2 hiện tại. Việc thiếu nút 'nhảy về nhiệm vụ hiện tại' là gap về PROP/SCOPE (đã ghi sổ, chờ prop targetTaskId), không phải lỗi chọn variant/size — nằm ngoài phạm vi trục button. |
| T3 | button | SurfaceCard bọc MarkdownContent (brief.body) — không có Button. |
| T4 | button | vùng là 2 SurfaceCardAccordion lồng nhau (tiêu chí + code) — trigger accordion không phải Button component chịu trục variant/size này; không có Button rời nào được nêu. |
| T5 | button | ContentRelatedList chỉ có label + items liệt kê — không có Button được nêu. |
| T7 | button | SubmissionScoreCard chỉ hiển thị số liệu/label (score, feedback, model, timeAgo) — không có Button nào được nêu trong panel Task này. |
| T8 | button | SplitWorkspace là khung layout 2 cột (Container + main/aside) — không phải Button, trục button không áp. |
| R1 | button | Không có Button nào trong vùng R1 — nút quay lại cố tình là LinkBack (atom Link, đã ghi rõ trong file header dòng 20-30: 'result page có đúng 1 nơi để quay lại → compose backLabel+onBack thành LinkBack, không phải Button'). Trục button không áp dụng. |
| R3 | button | Không có Button trong vùng R3 để đối chiếu variant/size — link submission cố tình là Typography isLink, không phải Button. Trục button không áp dụng. |
| R4 | button | SubmissionFindingsList là danh sách góp ý (severity low/medium/high) — không có Button được nêu. |
| R5 | button | Không có Button trong R5 (ContentRelatedList) — cố ý 'quiet, no button' theo source. Trục button không áp dụng cho vùng này; component thật sự chứa nút 'đi tiếp' nằm ở MilestoneUpNextCard, ngoài phạm vi vùng R5 như đề bài mô tả. |
| D1 | press | PageHeader tự thân (title/description/meta Chip trạng thái GitHub) không có onPress/href riêng — Chip chỉ là badge hiển thị tone theo isConnected, không phải chip dismiss/action. Breadcrumb là primitive điều hướng có hành vi link mặc định đã ổn định, không phải điểm lệch của vùng này. Cây §2 câu 1 trả lời KHÔNG cho phần thân PageHeader ⇒ none, không cần đo pixel. |
| D3 | press | ProgressMeter và dòng Typography thống kê là hiển thị thuần, không có onPress/href được nêu. Cây §2 câu 1 ⇒ none, không cần số đo pixel. |
| T1 | press | cùng dạng PageHeader như D1 — breadcrumb (collapseFrom=4)/title/description không có onPress/href riêng của khối; isSkeleton chỉ chạm Typography. Cây §2 câu 1 ⇒ none cho phần thân vùng. |
| T3 | press | SurfaceCard 'Hướng dẫn' chỉ bọc MarkdownContent hiển thị brief.body, không có onPress/href được nêu — cây §2 câu 1 ⇒ none. |
| T7 | press | SubmissionScoreCard trong panel Task chỉ hiển thị score/feedback/model/thời gian, không có onPress/href nào được liệt kê trong props đã biết — cây §2 câu 1 ⇒ none. |
| T8 | press | SplitWorkspace + Container là khung bố cục 2 cột (sticky aside), không phải phần tử bấm được — trục press không áp dụng ở tầng khung layout (thuộc frame/seam, không phải press). |
| R2 | press | §1 context.md: 'NÚT (Button/ButtonBase) NẰM NGOÀI THANG NÀY... Hỏi nút này ở bậc nào là hỏi sai câu.' Toàn vùng R2 chỉ gồm Button-family (ButtonRadioGroup + Button overflow); Chip bên trong không tự mang onPress riêng (chỉ là content trang trí bên trong nút). Vì vậy trục press không áp dụng được cho vùng này — verdict NA đúng theo canon, không phải thiếu dữ liệu. |
| R3 | press | cùng lý do T7 — SubmissionScoreCard bản đầy đủ ở Result cũng chỉ hiển thị điểm/chip trạng thái tĩnh, không có onPress/href được nêu thêm trong mô tả B0/B1 cho ngữ cảnh Result. Cây §2 câu 1 ⇒ none. |
| R4 | press | SubmissionFindingsList là danh sách góp ý hiển thị severity tĩnh, không có onPress/href nào được nêu trong props/hành vi đã biết — cây §2 câu 1 ⇒ none. (Nghi vấn duy nhất của vùng này là frame/rỗng-ẩn, thuộc trục khác, không phải press.) |
| R5 | press | Verdict NA vì phần tử được khoanh (ContentRelatedList) không xuất hiện trong DOM của story --passing nên không có gì để áp trục press lên; đây là phát hiện cấu trúc (đã đọc source dứt khoát, không phải thiếu dữ liệu) chứ không phải CAM. |
| T6 | markdown | toàn bộ chữ trong vùng này là nhãn hệ thống/form — InputText label, ListRow label/meta, Button label — đều nằm NGOÀI PHẠM VI trục markdown theo §3c ("label (button/input/chip/badge label): NGOÀI PHẠM VI... không phải nội dung tác giả"). Không có field chữ tác giả nào để áp thang §1. |
| T8 | markdown | đây là khung layout thuần (Container + SplitWorkspace main/aside), không sở hữu field chữ tác giả của riêng nó — nội dung chữ nằm ở các vùng con T1..T7 đã xét riêng. |
| R2 | markdown | đây là bộ chọn lần chấm dạng chip (nhãn số lần/ngày + nút "+N") — thuộc loại nhãn hệ thống/badge, ngoài phạm vi trục markdown theo §3c. Câu hỏi mở trong mô tả vùng (ngưỡng 5/+N) thuộc trục khác (không phải markdown), không ảnh hưởng verdict trục này. |
| T2 | skeleton | FeedbackCallout chỉ render khi !isSkeleton && isLocked — trong lúc tải (isSkeleton=true) node này KHÔNG mount, không tự vẽ hình gì cả nên trục skeleton không áp cho chính node này ở nhánh này. |

## Ngoài phạm vi (KHÔNG sửa vòng này)

Chưa có — chờ thầy chọn ô nào duyệt trước khi biết cái gì ngoài phạm vi.
