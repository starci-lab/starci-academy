# BACKLOG — hàng đợi proposal CHỐT → BUILD

> Status: ⏳ PENDING · 🔨 BUILDING · ✅ DONE · ❌ DROPPED
> Không xóa dòng — backlog là sử ký. Apply flip status khi dựng xong.

| Proposal | Status | Tóm tắt | Ngày |
|---|---|---|---|
| [story-driven-component-migrations](story-driven-component-migrations.proposal.md) | ⏳ PENDING | Migration component do lane `starci-fe-story` phát hiện. **DailyQuest surface-in-surface**: `LabeledCard` khung + `SurfaceListCard bordered` = 2 lớp fill chồng → về pattern canonical `LabeledCard frameless` + `SurfaceListCard` top-level, dòng `completePrompt` ngoài card `gap-2`. Kèm 1 mục chưa chốt: `UpcomingLivestreamCard` dính y hệt + còn dùng block legacy `SectionCard` | 2026-07-16 |
| content-ai-rail-scope | ✅ DONE-ĐỢT-1 2026-07-20 (FE-only · tsc+eslint sạch · ⚠️ RUNTIME CHƯA VERIFY — thầy soi Storybook tag `news` + app) — **Đã dựng**: (1) chip theo scope — `EMPTY_STATE_SKILLS` (lesson: challenges+flashcards · course: lessons+challenges+flashcards) + `retrievalLabelKey(token, scope)` → nhãn `…OfLesson`/`…InCourse`, 8 key i18n mới vi+en; (2) **giữ thread khi đổi bài** — `useEffect([contentId])` không còn `setMessages([])` vô điều kiện: có thread sống thì carry + chèn `contextMarker` (render là DIVIDER, không phải bubble) + `contentSelectedRef` claim trước để hydration không ghi đè; panel rỗng thì vẫn reset như cũ (khôi phục hội thoại cũ của bài đó); thêm `messagesRef` để effect đọc thread mà không dep vào `messages` (tránh chạy lại mỗi token stream); (3) **pill lên header** — tách block mới `ContentAiScopePill` (container tự đọc redux+store), rail bỏ `EntityLink` tiêu đề trùng; `prefersCourseScope` nâng từ local state → store mới `contentAiChatScope` (KHÔNG persist — nới scope là ý định của phiên đọc, không phải sở thích); (4) dòng-2 lịch sử → `originContentTitle ?? "Cả khoá" · N lượt`, snippet CHỈ khi đang search. **★ Tự phản biện bắt được 1 chỗ sót THẬT**: `runRetrievalSkill` gửi `t(commands.${token})` làm NỘI DUNG câu hỏi — chính dòng này in "Tìm challenges liên quan bài này" vào transcript trong ảnh thầy gửi; chip đổi nhãn mà quên nó thì vẫn sai y cũ. Story `ContentAiScopePill` (tag `news`). **Mặc định tôi chọn thay thầy** (thầy để No-preference §8): parked + FE-first. **Đợt 2 còn lại**: gate `courseId` cho rail ở `InnerLayout` + state parked (`Callout`+CTA) · rename (`renameContentAiSession`) · archive (`archived_at`) · chip "Học gì tiếp?" (`myCourseOutline`). | Mô hình SCOPE 4 lớp cho chat + relayout pane phải (thầy 2026-07-20). **3 lỗi đã định vị**: (a) `ContentAiChat/index.tsx:995` render `RETRIEVAL_SKILLS.slice(0,2)` ở CẢ 2 scope trong khi nhãn i18n viết cứng "liên quan **bài này**" → ở scope khoá chip nói "bài này" mà không có bài, trả về challenge module khác (đúng ảnh thầy gửi); `SUGGESTION_KEYS` thì ĐÃ gate đúng → lỗi ở NHÃN+TẬP chip, không phải cơ chế scope. (b) **bug MỚI do hoist rail**: `ContentAiFab` gate `if(!courseId) return null` nhưng `InnerLayout:49` KHÔNG gate → ra khỏi khoá rail vẫn mở, pill còn tên khoá cũ. (c) danh tính scope in 2 lần (header + pill composer). **Trả lời 3 câu thầy**: ngoài khoá KHÔNG cho hỏi vì BE không có grounding (`resolveCourseGrounding` cần courseId) → retrieval-only sẽ thành nói mồm; 4 bộ chip theo scope (đoạn/bài/khoá/ngoài) theo luật "chip hỏi-đáp cần VĂN BẢN, chip retrieval cần PHẠM VI"; chat xuyên trong-ngoài khoá → **parked** (thread read-only + CTA về khoá), không xoá. **Relayout 3 zone**: pill scope LÊN header thay tiêu đề (bỏ trùng), "Lịch sử trò chuyện" hạ xuống nút icon (accordion đang ăn ~15% chiều cao), body chỉ còn thread. Prototype :8080 (6 màn) + `/journey.html` (hành trình khoá A→bài B→bôi đen, đối chiếu hiện-tại/đề-xuất). Cần tạo: state parked (`Callout`+CTA). **+ Lỗi thứ 4 tìm được khi vẽ hành trình**: `useEffect([contentId])` chạy `setMessages([])`+`setCurrentSessionId(null)` ⇒ mở bài là XOÁ thread đang chat; cộng `onSelectSearchResult` gọi `closeChat()` ⇒ bấm kết quả tìm được là mất luôn cuộc trò chuyện vừa dẫn tới nó. Đề xuất: giữ thread + chèn MỐC ngăn cách khi đổi grounding; bôi đen là lớp CHỒNG LÊN (header đổi màu hổ phách) không phải scope thứ 4. **§5b thầy chốt A (2026-07-20)**: tách session theo bài, giữ auto-title (`COALESCE` câu hỏi đầu), đổi dòng-2 hàng lịch sử từ snippet → `originContentTitle ?? "Cả khoá"` + số lượt (snippet chỉ giữ khi search). ✅ **BE ĐÃ SẴN cho phần này** (verified repo chính — worktree tôi lệch, suýt báo nhầm thiếu): `originContentTitle` có sẵn, `listSessions`+`searchSessions` đều `LEFT JOIN contents` kèm comment cảnh báo INNER join sẽ rớt session course-general ⇒ **FE-only**. 🔨 **Rename + Archive phải mở BE**: hiện chỉ có `deleteSession`; cần mutation `renameContentAiSession` (UPDATE thẳng, không COALESCE; rỗng→NULL để về auto-title) + cột `archived_at timestamptz NULL` (**synchronize-safe**, không enum/drop-type) + `sessions(includeArchived=false)`; search NÊN quét cả archived. UI: nút xoá hover → overflow menu ⋯ (Đổi tên · Lưu trữ · Xoá). ⚠️ Giới hạn A: trả lời được "mẩu này ở đâu" nhưng KHÔNG nối lại được "hai mẩu từng là một mạch" — muốn thế phải `parent_session_id` + lịch sử dạng cây (phương án C). | 2026-07-20 |
| container-responsive-split-shell | ⏳ PENDING (chờ 3 câu §7) | Split-shell kiểu Phantom wallet (thầy 2026-07-20): nửa trái = Navbar+content bọc `@container`, nửa phải = `ContentAiChatRail` full-height (`top-0 h-dvh`, DỜI từ `LearnShell.rightRail` lên `InnerLayout`); kéo rail rộng thì **nửa trái tự xuống layout mobile**. Chẩn đoán: "lệch" KHÔNG phải sai sticky — `top-16` + `h-[calc(100dvh-4rem)]` trùng khớp `LearnSidebar` và left rail, container đã có `items-start` nên không stretch; nguyên nhân là app responsive theo VIEWPORT, đúng như comment `ContentAiChatRail` tự thú *"pushing the WHOLE app into a narrow column breaks the app's viewport-based responsive layout"*. ⚠️ **Điểm dễ coi nhẹ: CSS `@container` một mình KHÔNG đủ** — `useSmViewpoint` là `useMediaQuery` viewport và nó quyết định COMPONENT NÀO render (drawer vs rail), dùng ở learn/layout + **10 drawer**; sửa mỗi CSS → trạng thái lai TỆ HƠN hiện tại (CSS bảo hẹp, JS bảo desktop). Cần hook đo container (ResizeObserver) đổi song song. Tailwind v4 sẵn `@container`; tiền lệ `GroupPressableCard` (`@lg:` NOT viewport). Quy mô: LearnShell 16 `lg:` · ContentAiChat 4 · Navbar 0 (co bằng flex). Blast 🔴 `InnerLayout` (MỌI route) + hook (10 drawer). Rủi ro chốt trước: rail che navbar có phải chủ ý · sticky đổi scroll-container · SSR nhấp nháy · áp toàn app hay chỉ /learn | 2026-07-20 |
| content-ai-chat-app-wide | ✅ DONE-CODE 2026-07-20 (BE+FE tsc sạch · eslint sạch · ⚠️ RUNTIME CHƯA VERIFY: chưa boot BE (cần postgres+qdrant+minio+keycloak+ollama) · chưa chạy migration trên DB thật · chưa lái browser) — **BE**: 2 entity nullable (`content_ai_sessions.origin_content_id` + `content_ai_messages.content_id`) + migration `1728000000000-AllowCourseGeneralContentAiSessions` (widening-only, down xoá row course-general) · exception mới `ContentAiScopeNotFoundException` · `ContentAiService` tách `resolveLessonGrounding` (giữ NGUYÊN hành vi cũ) vs `resolveCourseGrounding` MỚI + `resolveEnrollmentIdByCourse` + `resolvePremiumContentIds` · `buildSystemPrompt` thêm scope `course` (prompt riêng: cấm bịa lesson, cấm claim biết tiến độ) · `retrieveCourseExcerpt` thêm `excludeContentIds` (Qdrant `must_not`) · 3 DTO + socket payload nhận `courseId`, `contentId` optional · `searchCourseContent` thêm `isLocked` + **strip snippet** + enrollment check (§12 A/B/C XONG) · socket gateway thêm rate-limit 30 ask/5phút/user (HTTP đã có Soft throttler sẵn — lỗ hổng thật là socket). **FE**: FAB bỏ gate `!contentId`→gate `!courseId` (hiện mọi tab) · `ContentAiChat` XÓA HẲN slash machinery (VERBS/FIND_OBJECTS/palette/keyboard-nav) → `RETRIEVAL_SKILLS` + nút 🔍 composer (tappable, mid-conversation) · context pill 3 trạng thái (lesson mặc định / course / ẩn khi có selection) + toggle widen-narrow · chip theo scope (summarize/hardest/example CHỈ lesson-scope) · rail+drawer header fallback tên khoá · `EntityResultRow` badge 🔒 "Ghi danh để mở" · i18n vi+en. **Tự phản biện bắt được 1 BUG THẬT**: search trong course-scope lọc `originContentId === contentId` (undefined) → xoá sạch kết quả, đã sửa. ⚠️ **2 LỆCH so proposal (cần thầy biết)**: (1) `isPremium`→đặt tên `isLocked` + resolve theo VIEWER, vì challenge/milestone KHÔNG có cột premium (milestone=capstone enrolled-only, `isPremium` thẳng sẽ gán sai) (2) context pill dựng INLINE trong ContentAiChat, KHÔNG tách block `ChatContextPill` → **chưa có story Storybook** (nó đọc thẳng scope/course/content state, tách ra phải prop-drill 5 giá trị cho 1 call-site duy nhất) — thầy muốn tách+story thì làm lượt sau. ⚠️ Worktree DÙNG CHUNG với session course-qa đang chạy (i18n sửa bằng edit nhỏ có anchor, không rewrite). **ĐỢT 2 (thầy: "challenges search theo content, milestone khỏi search")**: (a) BỎ HẲN milestone khỏi skills chat + `detectContentIntent` + `TOOL_RESULT_META` + lọc phòng thủ ở kết quả RAG + lọc luôn ở view "Xem tất cả" (nhất quán) — **BE GIỮ NGUYÊN kind milestone** vì `MindMapNodeDrawer` cũng dùng `searchCourseContent` gom 4 loại, chỉ chat thôi không hỏi; (b) challenges khi ĐANG MỞ BÀI giờ lấy từ query `challenges(contentId)` (quan hệ FK, chính xác+đủ) thay vì RAG — RAG có thể xếp thử thách của chính bài đó thấp hơn bài khác hoặc bỏ sót; course-scope vẫn RAG; (c) **VÁ BUG TIỀM ẨN**: FE chưa bao giờ truyền `kinds` cho `searchCourseContent` dù BE hỗ trợ — chính comment BE cảnh báo ca này ("must search ONLY challenge chunks, else… the caller's kind filter empties"), nên `/find challenges` cũ hay ra rỗng; giờ truyền `kinds` đúng cho mọi skill. tsc 0 + eslint 0. · ✅ 4 QUYẾT ĐỊNH CHỐT 2026-07-20 (trial ĐƯỢC retrieval+CTA ghi danh · rate-limit Soft throttler lane chatbot · nút ⌥ làm luôn đợt 1 · migration nullable làm tay) · ⚠️ RÀNG BUỘC BẢO MẬT do trial-retrieval: `searchCourseContent` KHÔNG lọc premium + snippet lộ 280 ký tự body → BE phải (A) thêm `isPremium` vào result (B) che snippet premium cho non-enrolled (C) nhánh course-general grounding LỌC chunk premium cho non-enrolled (proposal §12) | Content-AI chat app-wide TRONG 1 khóa (bỏ gate `if(!contentId) return null` ở `ContentAiFab` → FAB mọi tab; course-general RAG khi không mở bài; session `origin_content_id` nullable+migration). **Skills = CHỈ retrieval RAG** — BỎ mọi generative (không /quiz tạo thẻ, không digest tiến độ). **BỎ SLASH hoàn toàn** — retrieval gọi bằng chip theo-ngữ-cảnh + NL `detectContentIntent` (đã có sẵn); summarize/explain/example hạ về chip lesson-context (không phải skill). **THÊM context pill** ⭐: 3 lớp (🔖 đoạn bôi đen — đã có · 📖 bài đang đọc = MẶC ĐỊNH khi đọc · ✦ cả khóa), minh bạch+đổi được. Phân tích business: retrieval rẻ (embedding+Qdrant, 0 LLM) + dẫn phễu vs generative đắt GPU (1 con RTX5060) + ngõ cụt; trial+retrieval = result premium 🔒+CTA ghi danh (phễu trial→paid). Ma trận: 🟢 bỏ gate+XÓA slash+context pill+chip · 🟠 course-general RAG+session nullable+myCourseOutline+trial-lock · 🔴 BỎ digest tiến độ. Prototype :8097 (retrieval, no slash). Chờ chốt: 4 câu proposal §11 (trial được chat ko · rate-limit GPU · nút ⌥ phase-2 · migration prod). | 2026-07-20 |
| course-qa-chat-bubble | 🔨 BUILDING (✅ PHA 1·2·3 XONG 2026-07-20 — tsc+eslint sạch 2 repo, ⚠️ BE runtime CHƯA chạy; PHA 4 = ranh giới dừng-an-toàn) — **PHA 2 (4 việc, 0 migration)**: reaction-trên-câu-hỏi · sort "Sôi nổi nhất" (`CourseQuestionFilter.Engagement` enum + order-by subquery) · follow người hỏi (FE-only, field-resolver `isFollowedByMe` sẵn) · notify-khi-trả-lời (`CreateCommentService`+`NotificationService` global, `notifyAnswerTargets` bắn `CommentReply` parent+root-asker, i18n mới). **PHA 3 (accept+pin, boolean-col SYNCHRONIZE-SAFE, ko enum/drop trap)**: entity `content_comments.is_accepted_answer`+`is_pinned` (2 boolean default false); domain `CommentService.setAcceptedAnswer` (asker/founder-gate, 1-accepted-per-question, clear siblings) + `setPinned` (founder-gate); 2 mutation mới `acceptAnswer`+`setQuestionPinned` (resolver+graphql-types, reg DiscussionMutationsModule); expose `CourseQuestionNodeObject.isPinned`+`CommentNodeObject.isAcceptedAnswer`+mapper; roll-up order pinned-first. FE: types+query selects+`mutation-accept-answer`+`useMutateAcceptAnswerSwr`+`onAcceptAnswer` hook; badge "Câu trả lời được chấp nhận"+action Chấp nhận/Bỏ (asker-gate=isMineQuestion, direct-answer only); pin badge (question bubble + inbox row). **PIN-ACTION UI HOÃN**: FE ko có cờ founder → chỉ hiện badge ghim (đọc); mutation `setQuestionPinned` sẵn cho admin-UI sau. File mình tsc=0 (47 lỗi src/ BE + 7 comment.service.spec đều PRE-EXISTING). **PHA 4**: ✅ @mention-notify XONG 2026-07-20 — ⚠️ TỰ ĐÍNH CHÍNH: claim "cần enum MỚI → risk synchronize" là ĐÚNG NỬA (verify lại: `notification_type` là PG-enum THẬT nhưng CHỈ 1 CỘT dùng → bẫy memory "≥2 cột" KHÔNG áp dụng) **và có đường 0-SCHEMA**: `type` chỉ là phân loại, TEXT nằm ở PAYLOAD → mention TÁI DÙNG `NotificationType.CommentReply` + title-key riêng `notification.commentMention.title` ⇒ **0 enum, 0 migration**. Impl: `MENTION_PATTERN` + cap 10/comment; resolve handle qua QB `LOWER(username) IN (...)` (case-insensitive, 1 query) + `isDeleted=false`; skip actor + skip người đã ping ở `notifyAnswerTargets` (hàm này giờ RETURN Set recipient để dedupe); best-effort try/catch. i18n vi+en. ❌ **presence/typing VẪN DỪNG** (đã verify: 0 hạ tầng presence, ko cột lastSeen, chỉ có mock content dạy học → cần socket presence store MỚI, nhiều ngày, ko verify blind nổi). — **PHA 1 giao**: 3 comp mới feature-local `QaMessageBubble`(answer/reply = ChatBubble neo currentUserId, ReactionBar 6-cảm-xúc live + reply/edit/xoá + reply-to-reply FLATTEN tag `↳`) · `QaInboxRow`(row inbox flush: avatar+preview 2 dòng+tag+status chip+chấm màu, cả row 1 press target) · `QaConversationHeader`(asker + AvatarGroup người-trả-lời derive client + "N trả lời"/nudge "hãy là người đầu tiên"; follow HOÃN pha 2 vì cần isFollowedByMe). QuestionRow rewrite: collapsed→QaInboxRow, expanded→header+câu-hỏi-là-bubble-đầu(+lessonTag funnel+status)+answers bubble+`Composer` pill đáy. CourseQa list→1 `SurfaceListCard` flush (thay N-Card). Skeleton mirror inbox. i18n vi+en (you/askedBy/collapse/beFirstToAnswer/replyingTo). Lệch nhẹ vs prototype: bubble KHÔNG avatar riêng cạnh (dùng ChatBubble canonical, identity ở header line) · composer đáy=`Composer` block, top-ask giữ `CommentComposer` collapsible. KHÔNG story (feature-local P4 exempt). | CourseQa → không gian hỏi-đáp XÃ HỘI cả lớp (founder chỉ là 1 người nổi bật): composer pill đặt câu hỏi · danh sách = social inbox (avatar-group ai-tham-gia + reaction summary + reply count + chấm màu + follow-hover) · mở 1 câu hỏi = mạch bubble 1 dòng (câu hỏi=bubble đầu + status, answer cả-lớp+founder, mỗi bubble thả 6-cảm-xúc, header follow người hỏi). Grounded data ceiling: 🟢CÓ NGAY (reaction 6-emoji trên answer + thread + follow + enrollment — block `Discussion/ReactionBar`+`FacebookReactionSelector`+`FollowButton`+`AvatarGroup` đã có, mutation `reactToComment`/`setFollow` live) · 🟠BE RẺ ko-migration (reaction trên CÂU HỎI = enrich `CourseQuestionNodeObject`+`summarizeComments` · sort "Sôi nổi nhất" · notify-khi-trả-lời emit `CommentReply` chưa bắn) · 🔴SCHEMA MỚI (accept-answer/upvote/pin/@mention/presence — hỏi thầy). Component mới feature-local: `QaMessageBubble`·`QaInboxRow`·`QaConversationHeader`. Prototype :8080 (4 màn: inbox·hội thoại·tương-tác+chấm-data·mobile). Chờ chốt: route vs expand-inline · `CommentComposer`→`Composer` · làm lớp BE-rẻ luôn ko · lớp schema-mới cái nào · flatten reply · founder viền vs badge | 2026-07-19 |
| skills-graph | ⏳ PENDING (ĐA-PHASE, BE-heavy · deep-scan 2 agent xong) | Skills Graph cross-source (Option D full taxonomy thầy chốt): canonical `skills`+`skill_edges` entity · back-tag challenge/content (0 tag hiện) + normalize flashcard free-text tag→skill + coding-domain map · `user_skill_mastery_projection` (CDC, blend flashcard-retention+coding+interview+challenge+milestone) · FE `SkillGraph` block @xyflow (node=skill màu-mastery cụm-domain, click→panel bySource+CTA luyện). Prototype :8088. Chờ chốt: scope per-course/all · rubric-attr thành cụm riêng · back-tag tay/AI · blend weights | 2026-07-18 |
| rewards-categorized | ✅ DONE (tsc/eslint 0 · FE-only · runtime CHƯA verify, dev server session khác giữ) | Tab "Cửa hàng" (/vi/rewards) chia grid phẳng → SECTION theo hạng mục (`kind`: aiCredit+digital "Tăng tốc học"·voucher·physical); mỗi section=LabeledCard(icon+title)+grid Card. FE-only (group client-side `kind` đã fetch); i18n rewards.section.*; section rỗng tự ẩn. KHÔNG story (feature-comp, block canonical đã có). Chốt gộp-3 (không tách 4 kind) | 2026-07-17 |
| playground-guided-path | ⏳ PENDING (schema chưa build) · 🌱 CONTENT SEEDED (workflow 7 agent · 60 step = 20×3 dễ→khó, bilingual, 122 md, verify structure PASS) | 3 playground (Docker/K8s/RAG) → 1 lộ trình từng-bước CHUNG lưu `.mount`. Docker/K8s đã là step-path `.mount` (title/body/commandHint/verifyResource*); RAG là tool marketing riêng. Thiết kế: META thêm `# kind` (terminal|rag) quyết pane phải + họ verify; STEP hint đa hình (`commandHint`|`actionHint`+`sampleRef`/`askPrefill`) + verify đa hình (`verifyResource*`|`verifyKind: imported/asked/answered`). FE shell chung `PlaygroundPath` (pane trái step chung, pane phải widget theo kind: Terminal+Resources | RagPlayground 2-pane). RAG script hoá thành bước. Prototype `.artifacts/prototypes/playground-guided-path/` :8081. Chờ thầy chốt schema + RAG-giữ-landing-vs-thay + verifyKind depth. | 2026-07-17 |
| stats-canonical-fold | ✅ DONE (tsc/eslint 0 · runtime CHƯA verify HMR) — ⚠️ SCOPE HẸP HƠN đề xuất: đọc code thật thấy redesign ĐÃ gần canonical rồi (VerdictHeroCard ĐÃ ngồi trên `SectionCard withVerdict` — JSDoc "hand-roll border-l-4" là STALE; DeadlineCallout toàn token `bg-warning-soft` chuẩn). Chỗ tự-chế DUY NHẤT = vạch target vẽ tay → đã canonical-hoá: thêm prop `target`/`targetLabel` vào `ProgressMeter` (block canonical, tái dùng mọi nơi) + VerdictHeroCard dùng nó, xoá overlay tay + sửa JSDoc stale. KHÔNG retire block nào (đều đã canonical-based, retire chỉ lặp code). Story `ProgressMeter` +state `WithTarget` (news). Thầy chốt: canonical-ize-block(#1) + thêm target-prop(#2) + KHÔNG glow hero(#3, §3j trang trí nghịch tông data-signal danger). | 2026-07-17 |
| stats-insight-redesign | ✅ DONE (workflow 10 agent + fix course-scope · FE tsc/eslint 0 · BE tsc 0 ở file đụng · KHÔNG migration · schema live verified) — **Đợt 2 fix**: tab course-scoped nhưng gate+hero đọc `myFlashcardStats` LIFETIME (blend mọi khoá) → thêm `reviewedTotal`/`courseRetention` (jsonb, tính từ mature/young split sẵn có, 0 SQL mới) + guard drift ép recompute row cũ + siết `isLoading` (tránh nháy EmptyState oan) + copy empty nói rõ "trong khoá này". **Đợt 3 — BUG GỐC (có sẵn từ `b0c780f4`, KHÔNG do workflow)**: `computeQuiz` select `deckId: true` — nhưng `deckId` là `@RelationId` ảo, TypeORM find-select không nhận → `EntityPropertyNotFoundError` lúc RUNTIME → `recompute()` throw ngay fold ĐẦU TIÊN → **không row projection nào từng được ghi** → MỌI stats flashcard rỗng âm thầm từ ~2026-07-13 (row cũ 2026-07-11 thiếu `leechCards`/`quizHardCards` là dấu vết). Bắt bằng cách gọi thẳng `recompute()` qua scratch script (constructor chỉ cần EntityManager, khỏi boot Nest). Fix: bỏ dòng select thừa (0 code đọc `card.deckId`; deck lấy qua relation). Verify: recompute OK + row có data thật (reviewedTotal 62 · mature 48% vs young 29% · weakTags 11 · leech 8 · coverage 10/197). ⚠️ còn: mẫu số % leech vẫn dùng `gradeDistribution` lifetime (ngoài scope duyệt) — ⚠️ (a) `bestReviewHour` chưa render (BE trả 1 giá trị, copy cần morning/evening — chờ BE 2-bucket) · (b) BE edit bị commit `0c14e430` session khác cuốn vào (verify nội dung nguyên vẹn, git history không sạch) · (c) recurringGaps normalize exact-match (chưa cluster ngữ nghĩa) | Redesign 3 tab Thống kê (Ôn tập/Phỏng vấn/Hỏi nhanh) từ số-vanity → INSIGHT "verdict→evidence→action" (web research Anki/Duolingo/interviewing.io + deep-scan trần dữ liệu). Ôn tập ưu tiên 1: hero mature-vs-young retention + vs-target · điểm-yếu-theo-tag (bỏ `LIMIT 1` = gần free) · "sắp quên N thẻ trước thứ X" · độ chín ladder · leech-lapsed. Phỏng vấn: readiness-vs-passbar + lever + **byLanguage** (hợp multi-lang) + gaps lặp lại. Quiz: coverage-vs-target + difficulty-mix. Prototype `.artifacts/prototypes/stats-insight-redesign/` :8093. Ma trận build: quick-win 🟢 FE-only vs 🟠 aggregate-BE. Chờ thầy duyệt hướng + chọn scope. | 2026-07-17 |
| mind-map-journey | ✅ DONE (tsc/eslint sạch — runtime CHƯA verify, dev server session khác giữ; interactive nên thầy soi kỹ) | Vẽ lại mind-map → HÀNH TRÌNH serpentine (bỏ @xyflow cây liệt kê): auto-layout + scroll + progressive detail (scale 20+ module) · 2 lộ trình song song tab qua lại (📚 Học modules→lesson→challenge · 🏗️ Dự án milestones→task) · data 0-BE từ myCourseOutline · node click→surface, CTA resume | 2026-07-17 |
| league-card-community-tweaks | ✅ DONE | See-more→header + hàng-của-mình=surface-in-surface labeled list card + đồng nhất see-more="Xem bảng xếp hạng" (tab Cộng đồng) | 2026-07-17 |
| colored-left-border-card | ✅ DONE (tsc/eslint sạch · grep: 0 shape hand-roll sót — chỗ gọi chỉ còn class MÀU đúng thiết kế) — ⚠️ canon `card.md` §3i CẦN SỬA (ghi "block ColoredLeftBorderCard" nhưng thầy chốt KHÔNG tạo block, chỉ `withVerdict` field) · RatingBar KHÔNG migrate (host khác: stripe absolute trong GroupPressableCard) | Tách block `ColoredLeftBorderCard` (viền trái màu = tín hiệu DATA) — concept thầy đặt tên 2026-07-17, canon đã ghi `card.md` §3i (kèm phân định với rule §3g "cấm viền ad-hoc phân biệt vùng"). Đang hand-roll 3 nơi: `VerdictHeroCard` (band verdict, JSDoc tự thú thiếu block) · `RatingBar` (tier stripe SM-2) · `WeeklyBoard` (zone thăng/rớt). ⚠️ HOÃN migrate VerdictHeroCard — session khác đang viết story cho nó | 2026-07-17 |
| history-funnel-filter-relayout | ⏳ PENDING | Quiz+mock lịch sử theo chuẩn review (search+funnel toolbar, time-group, load-more button); filter dời vào phễu popover (như ProfileChallengeManage). Phase-2 opt: trích block chung HistoryListLayout | 2026-07-17 |
| league-page | ✅ DONE (tsc/eslint sạch — runtime CHƯA verify, dev server bị session khác giữ) | Redesign /vi/league business-standard: PageHeader+pill-tabs shell · StandingHeroCard+Podium (feature comp mới, KHÔNG story vì là feature không phải canonical block) · WeeklyBoard hero+meter+zone-tints+empty-phễu · GlobalBoard hero+podium+rank4-list-follow · i18n climbCta/pointsToNext/pointsToPromote/globalRankLine | 2026-07-17 |
| kpi-page | ✅ DONE (tsc/eslint sạch — runtime CHƯA verify, xem ghi chú) | See-more dời header (WeeklyGoals→OverviewTab) + gộp 6 box rời `/kpi` thành 1 SurfaceListCard (fix vi phạm card.md §1) + PageHeader/ResponsiveBreadcrumb | 2026-07-17 |
| league-consistency | ✅ DONE (tsc/eslint sạch · grep self-critique: 0 fill sót · runtime thầy tự soi HMR :3000) | Sửa DRIFT accent-system §5: badge hero bare fluent-emoji trophy (bỏ IconTile cam) · WeeklyBoard zone gate `promote+demote<total` + LeagueRow edge-marker `border-l` thay bg-flood (fix cohort-nhỏ degenerate) · my-row ring-accent+value không fill (self-card+global-list dùng value-accent) · Podium champ ring-inset+số accent bỏ bg-accent-soft · FollowButton +prop `quiet` bật ở GlobalBoard+TopLearners · i18n promoteZone/demoteZone | 2026-07-17 |
| dashboard-league-unify | ✅ DONE (tsc/eslint sạch · grep: 2 container đều render LeaderboardListCard, legend+tier-badge gone · LeagueRow xóa dead code · runtime thầy soi HMR :3000) | 2 card dashboard Cộng đồng render Y CHANG qua block chung `LeaderboardListCard`: standing = IconTile rank-driven (🥇 League-tuần / 🏆 Top-học-viên) · medal-list + trailing slot (caret/follow) · BỎ legend + tier-badge · 2 container rút gọn thành mapper | 2026-07-17 |
| dashboard-league-compact | ✅ DONE (tsc/eslint sạch · grep: self-card gone, medal chỉ hit 2 card dashboard không đụng /league · runtime thầy soi HMR :3000) | Đồng bộ 2 card dashboard Cộng đồng với trang /league: top-3 = 🥇🥈🥉 medal-in-list (KHÔNG podium) · viewer IN-PLACE (bỏ card "Hạng của bạn" tách rời, fix list-bắt-đầu-từ-2) · self-row ghim khi ngoài top · cả LeagueCardContent + TopLearners | 2026-07-17 |
| league-unify | ✅ DONE (tsc/eslint sạch · grep: Podium 4 importer OK, 0 bg-fill · runtime thầy soi HMR :3000) — ⚠️ dashboard League tuần GIỮ tier badge (flag hỏi thầy, xem ghi chú) | Relayout ĐỒNG BỘ 2 board + ground block canonical: badge = `IconTile` CÓ BG cả 2 tab (fix vi phạm icon.md §4; weekly=tier-art-in-tile, global=trophy) · BỎ Podium (global = hero+list y hệt weekly) · list = `SurfaceListCard` cả 2 (was weekly free-floating) · meter = `ProgressMeter` (bỏ hand-roll) · dashboard badge → IconTile khớp trang | 2026-07-17 |
| kpi-weekly-reset-countdown | ✅ DONE (BE+FE tsc/eslint sạch — runtime CHƯA verify, dev server bị session khác giữ) | Đổi 6 KPI từ rolling-7-day → tuần lịch thật (Mon 8h GMT+7, mirror league-reset) + field `weeklyStudyDays` riêng + render "Reset sau Xd Xh" (dashboard + /kpi), mirror WeeklyBoard countdown | 2026-07-17 |
| kpi-reward-coins | ✅ DONE (BE+FE tsc/eslint sạch — runtime CHƯA verify; ⚠️ CẦN `ALTER TYPE` tay trước khi chạy, xem ghi chú) | Claim coin per-KPI khi đạt target, thưởng scale theo target — chống lách bằng floor=target thấp nhất trong tuần; mirror hạ tầng DailyQuest claim; bảng hệ số coin CHỈ LÀ khung, cần thầy tune số | 2026-07-17 |
| coin-rewards-expansion | ✅ DONE (BE+FE tsc/eslint sạch; ⚠️ RỦI RO SCHEMA THẬT trước khi restart BE server, xem ghi chú) | Bỏ icon coin khỏi badge KPI · streak daily bonus MỚI (5 coin/ngày giữ streak) · weekly-challenge claim MỚI (40 coin/tuần) · TÁCH HẲN `CoinSource`+`coin_histories` khỏi `XpSource`/`xp_histories` (thầy chốt: coin-only ≠ XP) — cả DailyQuest+StreakMilestone cũ CŨNG chuyển sang, XpSource giờ chỉ còn 6 value XP thật | 2026-07-17 |
| coin-xp-economy-numbers | ✅ DONE (Opus brainstorm + áp dụng số + t-shirt repriced + vá flashcard + sửa kiến trúc Points + dọn tàn dư CV) | Chốt số placeholder: weeklyChallenge 30→40 · kpiReward.flashcards 0.3→0.2 · streakDailyBonus giữ 5 · t-shirt 1500→11000 (cày 6 tháng) · vá flashcardQuizSession=5/flashcardFirstReview=1 coin (trước đó XP-only) · SỬA KIẾN TRÚC: `totalPoints` tính live từ `SUM(amount) FROM xp_histories` (bỏ counter `users.total_points`) · DỌN TÀN DƯ (thầy: "xóa hết, lỗi sửa sau"): xoá field `total_points` (migration DROP COLUMN) + xoá HẲN 2 bảng legacy `cv_submissions`/`cv_submission_attempts` (query cvUrl/userCvSubmissionAttempts, mutation verifySubmitCvPresignUrl, 2 exception — đều 0 consumer FE thật) + sửa generateSubmitCvPresignUrl (bỏ đọc/ghi bảng cũ) + BỎ 1 tính năng thật (nhánh "cv" feedback trong learner-CMS feed, chấp nhận mất tạm) — 2 migration DROP mới CHƯA CHẠY trên DB thật · ⚠️ flag còn lại: dailyQuest=20 vòi thu nhập thống trị (chưa đổi) + data lệch flashcards default-target(20)<preset thấp nhất(50) | 2026-07-17 |
| mock-interview-workspace-fill | ✅ DONE (tsc/eslint sạch · grep: 0 fixed-height sót · runtime CHƯA verify, dev server bị session khác giữ) | Fix "chưa gọn/cân đối" màn Phỏng vấn thử: `MockInterviewWorkspace`/`MockInterviewDiagram` đổi `h-72`/`h-64 sm:h-80` (hộp cố định) → `h-full`/`flex-1 min-h-0` (fill hết cell đã khoá full-viewport) — áp cho CẢ `qna` lẫn `design` (design trước đó CHƯA khoá viewport height, giờ khoá luôn theo mirror qna, nếu không workspace `h-full` sẽ collapse) · cột trái `qna` thêm `justify-center` (cột thưa, tránh ghim-trên-đỉnh) · cột trái `design` GIỮ top-anchored (thread chat dài dần, không center) · tỉ lệ giữ 1:1 (thầy chưa chọn 2:3, prototype vẫn còn để so sau). Không có Storybook story (feature-local, không có `.stories` từ trước). **Đợt 2 (thầy: "code terminal xấu"):** ban đầu chỉ vá spellcheck (squiggly đỏ = browser tự spellcheck textarea ẩn của Monaco, không phải Monaco lint) + thêm `bracketPairColorization`/`cursorBlinking:smooth`. **Đợt 3 (thầy trỏ trang theme gallery `react-codemirror`, "dùng cái này thay đi"):** ĐỔI HẲN LIB Monaco → CodeMirror (`@uiw/react-codemirror` + theme `@uiw/codemirror-theme-white`/`@uiw/codemirror-theme-vscode`) chỉ ở `MockInterviewWorkspace` — CM6 tắt spellcheck NATIVE sẵn (bug biến mất luôn, không cần patch tay). Ngôn ngữ: TS/Java = package chính chủ, Csharp/Go = `@codemirror/legacy-modes` (StreamLanguage fallback, chưa có CM6-native). `PracticeProblem`/`CvBlocksWorkspace` CHƯA đổi theo (ngoài phạm vi feedback lần này) — `PracticeProblem` vẫn còn task nền `task_bc0894ee` (chỉ vá spellcheck, không đổi lib) đang chạy riêng. Rule ghi ở `.claude/fe/features/mock-interview.md`. **Đợt 4 (thầy khoanh ảnh: dropdown raw-key `programmingLanguage.dockerfile` + chip thừa, "học tư duy thẩm mỹ bố trí lại"):** phát hiện bug SÂU hơn cosmetic — `codeState.lang` từng ép về `ProgrammingLanguage` (4 giá trị) nên given-code ngoài 4 track (vd Docker `givenLang: "dockerfile"`) bị coerce sai về TypeScript, khiến **transcript chấm điểm cũng sai ngôn ngữ** không chỉ riêng UI. Fix: đổi `lang` sang `string` thô + `languageExtensionFor` thêm case `dockerfile` (fallback KHÔNG highlight cho ngôn ngữ lạ, thay vì tô sai màu) + picker chỉ hiện khi ≥2 variant (1 variant → label tĩnh, hết bug raw-key) + gộp toolbar+editor 1 card viền chung + xoá hẳn chip "Code đã nạp vào tab Code" (stale, nói tab đã bỏ 2026-07-09) + xoá 2 key i18n chết. Khảo sát: 17 giá trị `givenLang` thật đang authored xuyên khóa, mới cover thêm 1 (`dockerfile`) — 13 cái còn lại CHƯA có highlight riêng (liệt kê, chưa tự làm). tsc/eslint sạch. **Đợt 5 (thầy: "cho user chọn nhiều lang, random 1 trong số lang user chọn"):** FEATURE BE+FE — setup chọn NHIỀU lang (nâng `FlexWrapButtonRadio` thêm mode multi-select, giữ 14 caller single cũ), server draw RANDOM 1 lang/câu trong {user chọn ∩ câu author}; câu không giao lang nào → LOẠI khỏi pool bốc câu khác (thầy: "không support gen câu khác"); non-track (docker) + no-code luôn eligible. **Kèm fix chấm điểm** (grade resolve body theo `seed.givenCodes[0].lang` lang-đã-random-lưu, KHÔNG `session.lang` — nếu bỏ sẽ chấm câu-Go bằng idealAnswer-TS). BE 5 file + FE (MockInterviewSession + FlexWrapButtonRadio block + story `news` + mutation type + i18n). BE tsc 0 lỗi interview, FE tsc/eslint 0. CHƯA boot BE runtime (port bị giữ) + CHƯA verify browser. Rule `.claude/fe/features/mock-interview.md`. **Đợt 6 (thầy: "render nguyên cái surface bên phải có border, gap-6 render code/công cụ bên trong"):** pane workspace (phải) giờ = 1 surface `rounded-2xl border border-default bg-surface` fill full height, tool render bên trong `p-6`; tool bỏ border riêng (MockInterviewWorkspace bỏ khung code-card + header-fill, MockInterviewDiagram bỏ border ngoài — tránh border-in-border); áp cả qna+design, EmptyState cũng trong surface. Đảo note "no card frame pane phải" (2026-07-13) → Đính chính ở `.claude/fe/layouts/full-bleed-work-surface.md` (chỉ 1 bên có frame nên không phải "2 card phèn"). tsc/eslint 0. **Đợt 7 (thầy đính chính: "height full, sát mép phải, bên trái có border"):** KHÔNG phải card bo góc nổi — đổi thành PANEL DOCK kiểu IDE: pane phải tràn sát mép phải+trên+dưới viewport, chỉ `border-l`, `p-6` bên trong. Bỏ padding ngang khỏi grid container → pane trái tự mang padding+scroll, pane phải border-l + fill row. Mobile: `border-t` + `min-h-[28rem]`. Canon cập nhật (2 nhịp, giữ nhịp cuối). tsc/eslint 0. | 2026-07-17 |
- ✅ DONE **mind-map-rag-drawer** (2026-07-17) — ConceptMap node-click → `MindMapNodeDrawer` (View presentational + container SWR) RAG grouped 4 loại; thay panel authored-link cũ. tsc/eslint sạch; endpoint validate (runtime cần auth → thầy soi browser). Target thật = ConceptMap (không phải Canvas/derived như file-list proposal). Story news View 4 state.
| code-problems | ✅ DONE (tsc 0 · eslint 0 · runtime CHƯA verify, dev server session khác giữ) — ⚠️ 2 điểm thu hẹp vs proposal: (1) 2-pane KHÔNG resizable (fixed lg:grid-cols-2 — resizable-split chưa có block canon, follow-up) (2) "Chạy thử" disabled + hint giải thích (cần BE judge-sample, follow-up). KHÔNG đụng BE: query đã fetch sẵn perCaseResults/limits | Redesign màn giải bài `/practice/[slug]` (hệ A coding-practice): 2-pane IDE + BackLink · left ExtendedTabs (Mô tả/Lời giải/Lần nộp) + IOExampleCard thay `<pre>` thô · right Monaco (giữ logic) + CodeConsole (Test case/Kết quả) + TestCaseResultGrid render `perCaseResults` (đã persist, trước NAY vứt) + StatGridCard runtime/mem + limits. 3 block mới `blocks/code/*` + 3 story news. Prototype :8087 | 2026-07-18 |
| mind-map-concept-redesign | ✅ DONE (tsc+eslint sạch FE+BE · story news WithDescription · runtime CHƯA verify: port session khác giữ + CẦN re-seed course.mind_map từ mind-map.json mới + xoá Redis CourseMindMap cache để data vào DB · @dagrejs/dagre giờ 0 user, cân nhắc gỡ dep) — layout B (clustered keyword-tree, thay dagre rank-packing) + node=keyword trần + click→MindMapNodeDrawer (RAG, đã có). Đổi schema `mind_map`→cây keyword thuần (bỏ links+desc, optional nên không đổi entity/GraphQL). Khóa chưa authored→empty-state phễu (BE trả rỗng thay derived graph). JSON regen: 2 Opus agent đang chạy (0-fullstack viết lại + 2-devops mới); SD/ai-llm/claude lượt sau. Files: `ConceptMap/layout.ts`(core clustered) + `ConceptMap/index.tsx`(node keyword) + `MindMap/index.tsx`(empty) + BE `course-mind-map.service`(null→rỗng). Mobile=hướng C. Prototype :8095. Gộp `mind-map-rag-drawer`(DONE). | 2026-07-18 |
- ✅ DONE **playground-connect-flow** (2026-07-17) — BE emit agent:connected/disconnected + OnGatewayDisconnect (verified 3-event runtime + DB flip) · FE hook fix byomState.connected=agent-paired-thật · install-gate overlay + waiting/connected StatusChip · lệnh copy BARE. tsc/eslint sạch. Inline install-card (không tạo block mới → no story).
- ✅ DONE **cv-full-latex** (`cv-full-latex.proposal.md`, build 2026-07-18 — tsc BE+FE sạch · runtime CHƯA verify: cần tectonic trên BE thật + login) — đảo pivot CV render HTML-first → **full LaTeX** (giữ block-editor). BE: handler `.tex`→`tectonic -X compile --untrusted`→PDF→MinIO (bỏ Puppeteer/html-to-docx) · entity `cv_blocks`+`tex_source`+migration `1725...` · exception `CvLatexCompileFailedException` · request `html`→`tex`. FE: `buildCvTexSource` (port 8 block-renderer→LaTeX, 1 template, honour accent/fontScale) · toggle Khối↔LaTeX (CodeMirror stex) · `CvPdfPreview` compile-on-change (debounce 1.5s→embed presigned PDF) thay CvHtmlPreview (đã xoá) · bỏ Word +nút "Tải .tex" · `CvDocument+texSource`. ⚠️ Dockerfile KHÔNG đổi (tectonic đã có). ⚠️ Verify runtime: cú pháp `-X compile` trên tectonic image + network egress tải bundle lần đầu. ⚠️ đảo pivot HTML-first (proposal cũ) — không còn HTML render/Word.
| mind-map-node-ring | ✅ DONE (tsc+eslint sạch FE+BE · BE serve `popularity` verified qua API: fs 187 node dist high55/med89/low42 · BE --watch tự recompile, DB re-inject 3 khóa · FE HMR 0 error · runtime map cần login → thầy soi) — node card + ring màu theo `popularity` (high=xanh/success · medium=vàng/warning · low=đỏ/danger) + chấm tier a11y; cụm nút lookup (React Flow `Panel`): search keyword→`setCenter` focus · filter tier · Fit · `MiniMap`. Nguồn=A author tay (Sonnet, DONE: fs 55/89/42 · do 66/100/64 · sd 85/77/25 → `popularity` đã merge vào 3 JSON). Build: BE thêm `popularity` (entity `CourseMindMapNode` + `MindMapNodeData` response + FE type + query + service map) → re-inject DB → FE `ConceptNode` ring + `MindMap/MindMapToolbar`(mới) + `<MiniMap>`. Prototype :8096. | 2026-07-18 |
| playground-connect-sheet | ✅ DONE (tsc+eslint sạch · runtime CHƯA verify: dev server thầy giữ + cần login) — tách connect UI khỏi cột trái → block MỚI `ConnectSheet` (draggable bottom-sheet, 2 snap peek↔expanded, pointer-drag + tap/keyboard toggle, rounded-t-3xl, no framer-motion). PlaygroundSession: grid `relative` + sheet dock đáy, auto-snap per phase (setup/connected=thu · waiting/disconnected=bung · rag=ẩn); peek=StatusChip(+latency)+action, body=intro/installCard; cột trái còn guide+confirm/hint; pane pb-24/pb-20 clear peek. i18n +machineNotConnected/connectFromSheetHint/sheetToggle. Story ConnectSheet = follow-up. Prototype :8093. | 2026-07-18 |
| playground-connect-device | ✅ DONE (tsc+eslint+build sạch cả 3 repo · runtime device:info+agent:log relay→browser PASS: win32 i7-14700KF 28c 69GB RTX5060 + 3 log lines · GPU filter bỏ adapter ảo) (playground-connect-device.proposal.md) — connected-state ConnectSheet: latency badge (peek) + device config tiles OS/CPU/RAM/GPU (StatGridCard) + agent-log stream (AgentLog block). Data từ AGENT (os+gpu+wrap-log), Nest relay 2 event (device:info→agent:device, agent:log). Agent code + republish · BE relay 2 handler · FE hook+sheet body. Prototype :8082. | 2026-07-19 |
| playground-rag-ollama | 🔨 IN-PROGRESS — P1 FE blocks+Nguồn2mode+Prepare(+VRAM) ✅ · P2 BE 6 socket relay ✅ · P3 agent RAG engine ✅ smoke-test PASS thật · P3b VRAM+model động ✅ · P4b FE types+hook+RagWorkspace+**RagSession**+route ✅ tsc sạch. **CÒN: reconcile agent package (playground-rag-agent vs playground-agent) + verify browser end-to-end (agent+Ollama+login)** |
- ✅ DONE (2026-07-20, tsc+eslint sạch · runtime thầy soi HMR · scope revised — xem cuối) — **playground-byom** — setup-first flow, spec `playground-byom.proposal.md`, prototype `.artifacts/prototypes/playground-byom/`. **BUILT:** `PlaygroundPrepare` + prop `flavor:"infra"|"ollama"` (infra = 2 bước cài-engine+pair, Callout CPU/RAM, không model-pull; ollama giữ nguyên 3 bước) + i18n vi/en (`introInfra`/`stepEngineTitle`/`machineDetected`) + story `news` `Infra` — tsc+eslint sạch, backward-compat (flavor default "ollama"). **SCOPE REVISED khi soi code thật (2026-07-20):** KHÔNG wire Prepare-gate vào `PlaygroundSession` — course Session **cố ý** pair inline qua `ConnectSheet` (installCard = Callout+lệnh-pair+`pairingRequirement`, agentPackage branch theo kind, device tiles+agent log), build ở `playground-connect-sheet`+`playground-connect-device` (DONE); RAG không dùng sheet (`if(isRag) return`) → dùng `RagPlayground` embedded + `PlaygroundPrepare` ở route standalone `/rag-playground`. Nhét Prepare-gate vào Session = **duplicate/regress ConnectSheet**. Course flow đã handle setup đúng cho cả 3 kind. → `PlaygroundPrepare` flavor phục vụ route standalone (DONE); Session KHÔNG đụng. Hub render RAG card qua SWR (không cần sửa). Open: `PlaygroundHub` PageHeader/breadcrumb (giữ h5). **Kết: DONE trong scope đúng.**
- ✅ DONE (2026-07-20, tsc+eslint sạch · runtime thầy soi HMR :3000) — **playground-setup-vs-lab** (`playground-setup-vs-lab.proposal.md`, prototype :8082) — PA-3: tách Setup ↔ Lab thành 2 surface trên CÙNG route `[slug]` bằng state `phase`. Vấn đề gốc thầy bắt qua ảnh: docker/k8s ném thẳng vào lab với **pane phải chết**, setup nhồi thanh đáy, KHÔNG có bước cài Docker; RAG thì có gate tử tế → bất nhất. **Build:** `PlaygroundSession` + `phase:"prepare"|"session"` · header `ExtendedTabs` switch (Chuẩn bị · Lab) · auto-latch vào Lab lần pair đầu (ref, không đè switch tay) · pha prepare render `PlaygroundPrepare` (flavor infra cho docker/k8s, ollama cho rag) · `OS_INSTALL` per family (docker/kind+kubectl/ollama) · readiness neo field THẬT (`connected`/`deviceInfo`/`ollamaStatus.models`, KHÔNG bịa "Docker engine running") · **ConnectSheet thu hẹp thành status strip** (bỏ `installCard`, body = device tiles + agent log; peek "Đổi máy" → về prepare) · ProgressMeter/step-counter chỉ hiện ở Lab. i18n vi+en 7 key (`phasePrepare/phaseLab/phaseSwitchAria/readyDevice*/reconnectFromPrepareHint`), tái dùng 12 key `ragSession.ready*`. ⚠️ **Đổi hành vi:** session + pairing code giờ **tự tạo khi mở lab** (ref-latch 1 lần/mount) thay vì sau nút "Kết nối máy" — vì màn Setup phải hiện lệnh pair ngay. ⚠️ Sửa trên `PlaygroundSession/index.tsx` vốn đang M của session `playground-rag-ollama` (thầy xác nhận session đó nghỉ); đã backup untracked ra scratchpad trước khi sửa.
- ✅ DONE (2026-07-20, tsc+eslint sạch · runtime thầy soi HMR) — **playground-split-routes** (`playground-split-routes.proposal.md`, prototype dùng lại :8082) — tách Setup ↔ Lab từ state `phase` sang **2 ROUTE thật**: `[slug]/layout.tsx` (MỚI, `PlaygroundSessionProvider` giữ socket+session) · `[slug]/page.tsx` = Chuẩn bị (GIỮ URL cũ = entry, không vỡ link Hub) · `[slug]/lab/page.tsx` (MỚI) = Lab. Lý do: state-toggle làm **Back thoát hẳn lab**, **F5 văng về Chuẩn bị**, **không deep-link** được. ⚠️ **CHẶN**: `usePlaygroundByomSocketIo()` đang gọi TRONG component (`PlaygroundSession:161`, `PlaygroundRagSession:76`) và `[slug]` KHÔNG có layout → tách thô = unmount = **đứt socket, mất pairingCode** ⇒ học viên phải chạy lại `npx`. Phải LIFT socket/session lên layout+context TRƯỚC. RagSession cũng phải đọc context (nếu không = 2 socket song song). Verify chốt: pair xong tự sang `/lab` mà KHÔNG chạy lại lệnh + Back/Forward + F5 đúng màn, test cả 3 slug. Thầy chốt (B) 2026-07-20: chỉ proposal, build session sau. **BUILT:** `PlaygroundSessionProvider` (MỚI — socket+session+readiness+flavor, là chủ DUY NHẤT của `usePlaygroundByomSocketIo`) · `[slug]/layout.tsx` (MỚI) · `[slug]/page.tsx` = Setup (giữ URL entry) · `[slug]/lab/page.tsx` (MỚI, guard chưa-pair→về Setup) · `PlaygroundHeader` (MỚI — BackLink + tab Chuẩn bị·Lab điều hướng THẬT + counter/ProgressMeter chỉ ở Lab) · `pathConfig().…playground(slug).lab()` (MỚI) · `PlaygroundSession` 807→~660 dòng và `PlaygroundRagSession` bỏ socket/session/gate riêng, đọc context. `learn/layout.tsx` KHÔNG phải sửa (`segments[1]` đã match cả `/lab`). ⚠️ **Giới hạn còn lại:** F5 tạo session MỚI (mã pairing mới) nên guard sẽ đẩy về Setup và học viên phải chạy lại `npx` — muốn F5 giữ pairing phải persist `sessionId` (localStorage/URL) + re-subscribe, KHÔNG nằm trong proposal này.
- ✅ DONE (2026-07-20, tsc 0 lỗi toàn repo FE · eslint sạch · ⚠️ runtime CHƯA verify — route Setup cần đăng nhập, Storybook không drive qua Browser pane theo feedback cũ) — **playground-prepare-step-actions** (`playground-prepare-step-actions.proposal.md`, prototype `.artifacts/prototypes/playground-prepare-states/` :8081) — thầy chốt PA **A** 2026-07-20. Card step trong `PlaygroundPrepare` đang 7 tầng dọc, 2 nút (`Lấy mã mới` + `Kiểm tra lại`) bị dòng hint chen giữa vì sinh ở 2 nhánh render khác nhau (`step.body` vs footer dùng chung) → gom về **1 hàng hành động cuối card** qua field mới `step.actions`; card thành 4 vùng cố định (explain+chip → body → meta → actions → kết quả). Kèm: gộp countdown+hint thành 1 dòng meta (giữ đổi tông danger ≤60s) · `Kiểm tra lại` `secondary`→`tertiary` (button.md §1: không có primary trong cụm) · cả 2 nút bỏ tự-chế `isDisabled`+ternary-spinner → prop `isPending` HeroUI + render-prop Spinner (button.md §6c) · state ⑤ bỏ dòng cảnh báo thường trực `pairCodeRefreshWhileConnected` → **ConfirmDialog tone default lúc bấm** · state ① (đang mint mã) **MỚI** — hiện code render `pairCommand` cả khi chưa có mã, cần skeleton. 8 state, 8 story (4 sửa + 4 mới `news`). Tổ hợp bất khả: hết-hạn+đã-pair (`pairingCodeExpired` có `!connected`). ⚠️ Ngoài phạm vi, đã báo chưa chốt: `deviceInfo`/`envReport` KHÔNG bị clear khi disconnect (cố ý, phục vụ resume) → sinh state "xanh cũ" ở khối Trạng thái máy.
