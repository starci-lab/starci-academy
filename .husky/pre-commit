npx lint-staged

# Deps link trong panel anatomy: `storyId` trỏ sai thì KHÔNG có gì đỏ — không tsc,
# không eslint, không build. Chỉ lúc bấm mới biết. Nên chặn ở đây (continue.md #4).
node scripts/check-story-ids.mjs

# Mot state = MOT instance. Do ca union prop vao mot state thi `why` phai ta nam thu nen
# ta khong ro thu nao, `code` het copy-chay-duoc, va story buoc phai go tay `flex gap-3`
# de xep hang. Cong chi do khi mang duoc `.map` la mang CHUOI (gia tri prop); mang OBJECT
# la du lieu that, duoc phep nhieu hang (rules/2 §8).
node scripts/check-one-instance-per-state.mjs

# Doc-parity: block /** */ dau file COMPONENT (impl, khong tinh barrel) phai TRUNG
# block dau STORY (.stories.tsx) — story la nguon-su-that (thanh autodocs). Drift =
# doc code va doc thiet ke lech nhau, khong tsc/eslint nao bat. Sua: npm run check:doc-parity -- --fix
node scripts/check-doc-parity.mjs

# P1: cai gi render SHIMMER thi prop = `isSkeleton` (thread xuong). `isPending` (nut
# dang chay -> spinner) va `isLoading` (switch AsyncContent) la khai niem KHAC, khong bi
# cam. Vi pham = feed khai niem non-skeleton vao `isSkeleton={...}`.
node scripts/check-skeleton-prop.mjs
