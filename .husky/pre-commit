npx lint-staged

# tsc --noEmit — build type-check CO Y tat (ignoreBuildErrors), nen day la cho DUY NHAT
# type sai bi bat truoc CI. Bao gom type-guard PrincipleToken (token principles bia = do).
npx tsc --noEmit

# design gates removed from FE: they duplicated the backend canon SSOT (.claude/scripts/gates).
# pre-commit keeps only lint-staged (eslint) + tsc; run the canon gates from the backend repo.
