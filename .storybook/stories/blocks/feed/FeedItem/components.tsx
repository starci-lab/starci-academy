/**
 * A row that recounts an EVENT that already happened, with a timestamp. Choose FeedItem over
 * ListRow when the row is READ-only — FeedItem has no onPress/href, whereas ListRow is clickable
 * and leads somewhere.
 */
export const usage = "A row that recounts an event that already happened, with a timestamp (who did what, and when). Choose FeedItem over ListRow when the row is READ-only: FeedItem has no onPress or href, whereas ListRow is clickable and takes the user somewhere. If you need a vertical connector between rows, wrap them in a Timeline."
