"use client"

import React from "react"
import { Flashcards } from "@/components/features/learn/Flashcards"

/**
 * Learn / flashcards — mock INTERVIEW mode: a fixed-length random voice session
 * over the whole course. Mode = the route slug (`…/flashcards/interview`).
 */
const Page = () => {
    return <Flashcards />
}

export default Page
