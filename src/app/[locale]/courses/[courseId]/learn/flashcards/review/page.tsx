"use client"

import React from "react"
import { Flashcards } from "@/components/features/learn/Flashcards"

/**
 * Learn / flashcards — STUDY (review) mode: today's due queue + mastery overview,
 * a deck's SM-2 reviewer, or the cross-deck due session. Mode = the route slug.
 */
const Page = () => {
    return <Flashcards />
}

export default Page
