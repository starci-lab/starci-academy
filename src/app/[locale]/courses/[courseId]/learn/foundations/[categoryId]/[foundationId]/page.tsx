"use client"

import React from "react"
import { FoundationResourcePage } from "@/components/pages/FoundationResourcePage"

/**
 * Learn / foundations / [categoryId] / [foundationId]: the dedicated resource page
 * (markdown / video). Reached by clicking a Document/Video resource in the list.
 */
const Page = () => {
    return <FoundationResourcePage />
}

export default Page
