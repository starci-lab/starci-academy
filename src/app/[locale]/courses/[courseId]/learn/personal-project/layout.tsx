"use client"

import React from "react"
import { PersonalProjectWorkspaceLayout } from "@/components/layouts/PersonalProjectWorkspaceLayout"

/**
 * Learn / personal-project route layout. Everything the workspace needs — the
 * milestones fetch included — lives inside {@link PersonalProjectWorkspaceLayout};
 * a route file names its Layout and nothing else.
 */
const Layout = () => <PersonalProjectWorkspaceLayout />

export default Layout
