"use client"
import React from "react"

/**
 * Route `…/personal-project/tasks/[taskId]/result` — the dedicated task grading
 * result page. The personal-project layout renders `PersonalProjectWorkspace`,
 * which detects the `/result` segment and mounts `PersonalProjectTaskResult`, so
 * this route file is just an empty marker (like the task page).
 */
const Page = () => {
    return <></>
}

export default Page
