"use client"

import React from "react"
import { PlaygroundSessionPage } from "@/components/pages/PlaygroundSessionPage"

/** Learn / playground / [slug] / session — the exercise's work surface. */
const Page = () => <PlaygroundSessionPage />

export default Page
