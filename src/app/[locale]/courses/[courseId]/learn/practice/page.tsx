import React from "react"
import { Practice } from "@/components/features/practice"

/**
 * Learn / practice: course-level coding-practice tab (problem bank). Renders the
 * shared practice catalog feature. Thin route file: mounts the feature, no logic
 * here.
 */
const Page = () => {
    return <Practice />
}

export default Page
