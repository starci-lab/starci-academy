import { Chip } from "@heroui/react"
import type { ChipRootProps } from "@heroui/react"
import React from "react"

export const StarCiChip = (props: ChipRootProps) => {
    return <Chip {...props} />
}
