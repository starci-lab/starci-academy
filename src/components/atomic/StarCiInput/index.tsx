import { Input } from "@heroui/react"
import type { InputRootProps } from "@heroui/react"
import React from "react"

export const StarCiInput = (props: InputRootProps) => {
    return <Input {...props} />
}
