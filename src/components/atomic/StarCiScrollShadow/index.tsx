import React from "react"
import { ScrollShadow, ScrollShadowProps } from "@heroui/react"

export const StarCiScrollShadow = (props: ScrollShadowProps) => {
    return (
        <ScrollShadow {...props}/>
    )
}