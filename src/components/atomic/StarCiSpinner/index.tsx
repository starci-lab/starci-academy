import { Spinner } from "@heroui/react"
import type { SpinnerRootProps } from "@heroui/react"
import React from "react"

export const StarCiSpinner = (props: SpinnerRootProps) => {
    return <Spinner {...props} />
}
