/**
 * Placeholder keys for the loading skeleton grid — one entry per skeleton card
 * shown while the tiers query is loading.
 */
export const TIER_SKELETON_PLACEHOLDERS: Array<number> = [
    1,
    2,
    3,
    4,
]
