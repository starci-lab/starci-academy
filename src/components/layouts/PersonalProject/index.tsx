"use client"

import React from "react"

/**
 * @deprecated Layout and task rendering have been moved to the route-level
 * `layout.tsx` / `[taskId]/page.tsx` architecture.
 */
export const PersonalProject = () => {
    return null
}
