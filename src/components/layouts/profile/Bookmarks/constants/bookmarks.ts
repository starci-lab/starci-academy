/**
 * Placeholder keys for the loading skeleton — one entry per skeleton card shown
 * while the saved-contents query is loading.
 */
export const BOOKMARK_SKELETON_PLACEHOLDERS: Array<number> = [
    1,
    2,
    3,
]
