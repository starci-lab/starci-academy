/** Root `Tabs` classes — HeroUI custom-styles pill tabs. */
export const PROGRAMMING_LANGUAGE_TABS_ROOT_CLASS_NAME = "w-fit text-center"

/** `Tabs.List` slot overrides (compact pill tabs + accent foreground when selected). */
export const PROGRAMMING_LANGUAGE_TABS_LIST_CLASS_NAME =
    "w-fit  *:w-fit *:px-3 *:text-sm *:font-normal *:data-[selected=true]:text-accent-foreground"

/** Active tab pill fill — matches HeroUI custom Tabs example. */
export const PROGRAMMING_LANGUAGE_TABS_INDICATOR_CLASS_NAME = "bg-accent"

/** Root `Tabs` classes for secondary (underline) variant — full-width content tab bar. */
export const PROGRAMMING_LANGUAGE_TABS_SECONDARY_ROOT_CLASS_NAME = "w-full"

/** `Tabs.List` slot overrides for secondary variant (edge-to-edge underline row). */
export const PROGRAMMING_LANGUAGE_TABS_SECONDARY_LIST_CLASS_NAME = "w-full border-b-0!"

/** Per-tab classes for secondary variant — accent underline when selected. */
export const PROGRAMMING_LANGUAGE_TABS_SECONDARY_TAB_CLASS_NAME =
    "rounded-none data-[selected=true]:border-b-2 data-[selected=true]:border-accent data-[selected=true]:text-accent"
