/**
 * Day of week for recurring livestream slots (`day_of_week` / Nest `DayOfWeek` / GraphQL `DayOfWeek`).
 */
export enum DayOfWeek {
    Sunday = "sunday",
    Monday = "monday",
    Tuesday = "tuesday",
    Wednesday = "wednesday",
    Thursday = "thursday",
    Friday = "friday",
    Saturday = "saturday",
}
