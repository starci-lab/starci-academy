/**
 * Canonical programming-language keys for lesson/challenge tabs (matches mount/CDN `lang` fields).
 */
export enum ProgrammingLanguage {
    /** TypeScript implementation track. */
    TypeScript = "typescript",
    /** Java implementation track. */
    Java = "java",
    /** C# implementation track. */
    Csharp = "csharp",
    /** Go implementation track. */
    Go = "go",
}
